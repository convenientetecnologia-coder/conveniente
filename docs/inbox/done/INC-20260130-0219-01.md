### INC-20260130-0219-01 — P0: governor light/full (somente RAM) + snapshot 1/min por 48h

- **P**: P0
- **Data**: 2026-01-30
- **Sistema**: `conveniente`

---

## Status (rígido)

- **Estado atual**: `done`
- **Rollout**: `deployed_partial`
- **Validation**: `not_run` (monitoramento 48h em andamento)

---

## Pedido do humano (regras)

1) **Remover qualquer mecanismo de “fechar todos / abrir todos / reiniciar/resetar”** dentro do governor.
2) **Modo leve/full apenas por RAM**:
   - **light**: `freeMB < 2048` por **5 minutos**
   - **full**: `freeMB >= 2048` por **5 minutos**
3) **Telemetria por 48h**:
   - a cada **1 minuto**, registrar snapshot contendo **freeMB** e **modo** (light/full)
   - objetivo: descobrir picos/horários e quantificar tempo em light/full antes de novas decisões.

---

## Implementação (aplicada)

Arquivo: `C:\conveniente\scripts\worker.js`

- Governor **RAM-only** para transição de modo:
  - `CT_GOV_ENTER_CONFIRM_MS` default 5min
  - `CT_GOV_EXIT_CONFIRM_MS` default 5min
  - threshold default `CT_GOV_MEM_ENTER_MB/EXIT_MB = 2048`
  - lag é mantido só como telemetria (não muda o modo sozinho)
- Snapshot 1/min (JSONL, append-only):
  - arquivo: `C:\conveniente\dados\governor_snapshots.jsonl`
  - leader lock: `C:\conveniente\dados\_governor_snapshot_leader.lock` (evita duplicar em shards)
  - env:
    - `CT_GOV_SNAPSHOT_ENABLED` (default 1)
    - `CT_GOV_SNAPSHOT_INTERVAL_MS` (default 60000)
    - `CT_GOV_SNAPSHOT_LEADER_TTL_MS` (default 120000)

Arquivo: `C:\conveniente\scripts\dashboard.js`

- Allowlist do `fetch_logs`:
  - adicionada key `governor_snapshots` → `dados/governor_snapshots.jsonl`

---

## Evidência / Deploy

- Commits:
  - `3e32a40` — `fix(p0): governor RAM-only + 1/min snapshots`
  - `afc019a` — `feat(logs): allow fetch governor snapshots`
  - `f243902` — `fix(p0): prevent worker crash (illegal break)` (P0 boot crash que apareceu durante rollout)
- RM5:
  - `self_update` ok + **restart confirmado pelo humano** (“feito, reiniciado”)
  - CT `fetch_logs` passou a conseguir ler `governor_snapshots` após restart.

---

## Primeira coleta (RM5) — evidência real

Host: **ROBE MÃE 5** (`hostId=1b0f6f98-46bf-40c6-a0f9-dad6e1965c22`)

- 553 snapshots (~9h13m), intervalo ~60s
- modo: `full` 100% (0 transições)
- `freeMB`:
  - min: 2502
  - média: 3456
  - P5/P50/P95: 2764 / 3110 / 5179

---

## Próximo passo operacional (monitoramento 48h)

- Deixar o host rodar por 48h.
- Na coleta final, puxar `fetch_logs(keys=['governor_snapshots'])` e calcular:
  - min/max/p50/p95
  - contagem e horários de transições full↔light
  - tempo total em full vs light

