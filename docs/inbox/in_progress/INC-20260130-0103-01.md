### INC-20260130-0103-01 — P0: CT “estoque/servidores → liberar todos (RM3)” libera cooldown de ROBE em massa + RM3 não cadastra conta

- **P**: P0 (efeito colateral massivo, quebra governança)
- **Data**: 2026-01-30
- **Sistema**: CT `sitechatbot` + hosts `conveniente`
- **Host alvo (teste)**: ROBE MÃE 3 — hostId `5d7c3309-8581-4a50-a421-e6cbb52d8070`

---

## Status (rígido)

- **Estado atual**: `fix_ready`
- **Rollout**: `deployed_needs_restart`
- **Validation**: `not_run`

---

## Relato do humano (urgente)

- No CT/Estoque/Servidores: selecionou **apenas RM3** e clicou **“liberar todos”**.
- Resultado **inaceitável**:
  - conta **não foi cadastrada** (erro)
  - **todos os Robes** no RM3 estavam em cooldown 24h e “liberaram” (equivalente a “liberar robe todos”)
  - pior: aparente efeito colateral de **liberar cooldown de Robe em outros servidores** (reset em massa).

Regras desejadas (não negociáveis):
1) “liberar todos” em estoque/servidores deve **somente** liberar o **cooldown do servidor no CT** para ele voltar a receber conta.
2) **Nunca** deve resetar cooldowns do Robe (nem local, nem global), a menos que o operador peça explicitamente “liberar robes”.

---

## Evidência já encontrada (código CT)

No CT (`C:\sitechatbot\index.js`) existe o endpoint:
- `POST /api/stock/servers/release_all`

Ele faz DUAS coisas para cada host alvo:
1) `ctFbStock.setServerCooldown({ hostId, cooldownUntilMs: 0 })`
2) **Enfileira comando remoto**: `enqueueCommand(hostId, { type: 'robes_release_all', payload: null })`

Isso explica como uma ação do estoque/servidores pode liberar cooldowns de Robe (efeito colateral proibido).

---

## Causa raiz provável (com evidência)

### 1) Bug P0 no CT: endpoint acoplado

- O endpoint do CT `POST /api/stock/servers/release_all` está **acoplado** a `robes_release_all`.
- Portanto, qualquer “liberar” via estoque/servidores pode resetar cooldowns do Robe.

### 2) Bug P0 no host (conveniente): stock_provision está falhando por “pause_timeout”

No `conveniente/scripts/dashboard.js`, o `execStockProvision` chama `waitForQuiesce()` e no passo (B) ele:
- apenas **espera** `pauseableVirtusCount` cair a 0
- e se não cair dentro de `STOCK_PROVISION_WAIT_PAUSE_MS`, ele **falha** com `pause_timeout`.

Atualmente, em ambiente real (RM3), `pauseableVirtusCount` permanece alto (20), então o fluxo falha *antes* de criar o perfil.


---

## Evidência a coletar (obrigatória)

Do CT:
- `commands.log`: localizar o(s) `cmdId` de `robes_release_all` enfileirados no clique, com `target` e `hostId`.
  - confirmar se foi disparado **apenas** pro RM3 ou se houve bug de seleção/broadcast.
- ACK do(s) `robes_release_all` (ok/erro) e impacto (quantidade de perfis afetados, se tiver details).
- Identificar o `cmdId` do `stock_provision` do RM3 no mesmo teste e o erro exato.

Do host RM3 (via CT `fetch_logs`):
- `status` (ver `provisionLock`, `autoMode`, etc.)
- `provision_audit` (stock_provision begin/fail + qualquer evento de “release robes”)
- `issues_fallback` (se há marcações de cooldown/reset)
- `desired`

---

## Hipóteses (não confirmar sem logs)

- UI CT pode estar chamando `release_all` mesmo quando “somente RM3” está selecionado (bug de seleção → targets errados).
- Endpoint `release_all` está indevidamente acoplado a `robes_release_all` (design bug).
- O RM3 pode não ter carregado o fix anterior do `stock_provision` (sem restart) ou está falhando por outra causa (lock busy, busy_timeout, etc.).

---

## Correções propostas (mínimas) — aguardando OK do humano

### Fix 1 (CT `sitechatbot`) — separar “liberar servidor” de “liberar robe”

Objetivo: tornar **impossível** “liberar servidor” resetar cooldown do Robe.

- Alterar `POST /api/stock/servers/release_all` para **APENAS**:
  - `ctFbStock.setServerCooldown({ hostId, cooldownUntilMs: 0 })`
  - (sem `enqueueCommand(robes_release_all)`).
- Criar um endpoint separado e explícito para liberar Robes (ex.: `POST /api/servers/robes_release_all` ou `POST /api/stock/servers/release_robes`), com:
  - `hostIds` explícitos + confirmação forte no UI.

Status: **APLICADO no CT local** (pendente apenas restart do processo do CT para valer).

Detalhes do patch:
- `C:\sitechatbot\index.js`: `release_all` não enfileira mais `robes_release_all` e agora aceita `hostIds` (modo “selecionados”).
- `C:\sitechatbot\convenientetecnologia\public\estoque.html`: botão “Liberar todos” agora:
  - se houver seleção, envia `hostIds` e mostra “Liberar selecionados”
  - deixa explícito que **não mexe em Robe/Virtus**

### Fix 2 (`conveniente`) — remover falha “pause_timeout” do stock_provision

Objetivo: `stock_provision` **não** pode falhar só porque “Virtus não pausou” (isso hoje está quebrando o cadastro).

Mudança mínima sugerida em `scripts/dashboard.js`:
- Manter a espera de “busy” (sendLock/robeEmExecucao) para não interromper envios/postagens.
- Transformar a etapa “pause” em **best-effort**:
  - registrar no `provision_audit` (para auditoria)
  - **não** dar throw se `pauseableVirtusCount > 0`
  - seguir para as próximas etapas e usar `hardRecoverRam()` + `ensureFreeMBWithin()` se faltar RAM.

Status: **APLICADO no código do conveniente** (vai via git+pull/self_update; requer restart do processo no host para valer).

Evidência de deploy:
- Commit: `93b1c7e` (`fix(p0): do not fail stock_provision on virtus pause`)
- `self_update` (todos os hosts `conveniente`) com ACK ok=true:
  - RM3: `cmdId=4c34605d-4bd3-42a8-a4f8-088acd781e0b` `ackOk=true`


---

## Evidência coletada (CT + RM3) — 2026-01-30

### A) “Liberar” disparou `robes_release_all` (efeito colateral proibido)

- **CT enqueue**: `robes_release_all`
  - `cmdId=fc984608-6440-4831-abac-32c7023e377d`
  - `target=5d7c3309-8581-4a50-a421-e6cbb52d8070` (RM3)
  - `ts=1769745343124`
- **CT ack**: `ackOk=true` (aplicou no RM3)

Isso é compatível com o relato: “todos os Robes estavam em cooldown 24h e foram liberados”.

### B) Cadastro falhou no mesmo teste (stock_provision)

- **CT enqueue**: `stock_provision`
  - `cmdId=9101ea87-67ba-49ff-a3c3-40a60d4892dd`
  - `target=5d7c3309-8581-4a50-a421-e6cbb52d8070` (RM3)
  - `ts=1769745354086`
- **CT ack**: `ackOk=false` com `ackError="fail ok=0 fail=1"`
- **ACK details (RM3)**: falhou antes de criar perfil por:
  - `error="pause_timeout count=20 ..."`
  - `stock_provision_quiesce_pause_done ok=false pauseableVirtusCount=20`

Interpretação técnica:
- O fluxo está falhando na etapa de “quiesce_pause” (esperar Virtus pausar), sem relação com login/cookies.


