# INC-20260219-0940-01 — Cobrança Asaas diária (seg-sex 08:00), baixa automática e bloqueio às 15:00

- **P**: P0
- **Sistema**: `sitechatbot` + integração `Asaas`
- **State**: `done`
- **Rollout**: `pilot_ready`
- **Validation**: `pass`

---

## Regra de negócio (humano)

- gerar cobrança automática em dias úteis (segunda a sexta) às 08:00;
- lead de sexta/sábado/domingo entra na cobrança de segunda-feira;
- lead de segunda cobra na terça; terça na quarta; quarta na quinta; quinta na sexta;
- meios aceitos: boleto e pix (sem cartão);
- bloquear recebimento de novos leads às 15:00 do dia de cobrança, se houver pendência;
- quando houver pagamento, dar baixa e liberar automaticamente.

Observação operacional (humano):
- funcionamento interno: segunda a sexta, 10:00–17:00;
- a janela 08:00–15:00 existe para dar tempo de negociação/correções antes do bloqueio.

---

## Decisões pendentes

- usar atualização do boleto existente vs cancelar e emitir novo;
- política de vencimento e reenvio;
- regras de "liberação provisória 48h por comprovante" no painel;
- regra de fechamento do ciclo para reset de contagem de leads;
- política de tolerância para pagamento após 15:00 (bloqueia no dia, libera automático no próximo ciclo ou imediato via webhook).

---

## Evidência obrigatória antes de codar

- documentação/capacidades do Asaas para:
  - criação de cobrança boleto/pix;
  - webhook de pagamento/baixa;
  - cancelamento/substituição de cobrança;
- matriz de estados de cobrança no sistema interno;
- prova de scheduler confiável com timezone definido;
- logs de idempotência para evitar cobrança duplicada.
- contrato técnico local (sem segredo): `C:\sitechatbot\docs\INTEGRACAO_ASAAS.md`

---

## Estado real do código hoje (para evitar confusão)

No CT já existe um **protótipo** de ledger/invoices e ele foi alinhado com a regra canônica (seg-sex + 15:00):

- Arquivo: `C:\sitechatbot\convenientetecnologia\lib\ctLeadLedgerStore.js`
  - `isBillingDay(...)` retorna **segunda a sexta**.
  - `LEAD_BILLING_BLOCK_HOUR` default é **15**.
  - regra de competência (lead_award do dia não entra no invoice do próprio dia) aplicada no scheduler.
- Teste/prova local (passando):
  - `C:\sitechatbot\tools\validate_billing_window_rule.js` (valida bloqueio 15:01).

Conclusão: este INC segue como fonte da **regra canônica atual**; o protótipo já reflete a regra e agora o foco é E2E/piloto.

---

## Critério de sucesso

- geração automática ocorre em cada dia útil às 08:00 sem duplicação;
- pagamento confirmado libera motorista sem ação manual;
- débito em aberto bloqueia elegibilidade no sorteio a partir de 15:00.

---

## Evidência (fase atual)

- Webhook de baixa idempotente: `PASS`
  - `C:\sitechatbot\tools\validate_asaas_webhook_auto_settlement.js`
- Reconciliação por poll quando webhook atrasa: `PASS`
  - `C:\sitechatbot\dados\forensics\ct_poll_reconcile_1771623887927.sqlite`
- Regra de janela 08:00/15:00 e competência: `PASS`
  - `C:\sitechatbot\tools\validate_billing_window_rule.js`

