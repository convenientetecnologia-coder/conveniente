### INC-20260130-0205-01 — P0: governor light/full — sem “ram_relief_deactivate”; janelas 5min/30min + hard reset total

- **P**: P0
- **Data**: 2026-01-30
- **Sistema**: `conveniente`

---

## Status (rígido)

- **Estado atual**: `done`
- **Rollout**: `needs_restart`
- **Validation**: `not_run`

---

## Regras desejadas (humano)

1) Full ↔ Light por RAM:
   - entrar em light só após **>5min** abaixo de **2GB** livres (se subir, zera contador)
   - sair para full só após **>5min** acima de **2GB**
2) Lag: menos sensível (secundário).
3) Em light: reduzir frequência, mas **não parar** Robe/Virtus.
4) Se não recuperar em 30min: reset total (tudo-ou-nada), sem “escadinha”.

---

## Implementação (worker.js)

Arquivo: `C:\conveniente\scripts\worker.js`

- Removeu o fechamento gradual 1-a-1 (`ram_relief_deactivate`).
- Governor por janelas:
  - `CT_GOV_ENTER_CONFIRM_MS` (default 5min)
  - `CT_GOV_EXIT_CONFIRM_MS` (default 5min)
  - `CT_GOV_LIGHT_MAX_MS` (default 30min)
- Hard reset total (tudo-ou-nada, rate-limited):
  - `CT_GOV_HARD_RESET_ENABLED` (default 1)
  - `CT_GOV_HARD_RESET_COOLDOWN_MS` (default 6h)
  - após 30min em light sem recuperar: salva snapshot de `desired.active`, seta `desired.active=false` para todos, fecha browsers e restaura `desired.active` após estabilidade.

---

## Deploy evidence

- Commit: `d8a3abc` (`fix(p0): governor light/full with 5min/30min windows`)
- `self_update`:
  - RM3: `cmdId=fe2f993a-2afe-478a-ae79-a99e61db4c70` `ackOk=true`
  - RM4: `cmdId=28badd0b-2941-4c34-aab4-3b130b1923ee` `ackOk=true`

---

## Próximo passo (pra valer no processo)

- **Precisa reiniciar?** **Sim**
- **Qual projeto?** `conveniente`
- **Como reiniciar (humano)?** `node index.js`
- **Por quê?** carregar o `worker.js` novo (governor por janelas + hard reset total).

