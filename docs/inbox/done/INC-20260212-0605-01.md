### INC-20260212-0605-01 — RM1: auditoria CT vs servidor (dedupe + conflitos cross-host + “fantasmas” + teste de exclusão manual) (fechado)

- **P**: P0
- **Sistema**: `sitechatbot` (CT/estoque) + `conveniente` (RM1)
- **Status**: `done`
- **Rollout**: `operational_fix` (sem mudança de código; somente ações via CT + exclusões no servidor)
- **Validation**: `passed`

---

## Objetivo

Garantir que o **ROBE MÃE 1** esteja **100% alinhado** entre:

- **Servidor** (perfis reais existentes no host)
- **CT** (“Em uso” / estoque `assigned` / inventário `ct_fb_stock_server_profiles`)

E provar com evidência que:

- não há perfis duplicados (mesma conta) no servidor,
- não há conta atribuída a 2 servidores,
- não há “fantasmas” (CT `assigned` sem perfil no servidor) sem decisão explícita,
- exclusões manuais no servidor refletem corretamente no CT (vai para **Excluídas**, sai de **Em uso**).

---

## Host alvo

- **RM1**: `hostId=084c8fff-c508-47bd-a33e-3ab34aeb1e3d`

---

## Ações executadas (resumo)

### 1) Reprovisão controlada (1 por vez) de conta “fantasma”

- Provisão da conta **407 (Leo Macedo)** no RM1.
- Evidência CT: `sitechatbot/dados/commands.log` (enqueue/ack) + `ack_<cmdId>.json`.
- Resultado: perfil criado e mantido; estado operacional ficou “recurso em análise” (aceitável).

### 2) Contas sem credenciais (não dá para logar)

Critério decidido pelo humano: se a conta **não tem login/senha** e **não loga via cookies**, não faz sentido manter.

- Conta **597 (Jairo Neves)**: perfil criado, mas `login_remediate` falhou por `governor_busy`.
- Conta **661 (Carlos Pietro)**: perfil criado, mas `login_remediate` falhou por `governor_busy`.
- Ambas foram **removidas do servidor** via comando e **arquivadas no CT (Excluídas)**.

### 3) Exclusão manual no servidor + reflexo no CT (teste real)

Critério: o humano exclui manualmente no host; o CT deve:

- retirar de “Em uso” (`assigned`),
- mover para “Excluídas” (`archived`) com `archived_reason=manual_delete:unknown`,
- marcar inventário do servidor (`ct_fb_stock_server_profiles`) como deletado em snapshot.

Testadas e excluídas manualmente no RM1:

- **788 (Vitor Hugo Muniz)** — perfil `manaus-1770940909190`
- **1130 (Júnior Teixeira)** — perfil `belo_horizonte-1770941172744`

---

## Evidências (auditáveis)

### Remoção via CT (delete_perfis) — RM1

- Comando `delete_perfis` (2 perfis):
  - `cmdId=a7c5099f-ba12-44f2-a285-55d384330175`
  - ACK: `C:\sitechatbot\dados\logs\084c8fff-c508-47bd-a33e-3ab34aeb1e3d\ack_a7c5099f-ba12-44f2-a285-55d384330175.json`
  - Perfis removidos:
    - `sa_o_josa_dos_campos-1770940288534` (597)
    - `londrina-1770940390453` (661)

### Export (prova de contagem / “sem wipe”) — RM1

- Pós-delete de 597/661:
  - `cmdId=b3214669-1b63-4627-a8c3-5cba296b1abb`
  - ACK: `C:\sitechatbot\dados\logs\084c8fff-c508-47bd-a33e-3ab34aeb1e3d\ack_b3214669-1b63-4627-a8c3-5cba296b1abb.json`
  - `profilesCount=52`

### Exclusão manual no host (reflexo no CT)

- Export pós-exclusão manual:
  - `cmdId=ae28f131-890e-41f6-a0d1-41ed603f9c60`
  - ACK: `C:\sitechatbot\dados\logs\084c8fff-c508-47bd-a33e-3ab34aeb1e3d\ack_ae28f131-890e-41f6-a0d1-41ed603f9c60.json`
  - `profilesCount=52`
  - Perfis **não** presentes no export:
    - `manaus-1770940909190`
    - `belo_horizonte-1770941172744`

### Evidência no CT (estoque)

- Contas arquivadas (Excluídas) após exclusão manual:
  - `ct_fb_stock_accounts.status=archived`
  - `archived_reason=manual_delete:unknown`
  - `assigned_host_id=NULL` e `assigned_profile_name=NULL`

### Evidência no CT (inventário de perfis do servidor)

- `ct_fb_stock_server_profiles` marcou as linhas como deletadas em snapshot:
  - `deleted_on_server_reason=missing_in_snapshot`
  - para:
    - (788, `manaus-1770940909190`)
    - (1130, `belo_horizonte-1770941172744`)

---

## Resultado final

- RM1 ficou **alinhado** entre servidor e CT.
- O fluxo de **exclusão manual** no servidor refletiu corretamente no CT (**saiu de Em uso e foi para Excluídas**).

---

## Reinícios

- **Nenhum** (somente comandos via CT + exclusão manual no host).

---

## Rollback

N/A (ações operacionais irreversíveis no servidor; rollback seria reprovisionar manualmente a partir do estoque, caso o humano decidisse).

