### INC-20260204-0100-01 - P0 - CT Chat: mensagens misturadas entre salas (canais/privados/equipe)

- **State**: `need_evidence`
- **Rollout**: `not_deployed`
- **Validation**: `not_run`

#### Relato
- Ao abrir Equipe, Canais e Privados, todos mostram as mesmas mensagens (histÃ³rico misturado/repetido entre salas).

#### Impacto
- P0: inviabiliza uso de canais/privados; confusÃ£o operacional + risco de enviar mensagem no lugar errado.

#### HipÃ³teses
- H1: Frontend mantÃ©m cache global de mensagens e nÃ£o limpa ao trocar de sala (merge errado).
- H2: Frontend estÃ¡ usando sempre o mesmo roomId no GET (ex.: roomId preso).
- H3: Backend nÃ£o filtra por room_id (retorna mensagens de todas as salas).

#### EvidÃªncia (a coletar)
- debug.log runId `chat_roommix_v1`: roomId selecionado + request params + count/min/max retornados.

