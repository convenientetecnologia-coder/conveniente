### INC-20260212-0615-01 — RM3: auditoria CT vs servidor (mesmo playbook do RM1) (aberto)

- **P**: P0
- **Sistema**: `sitechatbot` (CT/estoque) + `conveniente` (RM3)
- **Status**: `need_evidence`
- **Rollout**: `not_deployed`
- **Validation**: `not_run`

---

## Host alvo

- **RM3**: `hostId=5d7c3309-8581-4a50-a421-e6cbb52d8070`

---

## Objetivo

Executar auditoria “CT em uso vs perfis do servidor” para:

- duplicados no servidor,
- cross-host,
- fantasmas,
- perfis sem CT.

---

## Evidência obrigatória

- `stock_export_profiles` (RM3) + ACK (`sitechatbot/dados/logs/<hostId>/ack_<cmdId>.json`)
- `C:\sitechatbot\scripts\auditHostAssigned.js` (relatório)

---

## Plano adicional (CANÔNICO) — alinhamento no disco (órfãos / recovery / purge)

Rodar também o playbook de “órfãos no disco” (perfis pastas antigas vs perfis runtime).

Referência canônica (RM1 validado):

- Runbook: `C:\conveniente\docs\RUNBOOK_TECNICO.md` (seção “Alinhamento no disco…”)
- Checkup: `C:\conveniente\docs\checkups\checkup_2026-02-13_rm1_profiles_orphans_alignment.md`

Resumo do procedimento (via CT):

1) `profiles_fs_audit`
2) classificar órfãos no CT
3) `profiles_purge_dirs` (dry-run → apply)
4) `profiles_manifest_probe` (decidir relink)
5) `profiles_relink_orphans` → teste visual humano
6) `delete_perfis` (excluir reprovados)

