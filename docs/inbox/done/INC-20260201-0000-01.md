# INC-20260201-0000-01 — Groq config: garantir auto-download + auto-correção de modelo em TODOS os hosts após update/restart

**P**: P1  
**Sistema**: conveniente + sitechatbot (CT)  
**State**: `done`  
**Rollout**: `deployed`  
**Validation**: `passed`  

---

## Pedido (humano)

- Garantir que ao atualizar/reiniciar qualquer host, o `groq_config.json` seja automaticamente obtido e que o **modelo** esteja correto em todos os servidores.

> Regra de segurança: **não registrar secrets** em docs/logs.

---

## O que foi feito (código)

- Host (`conveniente`) passou a sinalizar `needsGroqConfig=true` também quando **modelo efetivo diverge do modelo esperado**.
- Host inclui telemetria segura em `/report` (`payload.groq`) sem segredo.

Arquivos:
- `C:\conveniente\scripts\groqConfig.js` (`readGroqConfigMeta`)
- `C:\conveniente\scripts\dashboard.js` (`needsGroqConfig` + `payload.groq`)

---

## Evidência de validação (CT)

Confirmado via CT (arquivo `C:\sitechatbot\dados\commands.json`):
- RM1–RM7 executaram `set_groq_config` com `ok:true`
- `groqModel` enviado = `meta-llama/llama-4-maverick-17b-128e-instruct`
- `apiKeyPresent=true` (boolean; sem exibir segredo)

Exemplo (resumo):
- RM1 cmdId `4693107f-0a1d-4fd0-9042-3fa5fb219f93` ok
- RM2 cmdId `7dcfa0ed-4227-400a-b0c7-b7e1d5378685` ok
- RM3 cmdId `969408c8-deae-44c0-a1e9-0ec10dd64dd2` ok
- RM4 cmdId `fb4ec43e-9335-47af-99be-e89790171e4a` ok
- RM5 cmdId `4ed8d618-9457-46a6-a3f2-50c67df5a5d6` ok
- RM6 cmdId `ef718c1b-929e-4e84-8834-3f31cb4fa9e4` ok
- RM7 cmdId `0ff04c3a-a3c7-43d5-b429-ff2f5d58a991` ok

---

## Impacto operacional

- **Precisa reiniciar**: já realizado pelo humano em RM1–RM7 (conforme relato).
- **Rollback**: `git revert` dos commits relacionados (conveniente) + restart `node index.js` nos hosts.

