### INC-20260214-0940-01 — Delete E2E “perfeito”: idempotente + retry-safe (Windows locks) + auto-retry

- **P**: P0
- **Sistema**: `sitechatbot` (CT) + `conveniente` (hosts)
- **Status**: `cancelled`
- **Rollout**: `not_deployed`
- **Validation**: `not_run`

---

## Encerramento (fase 2)

Escopo reclassificado para fase 2.
Não bloqueia o GO do programa único de cadastro sem duplicação (`INC-20260214-0900-01`).

## Contexto

Historicamente, delete falhou deixando “sobras” (pastas, desired, userDataDir) por lock/Windows/EPERM.
Isso cria:

- lixo no disco
- risco de ressuscitar coisas antigas (se alguém tentar rebuild errado)
- perda de confiança no “excluir de verdade”

---

## Escopo

### In-scope

- Delete do ponto de vista CT→host (jobs + comando `delete_perfis`)
- Delete no host (API + command handler):
  - remover de `perfis.json`
  - remover de `desired.json`
  - remover `dados/perfis/<nome>/`
  - best-effort `userDataDir` com retry/backoff e pendência persistida
- Scheduler/boot: auto-retry de deleções pendentes

### Out-of-scope

- Registry secundário spec: `INC-20260214-0950-01`
- Canal de comandos/ACK: `INC-20260214-0910-01`

---

## Evidência requerida

- logs mostrando falhas reais por lock/EPERM e como ficam pendências
- prova de idempotência:
  - repetir delete N vezes => estado final igual
  - delete de perfil já ausente => ainda limpa sobras

---

## Critério de “done”

- Delete é “100% idempotente”: repetição não quebra nada e converge
- Se Windows bloquear, o sistema registra pendência e re-tenta até limpar
- CT reflete estado correto (job tracking + server_profiles deleted_on_server_at quando apropriado)

