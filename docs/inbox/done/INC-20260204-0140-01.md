### INC-20260204-0140-01 - P1 - CT Chat: mensagens demoram a chegar (latencia fim-a-fim)

- **State**: `done`
- **Rollout**: `not_applicable`
- **Validation**: `passed`

#### Encerramento
- Encerrado por decisão operacional do humano nesta rodada.
- Na observação atual, o comportamento está estável (“a princípio tá tudo certo”).
- Se o sintoma voltar, abrir novo INC com recorte de horário/sala para evidência objetiva.

#### Relato (humano)
- Alguns usuarios reclamam que as mensagens demoram a chegar em canais e privados.

#### Evidencia runtime (rodada atual)
- O teste do humano reportou "chegou em tempo real".
- Logs mostram latencias baixas no envio:
  - Client POST ack tipicamente ~100-200ms.
  - Persistencia no store ~0-2ms.
  - SSE `chat_changed` chegando ~200-300ms apos o clique (mesma maquina do browser).
- Evidencia: `c:\sitechatbot\.cursor\debug.log` `runId="chat_latency_v1"`.

#### Hipoteses ainda abertas
- H2 (throttle sobrescrever payload entre salas) **nao foi provada** nesta rodada (nao apareceu `sameRoom:false`).
- Pode haver cenarios reais diferentes:
  - alta frequencia (rajadas) em salas diferentes dentro da janela de 150ms,
  - SSE instavel/tab suspensa (fallback polling),
  - render pesado (muitas mensagens/anexos) causando "chegar em lote".

#### O que falta (para fechar com 110% verdade)
- Um caso real com:
  - usuario afetado + horario aproximado,
  - sala(s) envolvidas,
  - se estava em 2+ abas/janelas,
  - se o browser ficou em background/minimizado.
- Nova rodada de repro deve tentar: mandar mensagens **bem rapidas** (<=150ms entre elas) em **2 salas diferentes**.

