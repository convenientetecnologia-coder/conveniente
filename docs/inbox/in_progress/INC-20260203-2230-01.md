### INC-20260203-2230-01 — P1 — CT Sistema Interno: botão WhatsApp + menu (Testes encerrados)

- **P**: P1 (melhora operacional grande; evita retrabalho multi-navegador)
- **Sistema**: `sitechatbot` (CT) — módulo `convenientetecnologia`
- **State**: `in_progress`
- **Rollout**: `needs_restart`
- **Validation**: `not_run`
- **Precisa reiniciar agora?** sim (CT)
- **Como reiniciar (humano)?** `node index.js`
- **Por quê?** para carregar o novo botão/menu de WhatsApp na tela “Testes — Encerrados”.

---

## Relato (humano)

Na aba **Sistema Interno → Testes — Encerrados**, precisamos de um botão/ícone WhatsApp por linha, com menu:

1) Navegar para o WhatsApp do cliente  
2) Copiar link (para colar na barra de endereços)  
3) Copiar o número

Motivo: a equipe usa 2–4 navegadores com WhatsApp, mas o CT roda em um único navegador.

Pergunta do humano: dá para escolher “qual navegador 1/2/3/4” para abrir o WhatsApp?

---

## Hipóteses / constraints (sem achismo)

- **H1 (segurança browser)**: um web app **não consegue comandar outro navegador/instância** (navegador 2/3/4) diretamente por restrições de segurança.  
- **H2 (viável)**: dá para resolver o fluxo operacional com:
  - “Abrir WhatsApp” (no navegador atual / nova aba)
  - “Copiar link” (para colar no navegador desejado)
  - “Copiar número”
- **H3 (helper separado)**: para “escolher navegador”, seria necessário um **helper local / extensão** (novo item separado).

Decisão: implementar **menu 1–3 agora**, e a seleção de navegador vira um **item novo** se você quiser.

---

## Implementação (mudança mínima)

- Arquivo: `C:\sitechatbot\convenientetecnologia\public\ct.js`
- Ponto: `renderTests(... ended=true)` (tabela “Testes encerrados”)
- Adicionar botão “WhatsApp” na coluna Ações e abrir um modal com 3 opções:
  - Abrir WhatsApp (link `wa.me`)
  - Copiar link
  - Copiar número

Notas:
- O menu inclui dica: “para abrir no navegador 2/3/4, use Copiar link”.

---

## Validação (runtime)

Após reiniciar CT:

1) Abrir `Sistema Interno` → `Testes — Encerrados`  
2) Clicar em **WhatsApp** em uma linha  
3) Validar:
   - Abrir WhatsApp abre a conversa (nova aba)
   - Copiar link copia `https://wa.me/55...`
   - Copiar número copia somente dígitos

---

## Rollback

- Reverter alterações em `ct.js` e reiniciar `node index.js` no CT.

