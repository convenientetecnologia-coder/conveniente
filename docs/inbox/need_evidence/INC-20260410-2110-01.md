# INC-20260410-2110-01 — Dashboard PIL: idade da conta em dias

Status: `in_progress`  
Prioridade: `P0`  
Projeto: `conveniente`

## Contexto

Necessidade operacional no dashboard (lista de contas/servidores):

- exibir no PIL da conta a idade em dias desde cadastro no servidor;
- formato único e compacto (somente dias, ex.: `0`, `1`, `669`);
- sem codar nesta etapa (somente auditoria ponta a ponta).

## Objetivo

Definir desenho técnico canônico para mostrar "idade da conta" no PIL sem ambiguidade de fonte de dados e com baixo risco de regressão.

## Escopo de auditoria

1. Onde o PIL é renderizado (`public/index.html`).
2. Quais campos de status/perfil chegam ao frontend (`/api/status`, `/api/perfis`).
3. Qual fonte de data de cadastro existe hoje (persistida vs inferida).
4. Plano mínimo de implementação e validação (pré-código).

## Evidências auditadas

- `C:\conveniente\public\index.html`
- `C:\conveniente\scripts\worker.js`
- `C:\conveniente\scripts\api_status.js`
- `C:\conveniente\scripts\clusterMaster.js`
- `C:\conveniente\docs\contas_ativas_por_idade_2026-04-10.md`

## Entregável

- `docs/checkups/checkup_2026-04-10_pil_idade_conta_dias_pre_codigo.md`

## Critério de sucesso

- fechar fonte canônica da idade;
- mapear exatamente os pontos de patch para incluir o PIL de idade (dias);
- definir canário e rollback antes da codificação.

## Atualização runtime — 2026-04-10

- patch mínimo aplicado no frontend (`public/index.html`);
- novo pill por conta: `Idade: N dia(s)`;
- cálculo atual: primeiro tenta `profileCreatedAt/createdAt`, e em seguida fallback para slug (`-(\d{13})$`);
- fallback visual seguro para legado sem timestamp: `Idade: --`.

## Reinício

- Precisa reiniciar agora? `sim`, para garantir carregamento do novo frontend em todos os hosts.
