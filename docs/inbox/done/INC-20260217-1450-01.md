# INC-20260217-1450-01 — RM6: `about:blank` + `loginRequired` inconsistente com Messenger saudável

- **P**: P0
- **Sistema**: `conveniente` (worker/browser/nurse/login flags)
- **Host alvo**: ROBE MÃE 6 (RM6)
- **Conta referência principal**: `campina_grande-1769232949697` (`[005] Igor Barbosa`)
- **State**: `done`
- **Rollout**: `deployed_rm6_controlled`
- **Validation**: `passed_operational`

---

## Encerramento aprovado

Encerrado por decisão operacional do humano nesta rodada.

Motivo do encerramento:
- correção aplicada para reduzir falso positivo por `probe_failed`;
- atualização enviada ao RM6 com ACK OK;
- estabilização considerada suficiente para seguir e monitorar.

---

## Evidência objetiva desta rodada

- snapshot no RM6 confirmando caso base do Igor com `loginReason="probe_failed"` e `virtusOnline=false`;
- mapeamento dos dois perfis "Davi" para remover ambiguidade de investigação manual:
  - `manaus-1770224649136` (`Davi Vasconcelos`)
  - `piracicaba-1770786321129` (`Davi Torres`)
- correção implantada no código para tratar `probe_failed` como sinal transitório, não como bloqueio persistente.

---

## Regra de reabertura

Se o sintoma reaparecer (Messenger saudável + `loginRequired` preso / `Virtus Offline` indevido), reabrir com:

- id da conta;
- horário;
- snapshot e recorte de `login_required_events` / `status` / `provision_audit`.

Este INC permanece como histórico canônico desta rodada.

