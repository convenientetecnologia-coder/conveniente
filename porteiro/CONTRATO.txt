CONTRATO — Porteiro e o vigia do WINDOWS, nao filho do index
============================================================
Data: 2026-08-31

O Porteiro (C:\auto_vigia, tarefa ConvenientePorteiro ao logon) e um
sistema a parte. Ele TEM que funcionar com o Conveniente DESLIGADO:
  - reboot 04:00-04:20 + TEMP + Lixeira
  - AUTO_BOOT: se o index nao estiver no ar, o Porteiro sobe o PowerShell Conveniente_Node
  - NetGuard / ConvenienteNetBoot no startup (sem net = ate 3 reboot extra/dia)

O index.js NAO e o dono. O index so CORRIGE a versao quando voce liga
o Conveniente:
  - se o arquivo/loop for o velho (mem_soft / v5.1.13 / Get-CpuAvg) → instala v5.2.1-clean-cpu
    e a TAREFA DO WINDOWS sobe o loop (schtasks /Run). Nao fica filho do Node.
  - se ja for o correto e estiver vivo → o index NAO mexe.

RAM/StandbyList: o Conveniente cronometra (chromeMemorySweep no index) e
pede ao Windows a tarefa SYSTEM ConvenienteDiskClean. O loop do Porteiro
NAO dispara o exe (MemClean=OFF).
CPU: o loop NAO le carga de hardware. Disco (TEMP+Lixeira) no relogio,
sem gate de CPU.

1. SERVIDOR NOVO
   irm ... | iex
   fecha, reabre:
     cd \
     if (Test-Path "C:\conveniente") { Remove-Item -Path "C:\conveniente" -Recurse -Force }
     git clone https://github.com/convenientetecnologia-coder/conveniente.git "C:\conveniente"
     cd C:\conveniente
     npm install
   Clique o S (INICIAR_SISTEMA.vbs). Sem admin. Sem OK.
   Sobe o Conveniente e arma o vigia (C:\auto_vigia + tarefa ao logon + loop).
   Lixeira, reboot 04h, religar o PowerShell Conveniente_Node: o vigia faz.

2. MAE JA EXISTENTE / VERSAO NOVA DO VIGIA
   git pull, clique o S. Se o arquivo do vigia mudou: copia, mata o loop
   velho, sobe o novo. O Conveniente nao cai. Sem admin.
3. PROVA
   STATUS: Ver=v5.2.1-clean-cpu  MemClean=OFF
   Log: BOOT v5.2.1-clean-cpu
   Tarefa ConvenientePorteiro = Running (nao depende do Node estar aberto)

4. DECISAO 2026-09-05 (MAE 1) — AINDA NAO FAZER
   Por enquanto nao mexer no Porteiro nem no botao Iniciar.
   Se um dia for: conversa Porteiro <-> Conveniente (perguntar pro sistema
   ligado, nao so pro Windows) e log de todo taskkill (pid, quem pediu).
   NAO fazer: Iniciar usar o mesmo olho do loop (qualquer node = ligado =
   nao mata). O clique do usuario TEM que ter mais poder. Caso 21:11
   (clique em cima do AUTO_BOOT pos-reboot) foi caso a parte, nao motivo
   pra enfraquecer o botao.

5. MARTELO DA QUEDA (2026-09-06)
   Quando worker ou index cai, gravar Windows: modulo, excecao, commit%,
   Evento 1000/1001, WER, mini-dump do node.exe se LocalDumps estiver armado
   (NetBoot SYSTEM ou tuning admin). WerSvc fica Manual e LIGADO
   (nunca Disabled, nunca Stop) pra o dump nascer. Porteiro dispara se o Conveniente
   sumir (olho de fora). Nao e cura. E o corpo da queda.
   Arquivos: dados/crash_hammer.jsonl + crash_hammer_last.json
   + dados/crash_dumps\*.dmp
   Iniciar continua com mais poder. Sem misturar nurse/tinta/EWS.
