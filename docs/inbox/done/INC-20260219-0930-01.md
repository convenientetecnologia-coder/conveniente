# INC-20260219-0930-01 — Banco do motorista (ledger de leads, débitos e ajustes manuais)

- **P**: P0
- **Sistema**: `sitechatbot`
- **State**: `done`
- **Rollout**: `pilot_ready`
- **Validation**: `pass`

---

## Necessidade (humano)

Dentro do cadastro do motorista, criar menu "banco" para:

- registrar cada lead ganho;
- registrar débito unitário por lead;
- permitir abatimento, desconto, edição de valor e zeramento;
- manter trilha de auditoria para cobrança.

Contexto operacional (canônico):
- o ledger é a Fonte Única da Verdade do saldo que vai para cobrança (seg-sex 08:00) e para bloqueio (15:00 se pendente).

---

## Decisões pendentes

- entidade canônica (`ledger_entry`) e status de cada item;
- granularidade dos ajustes (por lead vs ajuste agregado);
- trilha obrigatória (`who/when/why`) em cada ajuste manual;
- política de estorno após renegociação.

---

## Evidência obrigatória antes de codar

- caminhos atuais de cadastro de motorista no CT (`path`/funções);
- modelo de dados atual de financeiro (se existir);
- mapeamento de relacionamento `driver <-> leads`;
- payload esperado para renderizar histórico no menu "banco".

---

## Critério de sucesso

- usuário interno consegue auditar saldo e itens sem cálculos externos;
- qualquer alteração manual fica historicamente rastreável;
- saldo devedor final é determinístico e reproduzível.

---

## Evidência (fase atual)

- UI/ações do “Banco/Carteira (pay-per-lead)” no CT validadas em testes reais controlados:
  - editar leads e reemissão com ajuste;
  - cancelar boleto (volta para leads em aberto e espera próximo ciclo);
  - excluir boleto/lead (vai para excluídos);
  - liberação provisória 48h (override temporário);
  - histórico de boletos com valor pago exibido corretamente.
- Stress financeiro atômico (forense isolado): `PASS`
  - `C:\sitechatbot\dados\forensics\ct_stress_billing_atomic_1771624251435.sqlite`

