### INC-20260130-0905-01 — P0: CT marca RM3 OFFLINE mas host está acessível (dash servidores + estoque)

- **P**: P0
- **Data**: 2026-01-30
- **Sistema**: CT `sitechatbot` + hosts `conveniente`
- **Host alvo**: ROBE MÃE 3 — `hostId=5d7c3309-8581-4a50-a421-e6cbb52d8070` — `hostname=DESKTOP-IR7JRIM`

---

## Status (rígido)

- **Estado atual**: `need_evidence`
- **Rollout**: `not_deployed`
- **Validation**: `not_run`

---

## Sintoma (humano)

- “ROBE MÃE 3 está online (acessei o servidor agora), mas no CT ele aparece OFFLINE no dashboard **Servidores** e também no **Estoque → Servidores**.”

---

## Entendimento do sistema (canônico — sem achismo)

### 1) O que é `hostId`, como nasce, quando muda

- **Onde nasce**: no host `conveniente`, gerado pelo `dashboard.js` e persistido em arquivo.
  - **Arquivo no host**: `C:\conveniente\dados\.telemetry_hostid`
  - **Código**: `C:\conveniente\scripts\dashboard.js` (`getOrCreateHostId()`).
- **Importante**: **reiniciar `node index.js` não muda o `hostId`**, porque ele lê do arquivo persistido.
- **Quando muda**: só muda se o arquivo `C:\conveniente\dados\.telemetry_hostid` **sumir** (apagado/limpeza/reinstalação) e o `conveniente` criar outro.
  - O humano informou: **não apaga** esse arquivo (apenas reinicia `node index.js`).

### 2) Com que frequência os servidores mandam dados para o CT

- O `conveniente` manda telemetria por `POST /report` (no CT) em loop no `dashboard.js`.
- **Intervalo padrão**: `DASHBOARD_INTERVAL_MS` (default **30000ms / 30s**).
  - **Código**: `C:\conveniente\scripts\dashboard.js` (`INTERVAL_MS`, `startDashboardMonitor()`).

### 3) “Host online” vs “CT vê online”

- O CT marca **ONLINE/OFFLINE** baseado em “quanto tempo faz desde o último `/report` recebido”, e não em “consigo acessar a máquina”.
- Portanto, o sintoma pode significar: **o host está acessível, mas o CT não está recebendo `/report` recente desse host**.

---

## Evidência já coletada (do CT local)

### Snapshot persistido do RM3 no CT

- **Arquivo**: `C:\sitechatbot\dados\5d7c3309-8581-4a50-a421-e6cbb52d8070-30b3fe928b.json`
- **Campos relevantes** (extraído do arquivo):
  - `hostId="5d7c3309-8581-4a50-a421-e6cbb52d8070"`
  - `hostname="DESKTOP-IR7JRIM"`
  - `sentAt=1769787151966`
  - `receivedAt=1769787152113`

### Como o CT calcula ONLINE/OFFLINE para `/servers`

- **Código**: `C:\sitechatbot\index.js` rota `GET /servers`
- **Regra**: `baseTs = receivedAt (preferido) senão sentAt`; `online = (now - baseTs) < graceSecs*1000`
- **Default atual**: `graceSecs >= 130` (env `ONLINE_GRACE_SECONDS` pode alterar).

---

## Hipóteses (sem assumir)

- **H1 (mais provável)**: o RM3 está rodando, mas **não está conseguindo enviar `/report`** para o CT (ctBaseUrl errado, rede/DNS/timeout, CT indisponível no endpoint, etc.). Resultado: `receivedAt` no CT fica velho → OFFLINE.
- **H2**: o `/report` chega, mas o CT falha ao persistir/atualizar o snapshot (ex.: erro de IO/rename) e o snapshot fica “parado”.
- **H3**: existe **duplicidade/ghost** no CT (ex.: entries antigas por hostname/hostId), e a UI do CT está mostrando a entrada errada (o “fantasma”).

---

## Coletas pendentes (evidência objetiva)

> Regra: o humano não coleta logs; o GPT coleta via CT. Por enquanto estamos apenas registrando o plano e as coletas.

### No CT (sitechatbot)

- **Verificar recência do snapshot** do RM3:
  - comparar `Date.now()` com `receivedAt` do arquivo de snapshot do RM3.
- **Verificar histórico de reports** do RM3 no CT:
  - `C:\sitechatbot\dados\snapshots.log` filtrado por `hostId=5d7c...` (últimos eventos).
- **Verificar se existe ghost**:
  - procurar múltiplos snapshots com o mesmo `hostname=DESKTOP-IR7JRIM` porém `hostId` diferente (ou vice-versa).

### No host RM3 (conveniente) via CT (futuro)

- Coletar `logs_manifest` e `fetch_logs` (keys allowlisted) para confirmar se o `dashboard.js` está conseguindo enviar telemetria:
  - keys candidatas: `logger`, `updates`, `git_head`, `status`, `service_stdout`, `service_stderr`.

---

## Respostas às dúvidas do humano (com evidência de código)

### A) O envio `/report` pode atrasar/falhar por concorrência/carga no host?

**Sim, pode.** O envio do `/report` é executado pelo próprio processo Node do `conveniente` (monitor `dashboard.js`), então depende:

- do **event loop** (se houver bloqueio/lag, timers atrasam),
- do **tempo de coleta do status** (inclui chamada HTTP local para `/api/status`),
- do **tempo de rede/endpoint** (POST para o CT),
- e de uma trava de **anti-overlap** (`inFlight`) que faz o tick “pular” se o anterior ainda não terminou.

Evidências:

- `C:\conveniente\scripts\dashboard.js`
  - `startDashboardMonitor()` roda `tick()` e faz `setInterval(..., INTERVAL_MS)` (`INTERVAL_MS` vem de `DASHBOARD_INTERVAL_MS`, default 30000ms).
  - `tick()` tem `if (inFlight) return;` e `inFlight=true` até o `finally` ⇒ se uma execução ficar lenta, as próximas execuções são ignoradas.
  - `readAggregatedStatus()` tenta `GET http://127.0.0.1:<PORT>/api/status` com timeout **15s** antes de cair em fallback.
  - `postPayload()` para o CT usa `DASHBOARD_TIMEOUT_MS` com default **8000ms** (AbortController).

**Implicação prática**: em um host muito carregado, é possível haver “buracos” de telemetria > 30s (e, no limite, > 130s), o que faz o CT marcar OFFLINE pelo critério atual.

### B) Dá pra “garantir 110%” que o host sempre envia e o CT sempre recebe?

No estado atual, **não existe garantia absoluta** por construção, porque:

- o envio roda no mesmo processo/event loop do `conveniente`,
- o CT persiste snapshots em disco de forma síncrona e pode responder 500 em falha de IO,
- e há dependências externas (rede/DNS, endpoint do CT).

O que dá para fazer (estratégias enterprise, para execução futura) é reduzir drasticamente a chance de falso OFFLINE:

- **Heartbeat mínimo separado do snapshot pesado**:
  - enviar um “ping leve” mais frequente (ex.: 5–10s) com `hostId + uptime + ts`;
  - manter o snapshot completo a cada 30s (ou sob demanda);
  - CT marca ONLINE pelo heartbeat e mostra “snapshot stale” separadamente.
- **Isolar telemetria do caminho crítico**:
  - mover envio para subprocesso/worker dedicado (ou pelo menos separar “snapshot” e “sender”), para não depender do Robe/Virtus.
- **SLO explícito e auditável na UI**:
  - quando OFFLINE, mostrar “CT não recebeu `/report` há Xs” (evita confundir “máquina acessível” com “telemetria chegando”).

> Nota: propostas; **não implementar agora** (somente mapear e registrar).

### C) “Pode ser 10 segundos em vez de 30?”

**Tecnicamente sim**, mas precisa de decisão consciente (trade-off) e evidência:

- **Pró**: reduz tempo até o CT perceber que o host está vivo.
- **Contra**:
  - aumenta tráfego e escrita em disco no CT (cada report gera persistência de snapshot),
  - aumenta probabilidade de overlap se a coleta do status ou a rede estiverem lentas,
  - aumenta risco de “flapping” visual se o grace/critério não estiver alinhado.

Antes de mexer em intervalo, definir:

- SLO esperado (ex.: “report a cada 10s; tolerar até 60s sem marcar OFFLINE”),
- qual dado define ONLINE (ex.: heartbeat leve) vs “dados detalhados” (snapshot),
- e o que a UI faz quando o host está ONLINE mas o snapshot detalhado está stale.

### D) O CT (`sitechatbot`) está “perfeito” para receber?

Ele está funcional e tem validação/persistência, mas não é “perfeito/garantido” ainda.

Evidências:

- `C:\sitechatbot\index.js` `POST /report` (`handleReport`)
  - calcula `hostId` (prioriza `payload.hostId`) e tenta persistir snapshot em disco (`salvarSnapshot`).
  - se falhar ao salvar, retorna HTTP 500 ⇒ host não recebe `ok:true`.
- A persistência usa IO síncrono (atômico, porém sujeito a `EPERM/EBUSY`/antivírus/disco em Windows).

### E) Como o host escolhe qual CT chamar (e onde pode dar ruim)

O endpoint de report é resolvido por `C:\conveniente\scripts\notifierEndpoints.js`:

- prioridade 1: `CT_BASE_URL`/`CT_URL` (env) ⇒ `{base}/report`
- prioridade 2: `C:\conveniente\dados\ct_config.json` (`ctBaseUrl`) ⇒ `{ctBaseUrl}/report`
- fallback: `CT_NOTIFIER_REPORT_URL` (env) ou URL ngrok hardcoded.

Se o host estiver com CT URL errada (ou ngrok expirado), ele pode “rodar normal”, mas o CT para de receber `/report` e marca OFFLINE.

### F) Proposta “ultra enterprise” alinhada com a ideia do humano (sem CT pingar host direto)

Você sugeriu:

- “host só envia quando muda” (para o dashboard ficar estável e ‘verdadeiro’)
- “CT pergunta pro host se tá online” (para confirmar)

**Essência correta.** Só tem uma restrição técnica importante no nosso sistema atual:

- o `conveniente` é **localhost-only** (bind `127.0.0.1`), então o CT normalmente **não consegue fazer HTTP direto** para o host para “perguntar se tá online”.

Então, para manter a essência e ficar enterprise, a forma robusta é:

#### Modelo recomendado: 2 canais (heartbeat leve + snapshot/delta)

1) **Heartbeat leve (sempre)**
- Host envia um payload mínimo com alta frequência (ex.: 5–10s):
  - `hostId`, `sentAt`, `uptime`, `pid`, `version/git_head` (opcional), e um `seq` monotônico.
- Isso define **ONLINE** de forma barata e confiável.

2) **Snapshot/delta (só quando muda)**
- Host mantém um “estado base” e calcula `hash` (ex.: SHA1 do JSON normalizado ou de um subconjunto).
- Só envia o snapshot completo (ou delta) quando o `hash` mudar.
- Mesmo assim, manda um “full refresh” bem menos frequente (ex.: a cada 5–15min) para autocorreção.

**Como isso elimina falso positivo**:
- O CT não confunde “máquina acessível” com “telemetria chegando”:
  - **ONLINE** = heartbeat recente
  - **DADOS ATUALIZADOS** = snapshot recente (ou delta aplicado)
- A UI pode mostrar claramente:
  - “Host ONLINE (heartbeat há 7s)”
  - “Snapshot stale (última mudança há 12min)” (não é OFFLINE)

#### “CT perguntar se tá online” (sem rede host→CT): usar o canal de comandos

Se você quiser que o CT faça uma checagem ativa, o jeito enterprise no nosso stack é:

- CT enfileira um comando (`ping`/`health_bundle`) para o `hostId`
- o host **recebe esse comando dentro da resposta do `/report`** e responde com **ACK** (evidence)

Ou seja, não é um “ping TCP”, é um “ping lógico” via o próprio mecanismo CT⇄host já existente (fila/ACK). Isso é auditável (tem `cmdId` e `ack_<cmdId>.json`).

#### Garantia de entrega (camada de confiabilidade)

Para ser “ultra robusto” (sem perder report):

- Host guarda em disco um pequeno spool do último heartbeat/snapshot pendente (ex.: `telemetry_outbox.jsonl`) e só descarta após receber `ok:true` do CT.
- CT, ao receber, salva em 2 camadas:
  - snapshot atual (`dados/<hostId>-*.json` ou idealmente `dados/<hostId>.json`)
  - journal append-only (`dados/reports.jsonl` ou SQLite) para auditoria/forense

> Isso transforma “falhou 1 request” em “atrasou, mas não perdeu”.

#### O que fica explícito como decisão (antes de executar)

- Frequência do heartbeat (5s/10s/30s)
- Frequência de refresh completo (ex.: 10min)
- Critério de OFFLINE (ex.: “sem heartbeat por 180s”)
- Critério de “stale snapshot” (ex.: “sem snapshot/delta por 15min”)
- UX do dashboard: ONLINE vs STALE vs OFFLINE (3 estados, não 2)

---

## Garantias “não ficar à mercê do Robe/Virtus” (isolamento do setor de telemetria)

### Problema atual (realista)

Hoje, o envio de telemetria (`/report`) roda dentro do mesmo processo do `conveniente` (`C:\conveniente\scripts\dashboard.js`), com `inFlight` e dependência do event loop. Isso significa:

- se o event loop ficar lento/bloqueado por carga, **o heartbeat/snapshot atrasam**
- como existe anti-overlap (`if (inFlight) return`), um tick lento pode causar **pulos** de telemetria

### Requisito enterprise

Telemetria precisa ser um “setor próprio”: **não pode depender do caminho crítico** de Robe/Virtus.

### Estratégias compatíveis (para execução futura; aqui só registro)

1) **Sender isolado em subprocesso**
- o processo principal só escreve “o que deve ser enviado” num spool local (arquivo pequeno)
- um subprocesso dedicado faz:
  - retry com backoff
  - confirmação de entrega (recebeu `ok:true` do CT)
  - sem concorrer com Robe/Virtus

2) **Heartbeat mínimo + snapshot/delta separado**
- heartbeat é barato e pode ser mais frequente mesmo em carga
- snapshot pesado é best-effort e pode atrasar sem virar OFFLINE

3) **Outbox persistente (não perder dados)**
- host grava um outbox (ex.: `telemetry_outbox.jsonl`) e só remove quando confirmado
- isso evita “perdi telemetria porque 1 POST falhou”

---

## Arquitetura CT/Dashboards (como os dados chegam e como a UI atualiza)

### Persistência do snapshot no CT

- CT recebe `POST /report` em `C:\sitechatbot\index.js` (`handleReport`)
- persiste snapshot em disco via `salvarSnapshot()`/`writeAtomicJson()` (write+rename)
- em falha ao salvar, retorna HTTP 500 ⇒ host não recebe `ok:true`

### Atualização “quase realtime” da UI (SSE)

- UI (`C:\sitechatbot\public\index.html`) abre SSE:
  - `new EventSource('/convenientetecnologia/api/stream')`
  - reage a `servers_changed` (fetch rápido) e `tick` (fallback)
- SSE é implementado em `C:\sitechatbot\convenientetecnologia\lib\ctRealtime.js`:
  - `tick` a cada 3s (fallback/relógio)
  - `: ping` a cada 20s (mantém stream vivo)
- O CT dispara `servers_changed` via `ctRealtime.broadcastThrottled('servers_changed', ..., 500)` (inclui após `/report`)

**Conclusão**: dashboards já têm mecanismo realtime; o gargalo/falso OFFLINE geralmente é “telemetria não chegou” ou “snapshot não persistiu/atualizou”, não a UI em si.

---

## “Melhorar sem quebrar” (plano de impacto/compatibilidade)

### Regras de ouro (para quando formos executar)

1) **Compatibilidade de schema**
- qualquer campo novo deve ser opcional (CT não pode quebrar se não vier)
- CT deve aceitar modo legado e modo novo em paralelo (durante rollout)

2) **Feature flags (kill-switch)**
- tudo novo entra desligado por default e habilita por env/flag
- deve existir “voltar ao legacy” de forma rápida

3) **Dual-write / shadow mode**
- por um tempo, host pode mandar heartbeat + continuar mandando snapshot legado (ou vice-versa)
- CT calcula online nos dois métodos, mas **só exibe um** (o outro fica em diagnóstico)

4) **Canary**
- primeiro liga em 1 host (RM3), observa estabilidade, depois expande

5) **Rollback**
- `git revert` + (se for runtime) reinício `node index.js`

### Arquivos com maior chance de impacto (mapa inicial)

- Host (`conveniente`)
  - `C:\conveniente\scripts\dashboard.js` (telemetria; candidato a outbox/heartbeat/delta)
  - `C:\conveniente\scripts\notifierEndpoints.js` + `C:\conveniente\scripts\ctConfig.js` (resolução do CT)
- CT (`sitechatbot`)
  - `C:\sitechatbot\index.js` (`handleReport`, persistência, `/servers`, endpoints de estoque)
  - `C:\sitechatbot\convenientetecnologia\lib\ctRealtime.js` (SSE; já existe)
- UI
  - `C:\sitechatbot\public\index.html` (dashboard servidores)
  - `C:\sitechatbot\convenientetecnologia\public\estoque.html` (estoque)

---

## Investigação forense — mapa “ponta a ponta” do que vamos mexer (sem executar ainda)

Objetivo desta seção: listar **exatamente** os pontos de mudança (arquivos/funções), o impacto e os riscos, antes de qualquer alteração em runtime.

### 1) Fluxo atual (fonte da verdade)

**Host (`conveniente`)**:

- Coleta status local + monta payload em `C:\conveniente\scripts\dashboard.js`
- Envia `POST /report` para o CT (endpoint resolvido por `C:\conveniente\scripts\notifierEndpoints.js` + `C:\conveniente\scripts\ctConfig.js`)

**CT (`sitechatbot`)**:

- Recebe `POST /report` e persiste snapshot em disco em `C:\sitechatbot\index.js` (`handleReport` → `salvarSnapshot`)
- Calcula ONLINE/OFFLINE em `GET /servers` e fornece dados de estoque em `GET /api/stock/servers`
- Dispara atualização de UI via SSE (`servers_changed`) usando `C:\sitechatbot\convenientetecnologia\lib\ctRealtime.js`

**Dashboards**:

- `C:\sitechatbot\public\index.html` (Servidores): fetch `/servers` + SSE `/convenientetecnologia/api/stream`
- `C:\sitechatbot\convenientetecnologia\public\estoque.html` (Estoque): fetch `/api/stock/servers` (e outros endpoints de estoque)

### 2) Tabela forense — “arquivo/função → responsabilidade → risco → mitigação”

| lado | arquivo | função/rota | responsabilidade | riscos ao mexer | mitigação/compat (sem quebrar) |
|---|---|---|---|---|---|
| host | `C:\conveniente\scripts\dashboard.js` | `startDashboardMonitor()`, `tick()`, `postPayload()`, `tryAllEndpoints()` | monta e envia `/report` | atrasar/derrubar telemetria; overlap (`inFlight`); aumento de carga; regressão no applyCommands | feature-flag; dual-write (legacy+novo); outbox persistente; subprocess sender |
| host | `C:\conveniente\scripts\notifierEndpoints.js` | `resolveEndpoints()` | decide URL do CT para `/report` | CT errado/ngrok expirado ⇒ “host vivo, CT cego” | manter compat; log claro do endpoint escolhido; checklist de config |
| host | `C:\conveniente\scripts\ctConfig.js` | `readCtConfig()/writeCtConfig()` | persistência de `ctBaseUrl`/`logIngestSecret` | config ausente ⇒ telemetria/logs falham | fallback por env; nunca exigir secret em chat; validação no boot |
| CT | `C:\sitechatbot\index.js` | `POST /report` (`handleReport`) | valida payload + persiste snapshot | falha IO ⇒ 500; snapshot “congela”; side effects em estoque | journal append-only; retries de persistência; campos novos opcionais |
| CT | `C:\sitechatbot\index.js` | `salvarSnapshot()/writeAtomicJson()` | grava snapshot em disco | `EPERM/EBUSY`/antivírus; custo de IO | manter atomicidade; path estável por hostId; fallback seguro |
| CT | `C:\sitechatbot\index.js` | `GET /servers`, `GET /api/servers/meta` | calcula online/offline (grace+receivedAt) | falso offline; flapping; UX confuso | separar ONLINE (heartbeat) de STALE (snapshot); expor motivo/age |
| CT | `C:\sitechatbot\index.js` | `GET /api/stock/servers` + `stockInvResyncFromSnapshot()` | lista servidores para estoque + reconcilia inventário | divergência com `/servers`; churn; auto-resync indevido | alinhar ONLINE/STALE/OFFLINE; expor motivo/age; limitar auto-resync |
| CT | `C:\sitechatbot\convenientetecnologia\lib\ctRealtime.js` | SSE `subscribe()`/`broadcastThrottled()` | realtime de dashboards | overload SSE; eventos demais | throttling já existe; manter eventos mínimos |
| UI | `C:\sitechatbot\public\index.html` | `fetchServers()` + SSE | render “Servidores” | UI confundir ONLINE vs STALE | UI exibir motivo/age/grace; 3 estados |
| UI | `C:\sitechatbot\convenientetecnologia\public\estoque.html` | `loadServers()` | render “Estoque → Servidores” | divergência de status com `/servers` | alinhar com backend; mostrar motivo/age |

### 3) Lista de riscos que não podem ficar de fora (P0/P1)

- **P0**: quebrar `/report` (CT rejeitar payload) ⇒ CT cego.
- **P0**: persistência falhar (IO) ⇒ snapshot congelado ⇒ offline falso.
- **P0**: mexer em lógica de estoque ligada ao snapshot e causar efeito colateral (ex.: resync indevido).
- **P1**: aumentar frequência sem limites ⇒ tempestade de IO/rede.
- **P1**: flapping (UX) ⇒ operador perde confiança.

### 4) Estratégia de execução segura (quando for autorizado)

- **Fase 1 (observabilidade)**: adicionar diagnóstico (age/motivo) sem mudar critério de online.
- **Fase 2 (heartbeat em paralelo)**: host envia heartbeat leve + mantém legado (dual-write).
- **Fase 3 (canary RM3)**: habilitar modo novo só no RM3 e medir estabilidade.
- **Fase 4 (migrar UI)**: UI passa a 3 estados (ONLINE/STALE/OFFLINE).
- **Fase 5 (desligar legado)**: apenas com kill-switch e rollback prontos.

---

## Contrato de Integração CT ⇄ Hosts (NÃO pode quebrar)

Objetivo: listar, com evidência de paths, as “interfaces públicas” reais entre `conveniente` e `sitechatbot`.

### 1) Host → CT (telemetria e ingest)

- **Telemetria principal**: `POST /report` (e alias `POST /api/report`)
  - **CT**: `C:\sitechatbot\index.js` (`handleReport`)
  - **Host**: `C:\conveniente\scripts\dashboard.js` (`tryAllEndpoints()` → `postPayload()`)
  - **Invariante**: CT deve responder JSON `ok:true` e pode retornar `commands` para execução imediata no host.
  - **Invariante**: CT deve persistir snapshot e atualizar dashboards (`servers_changed`).

- **Ingest de logs sob demanda**: `POST /api/logs/ingest`
  - **CT**: `C:\sitechatbot\index.js` (`/api/logs/ingest`) — protegido por header `X-Log-Secret` (nome do secret: `LOG_INGEST_SECRET` no CT).
  - **Host**: `C:\conveniente\scripts\dashboard.js` (`execFetchLogs*` → envia items via `${base}/api/logs/ingest`).
  - **Invariante**: **não colar secrets em chat/docs**; apenas nome e onde configurar.

### 2) CT → Host (comandos), mas entregue via `/report` (não via “ping HTTP”)

- **Enfileirar comandos (UI/admin)**:
  - `POST /api/commands/enqueue` (autenticado) — CT enfileira no `commands.json`
  - `POST /api/commands/enqueue_secret` (secret/local) — canal admin sem UI/login
  - **CT**: `C:\sitechatbot\index.js`

- **Entrega**:
  - CT devolve `commands` na própria resposta do `POST /report`:
    - `return res.json({ ok: true, commands: cmds });`
    - **CT**: `C:\sitechatbot\index.js` (`handleReport` → `pullCommandsForHost(hostId)`)
  - **Host**: `C:\conveniente\scripts\dashboard.js` (`tick()`): se `resp.commands[]`, executa `applyCommands()`.

- **ACK (host confirma execução)**:
  - Host envia `POST ${base}/api/commands/ack` com `{ hostId, id, ok, error, details }`
    - **Host**: `C:\conveniente\scripts\dashboard.js` (`ackCommand()`)
    - **CT**: `C:\sitechatbot\index.js` (`POST /api/commands/ack`)
  - **Evidência persistida no CT**:
    - `C:\sitechatbot\dados\commands.json` (fila/inflight/broadcast)
    - `C:\sitechatbot\dados\commands.log` (eventos)
    - `C:\sitechatbot\dados\logs\<hostId>\ack_<cmdId>.json` (ACK “hardcore”, alta resolução)

### 3) AUTO-CONFIG (precisa ser compatível)

- **Host sinaliza** `needsConfig` quando `ctBaseUrl` ou `logIngestSecret` estão ausentes:
  - **Host**: `C:\conveniente\scripts\dashboard.js` (lê `C:\conveniente\dados\ct_config.json` via `readCtConfig()`).
- **CT reage** enfileirando `set_ct_config` automaticamente:
  - **CT**: `C:\sitechatbot\index.js` bloco `if (payload.needsConfig === true) { enqueueCommand(hostId, { type:'set_ct_config', payload:{ ctBaseUrl, logIngestSecret } }) }`
- **Risco de compatibilidade**:
  - hosts antigos podem não suportar `set_ct_config` ⇒ erro `unknown_command:set_ct_config` no ACK.
  - portanto, qualquer evolução aqui precisa de **rollout em fases** (dual-write/canary) e/ou detecção de capacidade por versão.

### 4) Sub-sistema Estoque (CT ⇄ host via secret)

O host chama o CT (não o contrário) usando header `X-Log-Secret` (mesmo secret do CT):

- `POST /api/stock/profile_credentials_secret` (host busca login/senha por `hostId+profileName`)
- `POST /api/stock/assigned/archive_with_evidence_secret` (host arquiva por ban/2FA com evidência)
- `POST /api/stock/uafp_event_secret` e `POST /api/stock/uafp_presets_secret` (telemetria UA/FP)
- `POST /api/stock/assigned/update_password_secret` (host reporta nova senha)
- `POST /api/stock/provision/from_account_secret` (host/terminal pode disparar provisionamento via CT; CT enfileira `stock_provision`)

Evidência:

- **CT**: `C:\sitechatbot\index.js` (rotas `*_secret`)
- **Host**: `C:\conveniente\scripts\worker.js` (fetch para esses endpoints por `${base}/api/stock/..._secret`)
- **Host**: `C:\conveniente\scripts\api_stock.js` (chama `/api/stock/available_secret` e `/api/stock/provision/from_account_secret`)

### 5) Dashboards (realtime) — não quebrar o “evento de atualização”

- **SSE stream**: `GET /convenientetecnologia/api/stream`
  - **CT**: `C:\sitechatbot\convenientetecnologia\index.js` (router `/api/stream`) + `C:\sitechatbot\convenientetecnologia\lib\ctRealtime.js`
  - **UI**: `C:\sitechatbot\public\index.html` escuta `servers_changed` e `tick`
  - **Invariante**: após receber `/report`, CT deve disparar `servers_changed` (throttled) para a UI refazer fetch.

### 6) Não-regressão (checklist mínimo antes de qualquer deploy)

- `/report` responde `ok:true` e **continua retornando** `commands: []` quando apropriado.
- `POST /api/commands/enqueue` + entrega via `/report` + `POST /api/commands/ack` continua funcionando (inclui persistência de `ack_<cmdId>.json`).
- `GET /servers` e `GET /api/stock/servers` continuam renderizando (UI Servidores e Estoque).
- SSE `/convenientetecnologia/api/stream` continua emitindo `servers_changed`/`tick` e a UI continua reagindo.
- `POST /api/logs/ingest` (com `LOG_INGEST_SECRET`) continua aceitando ingest e salvando resposta em `dados/logs`.

### 7) Checkup irrestrito (varredura v3) — itens encontrados que precisam estar no radar

Este bloco existe para garantir que **nada ficou para trás** antes do refactor.

#### CT (`sitechatbot`) — superfície de API e artefatos de persistência

- **Quantidade de rotas `/api/*` no `index.js`**: **83** (evidência: `C:\sitechatbot\index.js`).
- **Artefatos de fila/lock em disco** (importante para não quebrar atomicidade/locking):
  - `C:\sitechatbot\dados\commands.json`
  - `C:\sitechatbot\dados\commands.lock`
  - `C:\sitechatbot\dados\commands.log`
  - presença de múltiplos `commands.json.*.tmp` (indica escrita atômica/concorrência; não assumir que só existe 1 arquivo)
- **Artefatos de logs sob demanda**:
  - `C:\sitechatbot\dados\logs\requests.jsonl` (journal de requests de logs/manifest)
  - `C:\sitechatbot\dados\logs\<hostId>\ack_<cmdId>.json` (ACK por cmd)

#### Host (`conveniente`) — chamadas adicionais ao CT além do `/report`

Além de `/report`, existem integrações host→CT que não podem ser quebradas:

- **ACK de comandos**: `POST /api/commands/ack` (host: `C:\conveniente\scripts\dashboard.js` `ackCommand()`)
- **Ingest de logs**: `POST /api/logs/ingest` (host: `C:\conveniente\scripts\dashboard.js`)
- **Estoque secret** (host: `C:\conveniente\scripts\worker.js` e `api_stock.js`)
- **FB GPT ingest/resolve** (host: `C:\conveniente\scripts\gptFallback.js`)
  - `POST /api/fb_gpt/ingest` (header `X-Log-Secret`)
  - `POST /api/fb_gpt/resolve_secret` (header `X-Log-Secret`)

#### Outros atores (não são o “/report”, mas convivem no mesmo CT e exigem cuidado em mudanças globais)

- **Notificador**:
  - `GET /api/notifier/next` e `POST /api/notifier/ack` (CT: `C:\sitechatbot\index.js`)
- **Virtus**:
  - existe `POST /api/virtus/ack` (CT: `C:\sitechatbot\index.js`)

> Implicação: mudanças “globais” em middleware/auth/IO/event-loop no `sitechatbot/index.js` podem afetar estes subsistemas também. Qualquer refactor do pipeline `/report` deve ser cirúrgico e isolado por flags.

### 8) Checkup “ponta a ponta” v4 — compatibilidade CT→Host (tipos de comando)

Objetivo: garantir que o CT não está emitindo comandos que o host não sabe executar (e vice-versa), evitando `unknown_command:*`.

#### Tipos de comando vistos no CT (estado real)

Extraído de `C:\sitechatbot\dados\commands.json` (broadcast + queue + inflight):

- **count**: 15
- **types**:
  - `backup_export_files`
  - `delete_perfis`
  - `fetch_logs`
  - `fetch_logs_query`
  - `health_bundle`
  - `list_backups`
  - `logs_manifest`
  - `migrate_profiles`
  - `open_all_24h`
  - `robes_release_all`
  - `self_update`
  - `set_ct_config`
  - `stock_export_profiles`
  - `stock_provision`
  - `stock_push_account_update`

#### Tipos de comando suportados pelo host (estado real)

Extraído de `C:\conveniente\scripts\dashboard.js` (`applyCommands()`):

- **count**: 19
- **types**:
  - `close_all`
  - `delete_perfis`
  - `fetch_logs`
  - `fetch_logs_query`
  - `health_bundle`
  - `login_remediate`
  - `logs_manifest`
  - `migrate_profiles`
  - `open_all_24h`
  - `profiles_cleanup`
  - `provision_unlock`
  - `robes_pause_24h_all`
  - `robes_release_all`
  - `rotate_logs`
  - `self_update`
  - `set_ct_config`
  - `stock_export_profiles`
  - `stock_provision`
  - `stock_push_account_update`

#### Diferenças (risco direto de `unknown_command`)

- **CT emite, host NÃO suporta (risco P0)**:
  - `backup_export_files`
  - `list_backups`

Evidência adicional: há ocorrência de `unknown_command:*` no `commands.json`, ex.:
- `unknown_command:set_ct_config` (hosts antigos)
- `unknown_command:health_bundle` (hosts antigos)
- `unknown_command:backup_export_files` e `unknown_command:list_backups` (CT emitiu, host não tinha handler)

**Implicação enterprise**: antes de qualquer refactor, precisamos definir política de compatibilidade por versão/capability:

- CT só emite comandos “novos” para hosts que provaram suportar (capability handshake), **ou**
- CT usa rollout por canary + fallback (não broadcast), **ou**
- CT converte esses comandos em “no-op” para hosts antigos (sem poluir com falhas).

---

## Plano anexado (rascunho, **não executar ainda**)

Arquivo de plano gerado pelo GPT (Cursor): `c:\Users\NOTIFICADOR\.cursor\plans\ct_status_correctness_62874b2c.plan.md`

Resumo do plano (conteúdo principal):

- Criar este INC canônico e registrar em `INBOX_RELATOS_DO_HUMANO.md` + `docs/inbox/INDEX.md`.
- (Quando formos executar) ajustar o CT para expor **diagnóstico do OFFLINE** (idade do report, grace usado, motivo) em `/servers` e refletir isso na UI de Servidores e Estoque, para eliminar confusão e “falso positivo sem explicação”.

### Plano detalhado (copiado aqui para ficar canônico dentro do repo)

> **Importante**: este é um rascunho para execução futura; neste momento NÃO vamos alterar código.

- **Ticket base (WIP=1)**: RM3 aparece OFFLINE no CT (dashboard Servidores e Estoque → Servidores).
- **Foco**: tornar o estado ONLINE/OFFLINE **auditável**.
  - quando OFFLINE, mostrar explicitamente: “CT não recebeu `/report` há Xs”, “grace usado”, e qualquer dedupe/ghost.
- **Mudanças planejadas (CT)**:
  - Backend `C:\sitechatbot\index.js`: incluir campos de diagnóstico no payload de `/servers` (e também na lista usada por `estoque.html`):
    - `lastReportReceivedAt`, `lastReportSentAt`, `reportAgeMs/reportAgeSec`, `graceSecsUsed`, `onlineReason`, `offlineSinceAt`
  - UI `C:\sitechatbot\public\index.html`: exibir grace real e o motivo/idade do report no card/modal.
  - UI `C:\sitechatbot\convenientetecnologia\public\estoque.html`: exibir o mesmo diagnóstico na lista de servidores.
- **Rollout futuro**:
  - vai exigir restart do CT (`sitechatbot`) para aplicar (humano: `node index.js`).

---

## Próximo passo (somente quando você autorizar execução)

- Escolher política “ultra enterprise” para:
  - dedupe de ghosts (quando existir duplicidade)
  - grace TTL (trade-off entre falso offline vs detecção rápida)
- Implementar as mudanças no `sitechatbot` + validar com evidência (sem achismo).

