### INC-20260130-2015-02 — P2: Abrir Todos deveria zerar flags para reavaliar estado real

- **P**: P2
- **Data**: 2026-01-30
- **Sistema**: `conveniente`
- **Host alvo**: RM3

---

## Status (rígido)

- **Estado atual**: `done`
- **Rollout**: `deployed`
- **Validation**: `passed`

---

## Sintoma (humano)

- “Ao clicar Abrir Todos / abrir conta, deveria zerar flags para reavaliar estado real; flags antigas podem engessar.”

---

## Hipóteses

- H1: flags persistidas (`loginRequired`, `loginRemediateFailed`) permanecem apesar da UI já ter mudado, causando bloqueio indevido.
- H2: o fluxo atual já reavalia e atualiza flags, então zerar tudo seria redundante ou perigoso (pode liberar conta ainda em login).

---

## Evidência necessária (CT)

- `fetch_logs_query` (RM3):
  - keys: `status`, `desired`, `login_required_events`, `provision_audit`
  - patterns: perfil alvo + `open_all` + `login_form` + `loginRequired`
- Comparar “antes de abrir” vs “após abrir” para confirmar flags presas e UI real.

---

## Impacto / risco

- Zerar flags pode mascarar estados reais e permitir automação em contas que ainda exigem login/2FA.

---

## Rollout / Restart

- **Precisa reiniciar agora?** não
- **Precisa reiniciar para validar?** não (apenas coleta de logs)

---

## Fechamento (2026-01-31)

- **Resultado**: o humano confirmou que do lado dele “ficou perfeito/pronto” para o uso atual.
- **Nota**: se a necessidade de “zerar flags ao abrir” voltar a ser relevante com outro sintoma, abrir novo INC e correlacionar com este como histórico.
