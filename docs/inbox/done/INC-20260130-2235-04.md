### INC-20260130-2235-04 — P1: “Confirme que você é humano” precisa auto-click

- **P**: P1
- **Data**: 2026-01-30
- **Sistema**: `conveniente`
- **Host alvo**: RM3 — `hostId=5d7c3309-8581-4a50-a421-e6cbb52d8070`

---

## Status (rígido)

- **Estado atual**: `done`
- **Rollout**: `deployed`
- **Validation**: `passed`

---

## Sintoma (humano)

- Perfil `blumenau-1769748927066` abriu na tela “confirme que você é humano”.
- O humano clicou “Continuar” manualmente; depois apareceu captcha/checkpoint.
- O sistema deveria **clicar “Continuar” automaticamente** para antecipar o processo e só então invocar humano.

---

## Hipóteses (GPT)

- H1: a tela “confirme que você é humano” não é reconhecida por `detectLoginRequired` e não dispara o fluxo de auto-click.
- H2: existe reconhecimento, mas não há ação automática para clicar “Continuar” antes de invocar humano.
- H3: o “Continuar” é renderizado em um container diferente e o seletor atual não cobre esse caso.

---

## Evidência a coletar (sem pedir investigação humana)

- `provision_audit` com padrões:
  - `human_reconcile_login_required`
  - `captcha_persona`
  - `checkpoint`
  - `confirm_human`
  - `human_resume_preflight_*`
- `login_required_events` para:
  - `blumenau-1769748927066`
  - `caxias_do_sul-1769748234162`

---

## Arquivos prováveis

- `C:\conveniente\scripts\browser.js` (detectLoginRequired / heurísticas)
- `C:\conveniente\scripts\worker.js` (fluxo `human_reconcile_*` / `login_remediate`)

---

## Mapa de impacto (obrigatório)

- **Fluxo ponta a ponta (alto nível)**: abrir navegador → detectar estado da página → decidir login_required → auto_login_remediate / invoke_human
- **Callers**:
  - `reconcileHumanState`
  - `autoLoginRemediate`
  - `human-resume`
- **Callees**:
  - `browserHelper.detectLoginRequired`
  - `failFastToHuman`
- **Estados tocados**: `perfis.json` / `status.json` / `desired.json` / flags `loginRequired` + `humanHold`
- **Efeitos colaterais possíveis**: auto-click incorreto pode avançar fluxo indevido; mitigação será guardrail por URL/domínio/heurística.
- **Risco de regressão**: baixo se restrito a tela específica.

---

## Plano (mudança mínima)

- Coletar evidência runtime (CT) para confirmar a tela e o caminho de decisão atual.
- Implementar auto-click “Continuar” **somente** quando heurística confirmar a tela “confirme que você é humano”.
- Validar com logs de `provision_audit` e `login_required_events`.

---

## Rollout / Restart

- **Precisa reiniciar agora?** não
- **Precisa reiniciar para validar/testar?** sim (após mudança)
- **Como reiniciar (humano)?** `node index.js`

---

## Fechamento (2026-01-31)

- **Resultado**: o humano confirmou que, para o uso atual, está “perfeito/pronto” e vai seguir testando outros pontos.
- **Nota**: se o caso “Confirme que você é humano → auto-click Continuar” voltar a ser necessário, reabrir como novo INC com evidência (HTML/URL + `provision_audit`).

