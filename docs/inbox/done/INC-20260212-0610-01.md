### INC-20260212-0610-01 — RM2: auditoria CT vs servidor (mesmo playbook do RM1) — alinhamento estoque/em uso/inventário (CONCLUÍDO)

- **P**: P0
- **Sistema**: `sitechatbot` (CT/estoque) + `conveniente` (RM2)
- **Status**: `done`
- **Rollout**: `deployed` (correções aplicadas no CT + limpeza executada nos hosts)
- **Validation**: `passed` (export+auditoria pós-correção + limpeza CT)

---

## Host alvo

- **RM2**: `hostId=bcf01e8d-82da-4d5d-aed0-d60305d4696d`

---

## Objetivo

Repetir o procedimento canônico validado no RM1 para garantir que o RM2 está 100% alinhado:

- **CT “Em uso” (assigned)** vs **perfis reais no servidor**
- detectar:
  - duplicados no servidor (mesma conta duplicada),
  - contas atribuídas em 2+ hosts (cross-host),
  - “fantasmas” (CT `assigned` sem perfil no servidor),
  - perfis no servidor sem correspondência no CT (estoque descontrole).

---

## Evidência obrigatória (a coletar via CT)

- `stock_export_profiles` (RM2) (ideal: `includeCookies=false`)
  - evidência: `C:\sitechatbot\dados\logs\<hostId>\ack_<cmdId>.json`
- Auditoria offline no CT:
  - script: `C:\sitechatbot\scripts\auditHostAssigned.js`
  - saída: anexar no próprio INC (resumo) e salvar o arquivo de saída no CT se necessário.

---

## Plano adicional (CANÔNICO) — alinhamento no disco (órfãos / recovery / purge)

Além da auditoria CT↔servidor, executar o playbook de “órfãos no disco” para evitar:

- `dados/perfis/` inchando indefinidamente,
- perfis quentes perdidos fora do `perfis.json`,
- risco de “ressuscitar legado” após updates.

Referência canônica (RM1 validado):

- Runbook: `C:\conveniente\docs\RUNBOOK_TECNICO.md` (seção “Alinhamento no disco…”)
- Checkup: `C:\conveniente\docs\checkups\checkup_2026-02-13_rm1_profiles_orphans_alignment.md`

Procedimento resumido por host (via CT):

1) `profiles_fs_audit` (somente leitura)
2) classificar órfãos (CT: `ct_audit_orphans_from_profiles_fs_audit_ack.js`)
3) `profiles_purge_dirs` (CT-deleted; dry-run → apply)
4) `profiles_manifest_probe` (CT-missing)
5) `profiles_relink_orphans` (para teste visual humano; `humanHold=true`)
6) `delete_perfis` (excluir reprovados)
7) purge final de sobras (`profiles_purge_dirs`)

---

## Evidência coletada (rodada atual)

### 1) Export do servidor (RM2 → CT)

- `cmdId=7624de5a-60e3-4e24-90ad-25df8c790827` (ACK ok=true)
- Evidência:
  - `C:\sitechatbot\dados\logs\bcf01e8d-82da-4d5d-aed0-d60305d4696d\ack_7624de5a-60e3-4e24-90ad-25df8c790827.json`
- Contagem reportada: `profilesCount=99`

### 2) Auditoria CT vs servidor (script)

- Script: `C:\sitechatbot\scripts\auditHostAssigned.js`
- Execução (CT):
  - `node scripts/auditHostAssigned.js --hostId bcf01e8d-82da-4d5d-aed0-d60305d4696d --ackPath <ack_export>`

Resumo (saída do script):

- **serverProfiles**: 99
- **ctAssignedRows**: 106
- **ctAssignedDistinctProfiles**: 101
- **dupAssignedProfiles**: 5
- **missingAssignedProfilesByName**: 10
- **untrackedServerProfilesByName**: 8
- **serverCookieFpDuplicates**: 1

Achados principais (com amostras objetivas):

#### A) Duplicado no servidor (mesmo `cookie_fp` em 2 perfis)

- `cookie_fp=09e03d704f30359a46f7b37beb7006feef6cd77e` aparece em:
  - `belem-1770323384894`
  - `macapa-1770325365131`

#### B) Cross-host (perfil existe no RM2, mas no CT a conta está assigned em outro host)

O script detectou 6 casos (amostras):

- Perfil no RM2 `belem-1770323384894` (`fp=09e03d...`) → CT `id=792` assigned no **RM3** (`ctProfile=recife-1770324673993`)
- Perfil no RM2 `macapa-1770325365131` (`fp=09e03d...`) → CT `id=792` assigned no **RM3** (`ctProfile=recife-1770324673993`)
- Perfil no RM2 `teresina-1770454520620` (`fp=91282d...`) → CT `id=845` assigned no **RM3** (`ctProfile=manaus-1770454728487`)
- Perfil no RM2 `balneario_camboriu-1770782540749` (`fp=5bb5de...`) → CT `id=964` assigned no **RM6** (`ctProfile=balneario_camboriu-1770784100654`)
- Perfil no RM2 `campinas-1770796410149` (`fp=5f6959...`) → CT `id=998` assigned no **RM1** (`ctProfile=cuiaba-1770797650567`)
- Perfil no RM2 `ribeirao_preto-1770835353293` (`fp=08512e...`) → CT `id=1040` assigned no **RM1** (`ctProfile=ribeirao_preto-1770834790438`)

#### C) “Leak” (CT diz available, mas perfil existe no servidor)

- Perfil `vila_velha-1770843729041` (`fp=c97711...`) → CT `id=1060` status=available
- Perfil `campinas-1770846166980` (`fp=654b6f...`) → CT `id=1066` status=available

#### D) CT com duplicidade (2 contas assigned no CT para o mesmo `assigned_profile_name`)

Perfis com duplicidade no CT (amostra do script):

- `ponta_grossa-1769669540929`: CT ids `[335, 563]` (serverFp bate com `563`)
- `juiz_de_fora-1769785519636`: CT ids `[583, 601]` (serverFp bate com `601`)
- `aracaju-1769800188002`: CT ids `[614, 663]` (serverFp bate com `663`)
- (Outros 2 casos reportados pelo script: `juazeiro_do_norte-1769118746257`, `taubate-1769786591106` — serverFp não apareceu no export)

---

## Correções executadas (RM2 manda)

### 1) Remover duplicado no RM2

- Decisão humana: **remover** `belem-1770323384894` e manter `macapa-1770325365131`.
- Execução:
  - `delete_perfis` no RM2
  - Evidência: `C:\sitechatbot\dados\logs\bcf01e8d-82da-4d5d-aed0-d60305d4696d\ack_426194a4-9ccb-4bde-9de5-3c1754b11aaf.json`

### 2) Cross-host → remover do host “errado” e alinhar CT para o RM2

- Execução (remoção física):
  - RM3: remover `recife-1770324673993`, `manaus-1770454728487`
    - Evidência: `C:\sitechatbot\dados\logs\5d7c3309-8581-4a50-a421-e6cbb52d8070\ack_6b479103-d33b-4b8f-be12-54ebc542c59b.json`
  - RM1: remover `cuiaba-1770797650567`, `ribeirao_preto-1770834790438`
    - Evidência: `C:\sitechatbot\dados\logs\084c8fff-c508-47bd-a33e-3ab34aeb1e3d\ack_bb0cef03-53eb-4263-a60d-a181b05709c5.json`
  - RM6: remover `balneario_camboriu-1770784100654`
    - Evidência: `C:\sitechatbot\dados\logs\aba80611-8b8a-46ed-a852-f5f0d573c03b\ack_32f544f9-a834-4f9a-808e-cc861c83d3e5.json`

- Execução (alinhamento CT → RM2):
  - Script: `C:\sitechatbot\tools\ct_apply_rm2_alignment.js` (reassign por `accountId`)
  - Mapa aplicado: `C:\sitechatbot\tools\_rm2_ct_reassign_accounts.json`
  - Observação operacional: após os deletes cross-host, alguns `accountId` foram **arquivados automaticamente** (efeito colateral do fluxo de delete); o script foi **reaplicado** para garantir `status=assigned` no CT apontando para RM2.

### 3) Leak (CT available, mas existe no RM2) → corrigir no CT

- Contas:
  - `id=1060` → `vila_velha-1770843729041`
  - `id=1066` → `campinas-1770846166980`
- Execução:
  - Reassign no CT (mesmo script/mapa acima) para `status=assigned` e `assigned_host_id=RM2`.

### 4) Duplicidade no CT (casos com prova por `cookie_fp`)

- Arquivar duplicado “errado” no CT (mantendo o que bate com `cookie_fp` do servidor):
  - `ponta_grossa-1769669540929`: manter `id=563`, arquivar `id=335`
  - `juiz_de_fora-1769785519636`: manter `id=601`, arquivar `id=583`
  - `aracaju-1769800188002`: manter `id=663`, arquivar `id=614`
- Evidência (input aplicado):
  - Script: `C:\sitechatbot\tools\ct_apply_rm2_alignment.js`
  - Mapa: `C:\sitechatbot\tools\_rm2_ct_archive_duplicates.json`

---

## Validação pós-correção (prova)

### Export atualizado (RM2 → CT)

- `cmdId=6567dc11-d4cb-4268-8fc3-a15ef23221e8` (ACK ok=true)
- Evidência:
  - `C:\sitechatbot\dados\logs\bcf01e8d-82da-4d5d-aed0-d60305d4696d\ack_6567dc11-d4cb-4268-8fc3-a15ef23221e8.json`
- Contagem reportada: `profilesCount=97`

### Auditoria pós-correção (script)

Resumo (saída do script):

- **serverProfiles**: 97
- **serverCookieFpDuplicates**: 0
- **ownership.assignedDifferentHost**: 0
- **ownership.availablePresentOnServer**: 0
- **untrackedServerProfilesByName**: 0

Pontos restantes (não tratados nesta rodada, requer decisão/plano próprio):

- **dupAssignedProfiles**: 2 (apenas no CT, e **não** aparecem no servidor)
  - `juazeiro_do_norte-1769118746257` (CT ids `[449, 482]`)
  - `taubate-1769786591106` (CT ids `[588, 602]`)
- **missingAssignedProfilesByName**: 10 (CT assigned no RM2, mas sem perfil no export do RM2)

---

## Limpeza final no CT (fantasmas / duplicidades “no CT”)

Regra (decisão humana): **só limpar no CT se houver evidência de delete no servidor**.

Evidência (CT):

- Os 10 perfis abaixo estavam **assigned no CT para o RM2**, mas **não estavam no export atual do RM2**.
- Para **todos** eles existe registro em `ct_fb_stock_server_profiles` com `deleted_on_server_at` (deletado no servidor).
  - (classificação offline no CT: `tools/ct_classify_missing_assigned.js`)

Lista (por `assigned_profile_name`):

- `juazeiro_do_norte-1769118746257` (CT ids `[449, 482]`)
- `goiania-1769671497763` (CT id `564`)
- `joinville-1769784281555` (CT id `600`)
- `taubate-1769786591106` (CT ids `[588, 602]`)
- `duque_de_caxias-1769788178138` (CT id `603`)
- `sao_paulo-1769809731635` (CT id `639`)
- `fortaleza-1770463738541` (CT id `858`)
- `natal-1770530710572` (CT id `888`)
- `maringa-1770810374725` (CT id `1025`)
- `sao_jose_do_rio_preto-1770845215371` (CT id `1067`)

Execução (CT):

- Script: `C:\sitechatbot\tools\ct_archive_assigned_profiles_by_name.js`
- Input: `C:\sitechatbot\tools\_rm2_missing_assigned_profiles_10.json`
- Resultado: **12 contas arquivadas** (as 2 duplicidades “no CT” viraram arquivo junto com os 10 fantasmas)

Observação operacional:

- Após o arquivamento, foi reaplicado o alinhamento “RM2 manda” para re-atribuir contas autoritativas do RM2 (evitar `archivedPresentOnServer`):
  - Script: `C:\sitechatbot\tools\ct_apply_rm2_alignment.js` (reapply)

---

## Validação final (CT limpo + RM2 alinhado)

Auditoria offline (CT) usando o mesmo export do RM2:

- **serverProfiles**: 97
- **ctAssignedRows**: 97
- **dupAssignedProfiles**: 0
- **missingAssignedProfilesByName**: 0
- **untrackedServerProfilesByName**: 0
- **serverCookieFpDuplicates**: 0

---

## Dependências / INCs relacionados

- Restore operacional RM2 (já feito):
  - `docs/inbox/done/INC-20260212-0240-01.md`
- Investigação do wipe RM2 (aberto):
  - `docs/inbox/in_progress/INC-20260212-0315-01.md`

