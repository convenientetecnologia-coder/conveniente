========================================
MANUTENCAO WINDOWS v5.2.1-clean-cpu
========================================

NAO altera C:\conveniente.
NAO limpa StandbyList / RAM. Isso ficou no Conveniente
(scripts\chromeMemorySweep.js no processo index).

FONTE CANONICA (servidor novo e MAE):
  C:\conveniente\porteiro\   (repo GitHub do Conveniente)
  Dono: o WINDOWS (tarefa ConvenientePorteiro ao logon).
  O index.js NAO e o dono. Ele so CORRIGE a versao se o arquivo/loop
  for o velho (mem_soft / v5.1.13 / Get-CpuAvg). Se ja for v5.2.1-clean-cpu e estiver
  vivo, o index NAO mexe. O Porteiro continua com o Conveniente desligado.

INSTALAR / ATUALIZAR:
  1) irm https://raw.githubusercontent.com/convenientetecnologia-coder/conveniente/main/instalar_conveniente.ps1 | iex
  2) Fecha o PowerShell, reabre, clona + npm (CONTRATO.txt).
  3) Clique INICIAR (porteiro\INICIAR_SISTEMA.vbs ou atalho Iniciar Conveniente).
     Sobe o Conveniente. Sem admin. Sem OK. Se o loop do Porteiro estiver morto,
     religa em silencio. AUTO/AUTO_BOOT (04h) NAO pedem UAC.

CAMINHO ANTIGO (esta pasta na area de trabalho):
  1) Botao direito em Setup_Manutencao.bat -> Executar como administrador
  Continua valido. O kit aqui e o mesmo v5.2.1-clean-cpu, sem limpador e sem medida de CPU.

DESLIGAR REBOOT EM 1 PC (ex.: notebook de trabalho):
  Crie o arquivo: C:\auto_vigia\NO_REBOOT.flag
  STATUS deve mostrar: RebootDaily=DESLIGADO
  (servidores SEM esse arquivo continuam reiniciando)

CONTEUDO DESTA PASTA:
  Setup_Manutencao.bat   <- instalador local
  LEIA-ME.txt
  INICIAR_SISTEMA.vbs
  PARAR_SISTEMA.bat
  STATUS.bat
  kit\manutencao.ps1     <- porteiro (loop MemClean=OFF; cria ConvenienteDiskClean)
  kit\install.ps1        <- usado pelo Setup

O QUE O SETUP INSTALA:
  C:\auto_vigia\manutencao.ps1
  Tarefa: ConvenientePorteiro (ao logon)
  Tarefa: ConvenienteNetBoot (startup SYSTEM - checa rede em TODO boot)
  Tarefa: ConvenienteDiskClean (SYSTEM, on-demand — o Conveniente dispara)
  Atalhos no Desktop: PARAR / INICIAR / STATUS

REBOOT DIARIO:
  - So entre 04:00 e 04:20: limpa TEMP+Lixeira e reinicia (1x/dia)
  - Instalar de tarde/noite NAO dispara reboot

GUARDA DE REDE (TODO BOOT) — remedio = so reiniciar:
  - Espera 4 min apos ligar
  - Testa internet 4 vezes (nao reinicia por 1 ping falho)
  - So reinicia se a placa sumiu de verdade (lista vazia)
  - Max 1 reboot extra por dia; se ainda falhar, para
  - ConvenienteNetBoot (SYSTEM) + porteiro no logon

MEMORIA (StandbyList):
  - Loop do Porteiro NAO dispara DiskClean.exe (MemClean=OFF / mem_off).
  - Porteiro so cria a tarefa SYSTEM ConvenienteDiskClean (on-demand).
  - Dono do relogio: Conveniente (index), a cada 15 min pede o Run ao Windows.
  - No log do porteiro: ACTION=...mem_off (prova de que o loop nao limpou RAM)

DISCO:
  - Madrugada: 1x/dia 04:30-06:00 (horario diferente por maquina)
  - Emergencia de dia: so se livre < 4 GB (max 5h+offset). Sem gate de CPU.
  - TEMP + Lixeira apenas (nao mata Chrome)

AUTO_BOOT:
  - Se o Conveniente estiver down 2 ciclos (~6 min), sobe de novo.
  - Se JA estiver ligado, NUNCA sobe segundo index.js.
  - Sobe powershell nativo (titulo Conveniente_Node). Nao usa cmd.exe.
  - PARAR / INICIAR / STATUS continuam iguais.
