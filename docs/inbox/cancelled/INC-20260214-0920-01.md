### INC-20260214-0920-01 — Duplicação zero (CT/estoque): forense + invariantes + prevenção definitiva

- **P**: P0
- **Sistema**: `sitechatbot` (CT/estoque)
- **Status**: `cancelled`
- **Rollout**: `not_deployed`
- **Validation**: `not_run`

---

## Encerramento (fundido)

Este INC foi fundido no `INC-20260214-0900-01` para execução em programa único P0.
As invariantes anti-duplicação do CT continuam mandatórias, porém dentro do mesmo pacote de execução.

## Contexto

Mesmo com servidores alinhados (RM1..RM7), apareceram conflitos de:

- duplicidade no CT (`assigned_profile_name` em múltiplos IDs)
- cross-host (mesmo `cookie_fp` ativo em mais de um host)
- “leaks” (CT disponível, mas perfil existe no host)

Precisamos sair com causa raiz + prevenção “pra sempre” (sem operação manual recorrente).

---

## Escopo

### In-scope

- Forense de duplicações:
  - dentro do CT (`ct_fb_stock_accounts`)
  - entre hosts (`ct_fb_stock_server_profiles` + conflitos por `cookie_fp`)
  - fluxo de transição `available -> reserved -> assigned -> archived/released`
- Hardening de invariantes:
  - 1 conta assigned em 1 host
  - 1 `cookie_fp` ativo (quando aplicável)
  - regras de conflito e bloqueio de promoção de estado
- Remediação determinística (jobs/flags) para conflitos detectados

### Out-of-scope

- Confiabilidade do canal de comandos/ACK: `INC-20260214-0910-01`
- Provision transacional CT↔host: `INC-20260214-0900-01`

---

## Evidência requerida

- Queries e contagens (sem secrets) para:
  - contas `reserved`/`assigned` incoerentes
  - duplicatas por `cookie_fp`
  - `assigned_profile_name` duplicado
  - server_profiles com `deleted_on_server_at` vs estado atual
- Linha do tempo por conta (job history + mudanças)

Fontes:
- `C:\sitechatbot\dados\convenientetecnologia.sqlite`
  - `ct_fb_stock_accounts`, `ct_fb_stock_jobs`, `ct_fb_stock_server_profiles`
- `C:\sitechatbot\convenientetecnologia\lib\ctFbStock.js` (transições + detectAndMarkConflicts)
- `C:\sitechatbot\convenientetecnologia\lib\ctDb.js` (constraints/índices)

---

## Critério de “done”

- Duplicações detectadas são **bloqueadas** antes de virarem “estado ativo” (assigned/active)
- Conflitos por `cookie_fp` geram estado explícito + job/ação recomendada (sem “mistério”)
- “Leak” tem regra canônica e trilha de remediação (não fica para sempre)
- Forense deixa resposta objetiva: por que duplicava, qual condição disparava, como foi impedido

