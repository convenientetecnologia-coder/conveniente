### INC-20260214-0950-01 — Registry secundário por perfil (`dados/perfis/<nome>/...`): spec + redaction + uso seguro

- **P**: P1
- **Sistema**: `conveniente` (hosts) + suporte CT (somente para forense/recovery)
- **Status**: `cancelled`
- **Rollout**: `not_deployed`
- **Validation**: `not_run`

---

## Encerramento (fase 2)

Escopo reclassificado para fase 2.
Não bloqueia o GO do programa único de cadastro sem duplicação (`INC-20260214-0900-01`).

## Contexto

Queremos um “banco secundário perfeito” por perfil para:

- forense (quem/como/quando)
- recovery (se `perfis.json` corromper)
- auto-retry de limpeza (deleções pendentes)

Mas com regra absoluta:

- **NUNCA** usar registry secundário como “fonte da verdade” do dashboard (para não ressuscitar lixo).

---

## Escopo (spec)

- Definir quais arquivos existem em `dados/perfis/<nome>/` e seu contrato:
  - `perfil.json` (metadados não sensíveis)
  - `events.jsonl` (ledger por perfil)
  - `delete_pending.json` (se aplicável)
- Definir o que **NUNCA gravar**:
  - evitar secrets em logs/docs
  - cookies/login/senha: regras claras de armazenamento (disco vs audit) e redaction
- Definir como usar para:
  - reconstrução assistida (somente quando `perfis.json` faltar/corromper)
  - prova de exclusão e re-tentativas

---

## Critério de “done”

- Spec canônico (contrato de arquivos + campos) documentado
- Regras de redaction e “no secrets” cristalinas
- Regras de rebuild/recovery que não ressuscitam lixo (gates + dry-run)

