### INC-20260207-1403-01 — RM3 50/50/24 após stock_provision (Virtus OFF em massa)

- **P**: P0 (impacto financeiro: filas sem atendimento)
- **Sistema**: `conveniente` (executor) + `sitechatbot` (CT evidência)
- **Host**: ROBE MÃE 3
  - **hostId**: `5d7c3309-8581-4a50-a421-e6cbb52d8070`

---

## Sintoma (humano)

- RM3 aparece com **50 total / 50 ativos / 24 trabalhando** “do nada”, apesar dos navegadores estarem “perfeitos” e chegando chamados.

---

## Evidência (CT / disco)

### E1) Snapshot CT confirma exatamente 50/50/24 e Virtus OFF nos 26 parados

- **Arquivo**: `C:\sitechatbot\dados\5d7c3309-8581-4a50-a421-e6cbb52d8070-30b3fe928b.json`
- **receivedAt**: `1770438472213`
- **Counts (derivado do snapshot)**:
  - total perfis: 50
  - active: 50
  - trabalhando: 24
  - virtusOnline: 24
  - **stuck (active && !trabalhando)**: 26
  - **loginRequired=true**: 2
  - **configurando=true**: 1

Conclusão objetiva: os 26 “parados” estão com **`virtusOnline=false`**, então não é só “contador/UI”; o Virtus está realmente OFF nesses perfis.

### E2) Stock provision imediatamente antes com quiesce pause timeout ignorado (best-effort)

- **ACK**: `C:\sitechatbot\dados\logs\5d7c3309-8581-4a50-a421-e6cbb52d8070\ack_13ec45e0-25aa-4dbe-b845-b2e1a2e7aa85.json`
- **cmdType**: `stock_provision`
- **cmdId**: `13ec45e0-25aa-4dbe-b845-b2e1a2e7aa85`
- **Ponto crítico**:
  - `quiesce_pause_done`: `ok=false`
  - `pauseableVirtusCount`: 47
  - `waitPauseMs`: 120000
  - Em seguida: `quiesce_pause_best_effort` com `note="pause_timeout_ignored_best_effort"`

### E3) Overlap (stuck agora vs pauseable do quiesce)

- **stuckCount**: 26
- **pauseableNamesCount**: 40 (lista de nomes no ACK; o campo `pauseableVirtusCount` reporta 47)
- **overlapCount**: 20
- **Leitura**: parte relevante dos perfis parados agora estava no conjunto que o `stock_provision` tentou “pausar” via quiesce.

### E4) Prova do “por que não voltou”: nurse está pulando perfis fora do shard (órfãos)

Coleta via CT (fetch_logs):

- **Bundle**: `C:\sitechatbot\dados\logs\5d7c3309-8581-4a50-a421-e6cbb52d8070\inc_20260207_1403_rm3_1770439615172.json`
  - item `provision_audit` contém `C:\conveniente\dados\provision_audit.jsonl` (tail).

Na janela:

- **tStart** (lock_acquired do stock_provision): `1770436775102` (ver ACK E2)
- **tEnd** (snapshot observado do CT com working=24): `1770438472213`

Resultado (derivado do `provision_audit.jsonl`):

- Para os **40 nomes** listados em `pauseableVirtusNames` do ACK E2, **40/40** tiveram evidência repetida de:
  - `event="nurse_skip_other_shard_active"` até perto do `tEnd`
  - com `shardSize≈25` e `pid=26120` (exemplos)

Leitura objetiva: o reconciliador (`nurseTick`) desse processo está **ignorando** perfis `desired.active=true` porque eles **não pertencem ao shard desse worker**. Se durante o `provision_lock` esses perfis tiveram o Virtus pausado, eles podem ficar OFF **por tempo indefinido** se o worker do shard “correto” não estiver rodando/saudável.

Isso bate com o sintoma: “volta parcial” (ex.: 24 trabalhando) em vez de “volta total”.

---

## Classificação (forense) dos 26 parados (a partir do snapshot)

- **R1 — LoginRequired (esperado bloquear Virtus)**: 2 perfis
  - ex.: `loginReason=login_form`
  - ex.: `loginReason=probe_failed`
- **R2 — Configurando (esperado bloquear Virtus temporariamente)**: 1 perfil
- **R3 — Sem bloqueio explícito (anomalia)**: 23 perfis
  - padrão: `loginRequired=false`, `humanHold=false`, `humanControl=false`, `problem=false`, `whyNotOpen=null`, `manifestStatus=ok`, `isFrozen=false`, **`virtusOnline=false`**
  - isso deveria ser auto-curado pela reconciliação (`nurseTick`/`start_work`) se a automação estiver permitida.

---

## Hipóteses (sem achismo; precisam de logs do host para provar)

- **H1 (principal, agora com evidência E4)**: efeito de **SHARD/órfãos** — durante `stock_provision` o host entra em quiesce/provision_lock e Virtus pode parar; depois, o “resume” depende do `nurseTick` do shard certo. Como o `nurseTick` deste worker registra `nurse_skip_other_shard_active` para os perfis do conjunto pausável, eles não são reativados por esse processo e podem ficar OFF se o shard dono não estiver operando.
- **H2**: `stock_provision` segue com `pause_timeout_ignored_best_effort`; isso não deveria impedir o resume, mas pode aumentar a chance de estados inconsistentes, exigindo “heal” do nurse (que falha por shard).
- **H3**: algum gate adicional (ex.: provision_lock preso / TTL / erro de release) pode agravar; precisa de `provision_lock.json` e `service_stdout` completos (bundle atual veio vazio nesses itens).

---

## Evidência a coletar via CT (próximo passo)

No host RM3, numa janela que cubra:
- `stock_provision` cmdId `13ec45e0-...` (quiesce) até o snapshot `receivedAt=1770438472213`.

Logs/keys candidatos:
- `provision_audit`
- `provision_lock`
- `desired`
- `status`
- `logger`
- `service_stdout` / `service_stderr`

Objetivo: achar eventos do tipo **pause/resume**, **stopVirtus/start_work**, e qualquer “why not working” emitido pela nurse/automationAllowed.

---

## Precisa reiniciar?

- **Agora**: não (investigação forense primeiro)
- **Para validar futuro patch**: a definir (quando houver mudança no `conveniente`)

---

## Rollout (fix aplicado)

- **Patch**: `conveniente/scripts/worker.js` — auto-resume pós `stock_provision` por shard (marker + sweep)
- **Commit (GitHub main)**: `127b7f6`
- **Self-update via CT**:
  - RM3 (`hostId=5d7c3309-...`): `self_update` cmdId `516bb8af-26e1-4be3-b3f9-a5cb0a8e812b` (ACK ok no `commands.log`)
  - RM2 (`hostId=bcf01e8d-...`): `self_update` cmdId `5403f96c-1537-44fe-ae79-9e58bf1fbfb8` (em queue — aguardando host ficar disponível para receber)

Validação planejada (CT):
- Após restart do host, coletar `fetch_logs(keys=provision_audit,git_head,git_main_ref,status,desired)` e confirmar eventos `stock_provision_*resume*` e HEAD atualizado.

---

## Validação (RM3) — concluída (evidência)

- **RM3 self_update OK** e **restart confirmado pelo humano** (“reiniciado”).
- **Bundle pós-restart** (CT fetch_logs):
  - `requestId`: `validate_inc_20260207_1403_rm3_1770441361979`
  - `git_main_ref`: `649e5ba...` (prova de disco atualizado)
  - `provision_audit` contém eventos:
    - `stock_provision_lock_end_detected`
    - `stock_provision_post_resume_tick` (por shard)
- **Snapshot CT atual** (estabilidade):
  - RM3: `active=53`, `working=52`, `virtusOnline=52`, `loginRequired=1` (dentro do esperado)
  - `provisionLock` não ativo no snapshot

Observação operacional:
- `stock_provision` pode ocorrer durante “Abrir todos”. Evidência atual indica que o sistema se auto-corrigiu e terminou em estado saudável (53/53/52).

---

## Nova ocorrência (RM3) — “cai e se corrige” (evidência CT)

Relato do humano: após horas cadastrando contas, RM3 volta a ficar “problemático” (ex.: `67/67/25`), enquanto outros hosts estão saudáveis.

Evidências coletadas (CT):

- **Snapshot CT (RM3)**: `C:\\sitechatbot\\dados\\5d7c3309-8581-4a50-a421-e6cbb52d8070-30b3fe928b.json`
  - No momento da coleta forense posterior, RM3 já havia **auto-corrigido** para `67/67/50`.
  - `loginRequired=18` (alto), `configurando=1`, `provisionLock.active=false` no snapshot.

- **fetch_logs comparativo RM3 vs RM2**:
  - RM3: `requestId=inc_rm_compare_rm3_1770476140001` (cmdId `81822e33-6dae-4667-90a3-78e5ad21e803`)
  - RM2: `requestId=inc_rm_compare_rm2_1770476140001` (cmdId `0338a79e-b307-46f7-b7d1-aca526008970`)
  - Achado chave: RM3 possui **muitos perfis com `loginReason=probe_failed`**; no estado analisado, os perfis “não trabalhando” estavam **100% explicados** por gates (`loginRequired/configurando/humanHold`), sem “suspicious”.

- **fetch_logs leve (RM3) focado em loginRequired/provision_audit**:
  - RM3: `requestId=inc_rm3_probe_diag_1770476365469` (cmdId `2f1fe32a-875c-4c02-bdec-f992f29f6cd2`)
  - `login_required_events`: confirma ocorrências recentes de **`reason=probe_failed`** (via scan de tabs).
  - `provision_audit`: janela recente dominada por `nurse_skip_other_shard_active` e presença de eventos `auto_lr_tick_blocked` com motivo `provision_lock` (indica períodos com lock ativo bloqueando autopilot LR).

Outros achados:
- **Desired “stale/ghost” no RM3**: `desired.perfis=117` vs `status/perfis=67` (50 perfis não existem no status; 18 com `active=true` no desired).

Status: **need_evidence** para capturar o estado exato “67/67/25” em CT no instante em que ocorre (flap/auto-correção rápida).

---

## P0 (RM3): `loginRequired=probe_failed` “flag presa” apesar de abas saudáveis (evidência + fix)

Relato do humano (confirmado visualmente no browser):
- Perfis **saudáveis** (Messenger/Marketplace OK) aparecem no painel como:
  - `Navegador Ativo`, `Virtus Offline`, `Login requerido`, `loginReason=probe_failed`

Evidência CT (RM3):
- Snapshot: `C:\\sitechatbot\\dados\\5d7c3309-8581-4a50-a421-e6cbb52d8070-30b3fe928b.json`
  - `cuiaba-1759347389687` (label “Javier Leo Fuentez”): `loginRequired=true`, `loginReason=probe_failed`, `virtusOnline=false`
  - `juiz_de_fora-1769026433175` (label “Gael Junqueira”): `loginRequired=true`, `loginReason=probe_failed`, `virtusOnline=false`
- Bundle `login_required_events` (RM3): `requestId=inc_rm3_probe_diag_1770476365469`
  - Para ambos perfis há **muitos** `lr_scan_tabs` com `lr=false` em URLs do Messenger, intercalado com raros `lr=true reason=probe_failed` (domínio null), indicando “probe flakey”.

Root cause (código):
- `worker.js` faz scan e **seta** loginRequired quando `lr.loginRequired=true`, mas **não tinha** caminho automático para **limpar** `loginRequired=probe_failed` quando scans subsequentes provam `lr=false` (fica “engessado”).

Fix aplicado (conservador, com guardrails):
- `C:\\conveniente\\scripts\\worker.js`
  - Auto-clear apenas quando:
    - flag persistida é `loginRequired=true` com `loginReason=probe_failed`
    - scan contém páginas válidas (`domain` messenger/facebook) e **todas** com `lr=false`
    - streak >= 3 scans “limpos” dentro de 2min
    - debounce 15min por perfil
  - Telemetria:
    - `provision_audit`: `lr_auto_clear_probe_failed`
    - `login_required_events.jsonl`: `lr_auto_clear`

Release:
- Commit: `7e7237d`
- `self_update` RM3: `cmdId=c6a24007-5247-42b2-9f07-13ca556fadc5` (`requestId=self_update_rm3_lr_autoclear_1770477036602`) — OK

