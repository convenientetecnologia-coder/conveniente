# INC-20260219-0920-01 — Sorteio 3 minutos (fila por menor consumo + desempate por chegada)

- **P**: P0
- **Sistema**: `sitechatbot` + `notificador`
- **State**: `done`
- **Rollout**: `pilot_ready`
- **Validation**: `pass`

---

## Regra de negócio (humano)

- primeira solicitação inicia janela de 3 minutos;
- todos que entrarem na janela participam;
- vencedor = motorista com menor quantidade de leads no ciclo;
- empate = quem entrou primeiro na fila;
- somente 1 vencedor recebe contato completo do cliente.

---

## Decisões pendentes

- definição do "ciclo de contagem" (zera segunda? zera após pagamento? ambos?);
- timezone oficial para corte de ciclo;
- política para mensagem duplicada do mesmo motorista;
- política para timeout sem nenhum participante.

Observação de consistência (canônica):
- a regra de cobrança/bloqueio atual é diária útil (08:00/15:00) e pode influenciar o reset do ciclo (ex.: reset por “período” vs por “cobrança paga”).

---

## Evidência obrigatória antes de codar

- logs reais de entrada no fluxo (timestamp por motorista);
- desenho da estrutura de fila (`leadToken`, `driverId`, `joinedAt`);
- prova de determinismo do empate (ordenador único auditável);
- plano de auditoria para losers e vencedor (messageId/traceId).

---

## Critério de sucesso

- para uma mesma janela, todos os nós chegam ao mesmo vencedor;
- não existe mais de um vencedor por `leadToken`;
- losers recebem retorno consistente de encerramento.

---

## Evidência (fase atual)

- Simulação pesada atômica (forense isolado): `PASS`
  - `C:\sitechatbot\dados\forensics\stress_lottery_report_1771624251610.json` (`failures=0`)
- Testes reais controlados:
  - empate por chegada validado;
  - bloqueado por boleto não participa;
  - sem cadastro não participa;
  - concorrência 4x simultânea sem repetição de vencedor no mesmo lote (quando há alternativas).

