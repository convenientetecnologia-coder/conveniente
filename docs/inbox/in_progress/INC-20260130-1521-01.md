### INC-20260130-1521-01 — P0: CT/Servidores mostra “Desconhecido” e precisa virar fonte de verdade operacional (flags + estados acionáveis)

- **P**: P0
- **Data**: 2026-01-30
- **Sistema**: `sitechatbot` (CT + UI Servidores) + `conveniente` (origem dos flags/telemetria por perfil)

---

## Status (rígido)

- **Estado atual**: `in_progress`
- **Rollout**: `not_deployed`
- **Validation**: `not_run`

---

## Sintoma (humano)

- No CT → menu **Servidores**, aparece estado **“Desconhecido”**.
- O usuário usa o menu **Servidores** como fonte de verdade para decidir qual host ir (ex.: “ROBE MÃE 2 tem 5 humano invocado”, “ROBE MÃE 3 tem 1 captcha”).
- Hoje os estados/contadores não estão 110% acionáveis/confiáveis.

---

## Como deveria ser (humano)

- O CT deve mostrar **estados/flags claros e acionáveis**, sem “desconhecido” genérico.
- O operador deve conseguir bater o olho e decidir: **qual host**, **qual problema**, **o que fazer**.

## Critério de sucesso (objetivo)

- O card do host no CT **nunca** exibe a palavra “Desconhecido” como categoria final.
- Para todo “login requerido”, o CT exibe:
  - **categoria humana** (ex.: Captcha / Login / Sessão / 2FA / Identidade / Consent / Outros),
  - e quando cair em “Outros”, exibe **o motivo técnico** (ex.: `probe_failed`, `messenger_page_not_available`) de forma auditável (tooltip/modal), sem exigir “investigação manual” do operador.
- O card do host exibe também flags operacionais (não exclusivas) como:
  - **Humano invocado** (ex.: `perfil.humanControl=true`)
  - **Limite excedido** (robe paused_limit / limit_posting)
  - (demais flags a definir conforme catálogo real do payload).

---

## Evidências (sem achismo)

### A) “Desconhecido” existe por design hoje

- UI do CT adiciona pill “Desconhecido” com base em `accountsAgg.unknown`:
  - Arquivo: `C:\sitechatbot\public\index.html`
  - Trecho: `addPill('Desconhecido', n('unknown'), ...)`

### B) O classificador central gera `kind:'unknown'` quando `loginRequired=true` e não reconhece o motivo

- Arquivo: `C:\sitechatbot\convenientetecnologia\lib\fbAccountState.js`
  - Quando `perfil.loginRequired === true` e `loginReason` não bate em nenhuma regra → retorna:
    - `kind: 'unknown'`
    - `title: 'Login requerido (motivo desconhecido)'`

### C) Motivos reais existem nos snapshots (exemplos)

- Snapshot RM3 (exemplo real em disco):
  - Arquivo: `C:\sitechatbot\dados\5d7c3309-8581-4a50-a421-e6cbb52d8070-30b3fe928b.json`
  - Campos por perfil: `loginRequired: true`, `loginReason: "probe_failed"`

---

## Hipóteses (GPT)

- H1: O “Desconhecido” não é bug de UI; é **schema/contrato incompleto** (faltam mapeamentos de `loginReason` → categoria humana).
- H2: Falta um catálogo explícito de **flags operacionais** (ex.: humano invocado) separado de “estado final” (mutuamente exclusivo), então o operador perde a visão acionável.

---

## Plano (mudança mínima, cirúrgica)

### 1) Catalogar e padronizar estados (CT)

- Renomear `unknown` → `login_other` (label humano: **“Outros (login)”**).
- Expor breakdown de `loginReason` quando cair em “Outros” (ex.: top 5 reasons + contagem) no payload do host (CT `/servers`), para a UI exibir em tooltip/modal.

### 2) Adicionar agregados de flags (não exclusivos) no CT `/servers`

- Ex.: `human_invoked` = perfis com `humanControl=true` (e/ou `humanHold=true`, se aplicável).
- Ex.: `messenger_pin` = perfis com `messengerPin=true`.
- (Outras flags somente após validar no snapshot/código do `conveniente`.)

### 3) UI Servidores (CT) — remover “Desconhecido” e exibir “Outros (login)” + flags

- Atualizar `C:\sitechatbot\public\index.html`:
  - trocar pill “Desconhecido” por “Outros (login)”
  - exibir “Humano invocado” como pill separada (flagsAgg).

---

## Rollout / Restart

- **Precisa reiniciar agora?** não (mudança ainda não aplicada)
- **Precisa reiniciar para validar?** sim
- **Qual projeto?** `sitechatbot` (CT)
- **Como reiniciar (humano)?** `node index.js`

---

## Rollback

- `git revert <commit>` no repo `sitechatbot` e reiniciar `node index.js` no host do CT.

