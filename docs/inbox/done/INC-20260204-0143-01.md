### INC-20260204-0143-01 - P1 - CT Aprovados: latência realtime (badge/som) após "Enviar p/ financeiro"

- **State**: `done`
- **Rollout**: `deployed`
- **Validation**: `passed`

#### Relato (humano)
- Após aprovar/enviar pro financeiro, às vezes demorava ~6s para badge/som e lista refletirem.
- Com 2 navegadores abertos, um atualizava antes do outro.

#### Diagnóstico com evidência (runtime)
- Instrumentação ponta-a-ponta (`runId=ct_approved_rt_v1`) registrou:
  - ACK da ação no frontend (ex.: `approve_test`): `dtMs=141` (linha `79075` no `debug.log`)
  - Recebimento de `ct_changed` via SSE (ex.: área `tests`): `dtFromActMs=138` (linha `79073`)
  - Atualização de core (badge/unseen) refletindo Aprovados: `dtFromActMs≈1946ms` com `navApproved=1` e `unseenApproved=1` (linha `79085`)
- Resultado humano: latência observada ~1–2s e considerada **ótima**.

#### Decisão
- Sem mudança adicional: comportamento atual atende (enterprise/tempo real “o suficiente”).
- Mantemos o fallback/poll como segurança para SSE instável.

#### Arquivos (instrumentação temporária)
- `C:\sitechatbot\convenientetecnologia\public\ct.js` (logs de ação/SSE/refresh)
- `C:\sitechatbot\convenientetecnologia\lib\ctRealtime.js` (logs server throttle/emit)

