### INC-20260204-0130-01 - P1 - CT UI: layout quebra com zoom do navegador

- **State**: `need_evidence`
- **Rollout**: `not_deployed`
- **Validation**: `not_run`

#### Relato
- Em diferentes zooms do Chrome (75%, 80%, 90%, 100%, 110%), a tela do CT fica "toda errada": itens somem, ficam sobrepostos (menu esquerdo, conteÃºdo central, chat).
- Objetivo: CT ficar utilizÃ¡vel e consistente em qualquer zoom razoÃ¡vel.

#### HipÃ³teses (a testar com evidÃªncia runtime)
- H1: CSS com larguras/alturas fixas (px/vh) + overflow hidden causa corte/sumiÃ§o em zoom maior.
- H2: elementos com position absolute/stacking (z-index) colidem sob zoom e viewport menor.
- H3: layout depende de cÃ¡lculo JS de alturas (ex.: chat) e falha em resize/zoom (nÃ£o recalcula corretamente).
- H4: flex/grid com min-width/min-height inadequado faz colapsar/overlap quando espaÃ§o Ãºtil muda.
- H5: algum container usa transform/scale ou zoom CSS e cria coordenadas inconsistentes (boundingClientRect).

#### EvidÃªncia (a coletar)
- `debug.log` runId `ct_zoom_layout_v1`: viewport (innerWidth/innerHeight), devicePixelRatio, visualViewport.scale (se existir), e bounding boxes de componentes-chave + flags de overlap/corte.

