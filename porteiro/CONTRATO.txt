CONTRATO BLINDADO — Porteiro no boot do index
============================================
Data: 2026-08-31  (ensure no index)

O dono e o index.js. Voce nao roda comando extra do Porteiro.

1. SERVIDOR NOVO (MAE 8, 9, ...)
   Passo A:
     irm https://raw.githubusercontent.com/convenientetecnologia-coder/conveniente/main/instalar_conveniente.ps1 | iex

   Passo B — fecha o PowerShell, abre de novo, cola ISTO (igual ao de sempre):

     cd \
     if (Test-Path "C:\conveniente") { Remove-Item -Path "C:\conveniente" -Recurse -Force }
     git clone https://github.com/convenientetecnologia-coder/conveniente.git "C:\conveniente"
     cd C:\conveniente
     npm install

   Passo C: inicia o Conveniente (atalho / node index.js).
   O index:
     - cria C:\auto_vigia se nao existir
     - copia o Porteiro v5.2.0-nomem (sem limpador de RAM)
     - sobe o loop (reboot 04:00, lixeira, AUTO_BOOT)
     - se as tarefas ao ligar o Windows ainda nao existirem, o Windows
       pede admin UMA vez (UAC). Aceita. Depois nunca mais.
   RAM/StandbyList: Conveniente (index), nao o Porteiro.

2. MAE 1-7 (ja tem Porteiro velho)
   git pull (ou o passo B) em C:\conveniente.
   REINICIA o index (processo velho nao le arquivo novo).
   O index ve o .ps1 velho (mem_soft), copia o novo, recicla SO o loop.
   Nao mata Node. Nao pede UAC (tarefas ja existem).
   STATUS: Ver=v5.2.0-nomem  MemClean=OFF
   Log: ACTION=...mem_off   (nunca mem_soft)

3. O QUE O PORTEIRO FAZ / NAO FAZ
   Faz: reboot 04:00-04:20 + TEMP + Lixeira, disco, AUTO_BOOT, NetGuard,
        PARAR / INICIAR / STATUS.
   Nao faz: DiskClean / StandbyList. Isso e o Conveniente.

4. KILL SWITCH
   PORTEIRO_SYNC_DISABLED=1 no ambiente do Node: o index nao mexe no Porteiro.

5. PROVA
   powershell -NoProfile -ExecutionPolicy Bypass -File C:\auto_vigia\manutencao.ps1 -Action status
   Tem que imprimir Ver=v5.2.0-nomem e MemClean=OFF.

   Select-String -Path C:\auto_vigia\logs\porteiro.log -Pattern "mem_soft|mem_off|v5.2.0-nomem" | Select-Object -Last 8
   mem_off + v5.2.0-nomem = ok. mem_soft = loop velho ainda em memoria.
