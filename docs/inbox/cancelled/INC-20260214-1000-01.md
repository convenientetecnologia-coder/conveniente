### INC-20260214-1000-01 — Observabilidade forense: provar “quem/como/quando” (ledger + audit + commands)

- **P**: P1
- **Sistema**: `sitechatbot` (CT) + `conveniente` (hosts)
- **Status**: `cancelled`
- **Rollout**: `not_deployed`
- **Validation**: `not_run`

---

## Encerramento (fase 2)

Escopo reclassificado para fase 2.
Não bloqueia o GO do programa único de cadastro sem duplicação (`INC-20260214-0900-01`).

## Contexto

Para nunca mais virar “achismo”, precisamos que qualquer incidente responda:

- quem disparou (caller/operator)
- o que mudou (before/after)
- quando (timestamp)
- por qual caminho (endpoint/commandId/jobId)

---

## Escopo

- Padronizar eventos mínimos e correlação:
  - `hostId`, `cmdId`, `jobId`, `requestId`, `profileName`, `stockAccountId`
- Ledger/telemetria:
  - `perfis_ledger.jsonl`
  - `provision_audit.jsonl`
  - `commands.log` + ACK raw
  - `snapshots.log`
- Gaps: onde hoje falta correlação e como fechar

---

## Critério de “done”

- Existe trilha completa para provision e delete (CT e host) com correlação por IDs
- Eventos de risco (`ack_job_not_found`, `queue_corrupt`, `perfis_write_blocked_unreadable`, `delete_pending`) viram “alarme” auditável

