### INC-20260129-2340-01 — Abrir Todos (modo seguro): fica preso em 26/28, sem progresso visível, e não libera Robe/Virtus

- **P**: P1 (pode virar P0 se bloquear produção por longo tempo)
- **Data**: 2026-01-29
- **Sistema**: `conveniente`
- **Host alvo (evidência)**: ROBE MÃE 4 — hostId `825a4485-1465-4c11-aa18-52f0597b23a3`
- **Relacionados**:
  - `done/INC-20260129-2058-03.md` (Abrir Todos 24h / coordenação)
  - `done/INC-20260130-0128-01.md` (open_all não iniciava quando controllers=0)
  - `done/INC-20260129-2058-04.md` (Governança de concorrência)

---

## Status (rígido)

- **Estado atual**: `done` (prescrição técnica entregue; rollout/validação são campos separados)
- **Rollout**: `needs_restart`
- **Validation**: `not_run`

---

## Sintomas (humano)

- RM4 tem 28 perfis; ao clicar “Abrir Todos”, abre só 26 navegadores.
- Fica “parado” em 26, com ~<3GB livre de RAM.
- UI não mostra progresso/andamento (ex.: “Abrindo… 0/28… 1/28…”), então dá sensação de travou.
- Enquanto está assim, nenhum perfil fica “trabalhando” (Robe/Virtus não liberam).

## Como deveria ser (humano)

- O “Abrir Todos” deve mostrar progresso (X/28) de forma confiável.
- Se bater limite real (ex.: RAM insuficiente) e não for possível abrir todos:
  - o sistema deve **finalizar como sucesso parcial** (ex.: abriu 26/28),
  - **liberar Robe/Virtus** para trabalhar nos que abriram,
  - registrar evidência/erro “não abriu tudo por limite” (sem travar).

---

## Hipóteses (GPT) — a confirmar com logs

- H1: O supervisor está negando novas aberturas por **RAM mínima** / política de headroom; nurse continua tentando (ou entra em loop) e não finaliza `_openAll`.
- H2: Dois perfis estão bloqueados por **kill_guard / cooldown / whyNotOpen / ban/2FA** e o open_all não está contabilizando corretamente como “impossível agora”.
- H3: O `_openAll` não está atualizando status/progresso (telemetria), então UI não tem como mostrar “X/Y”.

---

## Evidência coletada (RM4)

CT `fetch_logs` (RM4) — requestId `0ea405ba-e605-40c5-9b21-e4331b95ae2b`:

- `status.ts=1769740934513`
- `controllersCount=26` (shardSize=28)
- `sys.freeMB≈2777`
- `desired._openAll.active=true` com `lockOwner=open_all_map:1769740340810` (UI `by=ui_open_all`)
- Perfis faltantes (não ativos) no snapshot:
  - `recife-1769723410217` (`loginRequired=true`, `activationHeldUntil` ativo)
  - `porto_velho-1769724843855` (`loginRequired=true`, `activationHeldUntil` ativo)

Evidência de travamento do servidor durante open_all (causa do “ninguém trabalhando”):
- `provision_audit`: eventos repetidos `provision_lock_virtus_pause_tick` com `kind=open_all_map` e `controllers=26`
  - isto pausa Virtus globalmente enquanto o open_all está ativo.

Conclusão técnica:
- O nurse tenta abrir os 2 perfis restantes, mas recebe negações relacionadas a recurso (RAM/supervisor) e aplica `activationHeldUntil` curto.
- Como `pending>0`, o open_all mantém o lock vivo (`keepalive`) indefinidamente.
- Resultado: Virtus fica pausado “para sempre” e o servidor não volta a trabalhar (mesmo com 26 browsers abertos).

---

## Plano — EXECUTADO (mudança mínima, ultra-safe)

- **Progresso no painel**: botão “Abrir Todos 24h” agora exibe `Abrindo (modo seguro)... X/Y` usando `openAll.queue` + `status.perfis[*].active`.
- **Finalização parcial (anti-trava)**: se o open_all ficar “travado” por tempo suficiente com todos os pendentes negados por RAM/supervisor:
  - finaliza `_openAll` com `lastError=partial_ram`,
  - desmarca `desired.active=false` para os perfis impossíveis naquele momento (evita loop infinito),
  - libera Virtus para os perfis ativos (`virtus=on` se não estiver `humanHold`),
  - registra `open_all_finalize_partial` em `provision_audit`.

Arquivos:
- `C:\conveniente\scripts\worker.js`
- `C:\conveniente\public\index.html`

