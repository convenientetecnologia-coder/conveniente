### INC-20260130-1544-01 — P0 URGENTE: RM3 “trabalhando 0” no CT (provar motivo com evidência)

- **P**: P0
- **Data**: 2026-01-30
- **Sistema**: `conveniente` (host) + `sitechatbot` (CT)
- **Host alvo**: ROBE MÃE 3 — `hostId=5d7c3309-8581-4a50-a421-e6cbb52d8070` — `hostname=DESKTOP-IR7JRIM`

---

## Status (rígido)

- **Estado atual**: `done`
- **Rollout**: `deployed`
- **Validation**: `passed`

---

## Sintoma (humano)

- “ROBE MÃE 3 (RM3) está oscilando no CT: vi `trabalhando 0`, depois `4`, depois `6`.”
- “Dificuldade no **invocar humano**: cliquei e **não abre o painel/HUD** esperado.”
- “**Retomar trabalho** não está retomando (às vezes só funciona depois de um tempo); o host fica ‘travado/estranho’.”
- “Tem contas que parecem OK (messenger+facebook), mas o sistema marca **login requerido** e **virtus offline**.”

---

## Perguntas obrigatórias (alinhamento rápido)

- **Como deveria ser?** (ex.: “se tem N perfis ativos, deveria ter pelo menos 1 trabalhando”)
- **Critério de sucesso**: (ex.: “entender e provar a causa em logs + instrução acionável do que fazer no host”)

---

## Evidência a coletar (sem pedir investigação humana)

### 1) Snapshot atual do RM3 (CT em disco)

- Arquivo: `C:\sitechatbot\dados\5d7c3309-8581-4a50-a421-e6cbb52d8070-30b3fe928b.json`
- Extrair:
  - contagem de perfis `active=true`
  - contagem de perfis `trabalhando=true`
  - flags que bloqueiam trabalho (ex.: `loginRequired`, `humanControl`, `virtusOnline`, `sendLockActive`, etc.)

### 2) Logs do host via CT (comandos remotos)

- `logs_manifest` (inventário rápido, sem adivinhar paths)
- `fetch_logs_query` com keys:
  - `status`, `desired`, `issues_fallback`, `logger`, `provision_audit`, `commands`, `service_stdout`, `service_stderr`
- patterns:
  - `autoMode`, `light`, `slow`, `govern`, `paused`, `trabalhando`, `robe`, `virtus`, `loginRequired`, `probe_failed`, `humanControl`, `humanHold`

### 2.1) Evidência já coletada (CT → host RM3)

- **fetch_logs** (status/desired/issues_fallback/provision_audit):
  - cmdId `d43463ad-7d59-484e-bed5-7addf220adfe` → requestId `rm3_hud_20260130_161238` → arquivo CT:
    - `C:\sitechatbot\dados\logs\5d7c3309-8581-4a50-a421-e6cbb52d8070\rm3_hud_20260130_161238.json`
  - cmdId `aea93de3-d373-4e17-8127-27e96eeb1426` → requestId `rm3_desired_20260130_161417` → arquivo CT:
    - `C:\sitechatbot\dados\logs\5d7c3309-8581-4a50-a421-e6cbb52d8070\rm3_desired_20260130_161417.json`
- **fetch_logs_query**:
  - cmdId `1cf1fd4e-c7f8-43e9-8f7a-70983abfdb78` → requestId `rm3_pa_q_20260130_161656` (query_provision_audit) → arquivo CT:
    - `C:\sitechatbot\dados\logs\5d7c3309-8581-4a50-a421-e6cbb52d8070\rm3_pa_q_20260130_161656.json`
  - cmdId `6001a274-4b75-4cf3-b44e-7c03d803570c` → requestId `rm3_issues_q_20260130_161659` (query_issues_fallback) → arquivo CT:
    - `C:\sitechatbot\dados\logs\5d7c3309-8581-4a50-a421-e6cbb52d8070\rm3_issues_q_20260130_161659.json`

### 2.2) Observações já provadas (sem achismo)

- **`probe_failed` existe e é usado como fail-safe**:
  - `conveniente/scripts/worker.js`: vários `detectLoginRequired(...).catch(()=>({ loginRequired:true, reason:'probe_failed' }))`
  - `conveniente/scripts/browser.js`: comentário de fail-safe e retorno `{ loginRequired: true, reason: 'probe_failed' }`
  - Implicação: conta “parece ok” visualmente, mas se o probe falha (aba fechando/contexto destruído), o sistema pode marcar `loginRequired=true` por segurança e manter Virtus OFF.
- **desired.json do RM3 (coleta atual) contém `invokeHuman:false`/`humanHold:false` em vários perfis** (amostra no arquivo `rm3_desired_20260130_161417.json`):
  - ainda precisamos correlacionar com **horário exato** do clique do humano para confirmar se o click escreveu em desired e depois “voltou” ou se o click nem chegou a executar.

### 2.3) Evidência NOVA (2026-01-30)

- **health_bundle RM3 (snapshot runtime)**:
  - requestId: `rm3_health_20260130_1`
  - arquivo CT: `C:\sitechatbot\dados\logs\5d7c3309-8581-4a50-a421-e6cbb52d8070\rm3_health_20260130_1.json`
  - achados:
    - `activeCount=36` e `busyCount=0` (não há processos marcados como “busy” no momento).
    - RAM livre alta (`freeMB=41200`) → não indica pressão de memória agora.
    - `logger.log` **não existe** (LOG_TO_FILE provavelmente desativado).
    - `service_stdout.log`/`service_stderr.log` **não existem** (serviço não está registrando stdout/stderr).
    - `provision_lock.json` **não existe** (sem lock global ativo).
- **CT snapshot (último /report)**:
  - arquivo: `C:\sitechatbot\dados\5d7c3309-8581-4a50-a421-e6cbb52d8070-30b3fe928b.json`
  - contagens (calculadas via script local):
    - `perfis=36`, `active=36`, `trabalhando=33`
    - `loginRequired=1` (motivo top: `probe_failed`)
    - `virtusOffline=3`
    - `problem=2`
- **Query status (CT → host)**:
  - requestId: `rm3_q_status_20260130_1`
  - arquivo CT: `C:\sitechatbot\dados\logs\5d7c3309-8581-4a50-a421-e6cbb52d8070\rm3_q_status_20260130_1.json`
  - encontrado: ocorrências de `virtusOnline=false` e `loginRequired=true` com `loginReason=probe_failed`.
- **Query issues_fallback (CT → host)**:
  - requestId: `rm3_q_issues_20260130_1`
  - arquivo CT: `C:\sitechatbot\dados\logs\5d7c3309-8581-4a50-a421-e6cbb52d8070\rm3_q_issues_20260130_1.json`
  - encontrado (histórico): eventos de `robeTickGlobal_skip_due_provision_lock` e `robeTickGlobal_skip_due_slowmode` (ex.: `mode=light reason=loop_lag`), além de `virtus_page_dead`.

### 2.4) Evidência NOVA (2026-01-30 — Retomar/Retry)

- **provision_audit (RM3)**:
  - requestId: `rm3_pa_full_20260130_2`
  - arquivo CT: `C:\sitechatbot\dados\logs\5d7c3309-8581-4a50-a421-e6cbb52d8070\rm3_pa_full_20260130_2.json`
  - achados:
    - `human_resume_login_form` → login_remediate iniciado após Retomar trabalho.
    - `login_remediate_governor_denied` quando outro login_remediate estava em andamento.
    - `missing_credentials_in_manifest` + `ct_credentials_fetch ok=false` → `fail_fast_invoke_human`.

### 2.5) Fix aplicado (2026-01-30)

- **Mudança**: quando `login_remediate` é negado por governor (busy) após **Retomar trabalho**, o sistema enfileira retry imediato.
- **Commit**: `9c5f315` (`fix: retry login_remediate on governor busy`)
- **Self_update**: requestId `deploy_conveniente_20260130_governor_retry` (cmdId `c49c6485-d75e-4b02-b415-d97d347253ea`)

### 3) Diagnóstico: “trabalhando 0” não é “travado”

Regra: “trabalhando” pode estar 0 por razões legítimas (ex.: todos em login_required/humano invocado/limit) ou por falha (crash/loop/worker travado).

---

## Hipóteses (GPT) — sem assumir

- H1: `loginRequired`/`human_invoked`/`limit_exceeded` está impedindo trabalho (não é travamento).
- H2: `virtusOnline=false` em todos os perfis (fluxo principal não inicia).
- H3: governor em modo leve/pausa global (robeTickGlobal pausado), reduzindo trabalho.
- H4: erro em worker/loop (crash parcial) — evidência via `service_stderr`/`logger`.

---

## Rollout / Restart

- **Precisa reiniciar agora?** não (até a evidência apontar necessidade)
- **Precisa reiniciar para validar?** possivelmente sim
- **Qual projeto?** `conveniente` (host RM3)
- **Como reiniciar (humano)?** `node index.js`

---

## Rollback

- Se houver mudança no `conveniente`: `git revert <commit>` + reiniciar `node index.js` no RM3.

---

## Fechamento (2026-01-31)

- **Resultado**: humano confirmou em produção que “Retomar trabalho” voltou a funcionar de forma confiável e que o problema “só 1 conta faz o fluxo” foi resolvido.
- **Causa raiz (provada)**: o retry existia (governor busy) mas o `autoLoginRemediateTick` pulava o perfil por `configurando=true`, então a fila não avançava.
- **Fix (código)**:
  - Guardrail de fila: `queueAutoLoginRemediate(...force=true)` persiste `st.force`.
  - `autoLoginRemediateTick` pode **override** de `configurando` **somente** quando `queued && force && nextAt<=now` e `provisionLock` não está ativo (mantendo `humanControl` como “hard stop”).
- **Evidência (runtime)**:
  - RM3 `provision_audit`: eventos `login_remediate_governor_denied` → `login_remediate_governor_retry_queued` → `auto_lr_tick_override_configurando` → `auto_login_remediate_begin` para o segundo perfil.
  - Coleta no CT: `C:\sitechatbot\dados\logs\5d7c3309-8581-4a50-a421-e6cbb52d8070\rm3_pa_tail_verify_20260131_01.json` (tail `provision_audit`).
- **Commits**:
  - `d1d84f8` (fix fila atômica/serial para retry `governor_busy`).

