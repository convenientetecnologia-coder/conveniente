# INC-20260203-1800-01 — RM3: `loginRequired=probe_failed` com navegador aparentemente OK (Virtus derrubado por falso positivo)

- **P**: P0
- **Sistema**: `conveniente` + `sitechatbot`
- **Host alvo**: ROBE MÃE 3 (`5d7c3309-8581-4a50-a421-e6cbb52d8070`)
- **State**: `done`
- **Rollout**: `deployed_rm3_controlled`
- **Validation**: `passed_operational`

---

## Encerramento aprovado pelo humano

Status encerrado por confirmação operacional:

- operação estabilizada em produção RM3;
- convergência consistente entre `Virtus Online/Offline` e flags reais;
- melhoria relevante do cenário de falso positivo de `loginRequired`;
- aceite explícito do humano para fechar o INC nesta rodada.

---

## Resultado consolidado

- hardening aplicado para reduzir contaminação de domínio entre Messenger (Virtus) e Create Item (Robe);
- correções de fluxo e sinalização de `loginRequired` já implantadas no RM3;
- regressão de marcação em massa corrigida com hotfix dedicado e atualização aplicada;
- comportamento atual considerado a melhor versão deste ciclo, com estabilidade operacional alta.

---

## Critério para reabertura

Reabrir somente com nova evidência objetiva em produção, por exemplo:

- conta saudável em Messenger/Create Item com `Virtus Offline` sem bloqueador real;
- `loginRequired` persistente inconsistente com estado observado;
- oscilação de estado que cause prejuízo operacional confirmado.

Se ocorrer, abrir nova rodada com referência a este INC.

