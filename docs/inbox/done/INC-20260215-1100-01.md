### INC-20260215-1100-01 — RM1: Robe postar (Marketplace) — aba 1 tela preta ao criar item

- **P**: P0
- **Sistema**: `conveniente` (Robe/Virtus + browser automation)
- **Host alvo**: RM1 (ROBE MAE 1) — `hostId=084c8fff-c508-47bd-a33e-3ab34aeb1e3d`
- **Perfil de referencia (teste)**: `maringa-1759198592235` (`[001] Alex Santana`)
- **Status**: `done`
- **Rollout**: `deployed`
- **Validation**: `passed`

---

## Encerramento

Incidente encerrado com evidencia runtime e validacao humana em multiplos ciclos:

- Robe voltou a postar de forma estavel.
- Fluxo `open_manual`/`Abrir Navegador` nao abre mais aba extra de verificacao (`aba 1`) no bootstrap.
- Fluxo `open_all` permaneceu sem abrir aba extra obsoleta.

---

## RCA (causa raiz confirmada)

1. A tela de create do Marketplace degradava de forma intermitente durante o fluxo de postagem, derrubando disponibilidade de campos (ex.: upload).
2. Em parte dos ciclos, a degradacao acontecia apos fast-lane e antes do upload, faltando retentativa robusta no ramo de upload.
3. Havia tambem comportamento legado de probe de bootstrap abrindo aba extra em `open_manual` (obsoleto para esta etapa).

---

## Correcao aplicada

Commits publicados em `main`:

- `40a6ae1` — guard para requisicao GraphQL critica do composer que precipita colapso visual.
- `08d2410` — pre-pick de foto antes da navegacao para reduzir janela de degradacao.
- `5f17b31` — recovery no estagio de upload quando input desaparece apos trigger.
- `0062a42` — desativa por padrao probe de bootstrap que abria aba extra de create; somente opt-in por `BOOTSTRAP_ROBE_PROBE_ENABLED=1`.
- `91dd50e` — limpeza de instrumentacao temporaria do endpoint de debug.

---

## Evidencia de validacao (canonica)

- `git_main_ref` observado em host durante validacao final: `0062a424818ff6b1bb239c38a4f9248d90a2da57`.
- Eventos de bootstrap mostram skip do probe com `reason=disabled_by_default` para `open_manual` e `reason=open_all` no `open_all`.
- Ciclos de `robe-play` no periodo final sem falhas de upload e sem regressao de tela preta reportada pelo humano.

---

## Reinicios / rollout

- Rollout via `self_update` com restart no RM1 durante as iteracoes de validacao.

---

## Rollback

Se necessario, reverter commits desta cadeia em ordem inversa; padrao atual permanece com probe de bootstrap desabilitado por default.

