### INC-20260305-0900-01 — Conveniente: migração `chrome.exe` → `chromium` (sessão persistente + perfil visível)

- **P**: P0
- **Sistema**: `conveniente`
- **State**: `in_progress` (WIP=1)
- **Rollout**: `not_deployed`
- **Validation**: `not_run`

---

## Contexto (humano)

Queremos trocar o navegador do runtime do `conveniente` de **Google Chrome (`chrome.exe`)** para **Chromium**, mantendo o sistema **idêntico em comportamento**:

- Persistência de sessão/cookies: fechar/abrir navegador e continuar logado.
- `userDataDir` no mesmo padrão (pasta conhecida/organizada; sem “mudar onde fica”).
- Não quebrar UA/FP nem a estrutura atual (nada de refactor agora).
- Melhorar UX: o menu do perfil do navegador deve exibir:
  - **Nome**: `profileName`
  - **Email**: `<profileName>@gmail.com` (mesmo sem login real em conta Google).

Restrições:
- Nesta fase: **sem codar** e **sem executar rollout**. Primeiro: dossiê + evidência + plano cirúrgico + rollback.

Checkup pré-código desta etapa:
- `C:\conveniente\docs\checkups\checkup_2026-03-05_migracao_chromium_pre_codigo.md`
- `C:\conveniente\docs\checkups\checkup_2026-03-05_auditoria_e2e_chromium_sessoes.md`

---

## Objetivo (o que significa “deu certo”)

Critério de sucesso (validável, sem achismo):

1) **Persistência**:
   - Um perfil (N=1) faz login no Facebook/Messenger.
   - Reinicia o navegador desse perfil (fechar → abrir).
   - Volta **logado** sem intervenção humana.
   - Repetir o ciclo 3x sem degradação.

2) **Compatibilidade do `userDataDir`**:
   - O perfil continua usando o diretório esperado (sem “criar perfil novo” sem querer).
   - Não quebra naming/estrutura de pastas que o time já conhece.

3) **Perfil visível (UX)**:
   - No menu de perfil do navegador, aparece o **nome** e o **email** definidos (não precisa ser uma conta Google real).

4) **Sem regressão operacional**:
   - Fluxos existentes (abrir/fechar todos, invocar humano, retomar trabalho, virtus/robe) continuam funcionando.

---

## Não-objetivos (fora do escopo agora)

- Não vamos “otimizar tudo”, nem reestruturar `perfis/desired/status`.
- Não vamos trocar local padrão de dados nem mexer em paths de forma ampla.
- Não vamos tentar “sincronizar conta Google” (não é necessário).

---

## Perguntas de alinhamento (obrigatórias antes de qualquer PR)

- **A1 (escopo de hosts)**: a migração vai ser testada primeiro em qual host? (ex.: “1 servidor de teste”, depois “ROBE MÃE X”).
- **A2 (política de fallback)**: se Chromium falhar, voltamos automaticamente pro Chrome (flag/env) ou rollback via git + restart?
- **A3 (UX do perfil)**: basta aparecer “nome/email” no UI do navegador ou precisa também foto/ícone custom?

> Observação: essas perguntas não exigem o humano coletar nada; são só decisão/produto.

---

## Evidências a coletar (o GPT coleta por código/logs; humano não investiga)

### 1) Evidência por código (onde o browser é lançado hoje)

- Localizar (no repo `conveniente`) os pontos que definem:
  - executável (Chrome/Chromium)
  - `userDataDir`
  - args/flags (ex.: `--user-data-dir`, `--profile-directory`, `--disable-features...`)
  - se usamos Puppeteer/Playwright/Chrome DevTools Protocol (CDP) direto

Saída esperada desta etapa (para colar aqui):
- arquivo(s) + função(ões) exatas onde `executablePath`/launch ocorre
- lista canônica de flags atuais

#### Evidência já coletada (2026-03-05)

- **Launcher/automação**: `C:\conveniente\scripts\browser.js`
  - Usa **Puppeteer** (`puppeteer-extra` + stealth).
  - Abre browser em `openBrowser(...)` usando:
    - `userDataDir` (por perfil) e `args` (flags fixas) e `defaultViewport=null`.
    - `executablePath` via `findChromeStable()`.
  - `findChromeStable()` já suporta **Chromium** por env:
    - prioridade: `CHROME_PATH` (se existir) → depois `CHROMIUM_PATH` → depois paths default.
  - `userDataDir` hoje é “enforced” para ficar sob **Google Chrome User Data**:
    - `ensureUserDataDirUnderChrome(manifest)` define `...\Google\Chrome\User Data\Conveniente\<profileName>`.
  - Evidência (linhas do arquivo):
    - `ensureUserDataDirUnderChrome` + `findChromeStable` + `openBrowser` (launch): ver `C:\conveniente\scripts\browser.js` (trecho por volta de ~671, ~965, ~1017).

- **Backup automático já existente (parcial)**: `C:\conveniente\index.js`
  - `startAutoBackupConveniente()` inicia snapshots em subprocesso (`scripts/autoBackupWorker.js`).
  - Isso é útil para rollback rápido de código/config pequena, mas **não cobre** userDataDir/sessões completas (ver INC de backup).

- **Achados críticos da auditoria pré-código** (detalhados no checkup):
  - `P0`: descoberta de executável Chromium no Windows não é default (depende de env).
  - `P0`: bootstrap/service mode não propaga env de engine/executable (risco de drift).
  - `P0`: persistência atual é acoplada ao root `Google\\Chrome\\User Data` (bom para manter sessão existente na fase 1).
  - `P1`: UX nome/email no menu de perfil não está implementada e precisa prova técnica controlada.

Estado consolidado (auditoria E2E):
- Chromium sem fallback Chrome: **NÃO GARANTIDO AINDA**.
- Reuso de sessões já salvas: **SIM (já automático)**.
- Sessão quente durante operação (cookies frescos -> manifest): **SIM (já automático)**.
- UX nome/email no perfil nativo: **NÃO IMPLEMENTADO**.

### 2) Evidência por logs (em runtime, depois do plano)

Quando for a hora de validar (não agora), coletar via CT:

- `logs_manifest` do host de teste
- `fetch_logs(keys=["logger","issues_fallback","status","desired"])`

E adicionar aqui:
- requestId/cmdId
- trechos relevantes (sem secrets)

---

## Plano (sem executar ainda) — fases

### Fase 0 — Backup (bloqueador)

Dependia do INC `INC-20260305-0900-02` (backup baseline Chrome). **Concluído em 2026-03-05**:
- `C:\sitechatbot\backups\conveniente_full_20260305_140355`
- evidências: `_backup_manifest.json` e `_backup_robocopy.log`

### Fase 1 — Implementação cirúrgica (**aplicada em código**)

- Escolha explícita de engine implementada:
  - `BROWSER_ENGINE=chromium|chrome` (default agora: `chromium`).
- Regra estrita implementada:
  - `BROWSER_ENGINE=chromium` **não faz fallback para Chrome**;
  - exige `CHROMIUM_PATH` válido ou binário Chromium em paths default do SO.
- Preservação de sessão mantida:
  - `userDataDir` continua no mesmo root (`Google\Chrome\User Data\Conveniente\<nome>`) para reaproveitar sessões já salvas.
- Propagação para modo serviço (bootstrap) aplicada:
  - `BROWSER_ENGINE`, `CHROMIUM_PATH` e `CHROME_PATH` passam a ser propagados para task/NSSM.
- Arquivos alterados:
  - `C:\conveniente\scripts\browser.js`
  - `C:\conveniente\scripts\bootstrapService.js`
  - `C:\conveniente\instalar_conveniente.ps1` (Chromium obrigatório + verificação pós-instalação)

Status desta fase:
- código: **concluído**
- rollout runtime: **pendente** (depende de commit/push/self_update + restart no host alvo)
- validação em host: **pendente**

### Fase 2 — Perfil visível (nome/email)

- Definir técnica (depois de ver o código atual):
  - via flag/Preference file no `userDataDir` **ou**
  - via automação inicial (uma vez) para setar identidade local do perfil
- Guardrail: não depender de login Google; não armazenar secrets em prefs.

### Fase 3 — Rollout controlado

- Deploy por git + `self_update`
- Restart humano apenas no host alvo (`node index.js`)
- Monitorar e coletar evidência objetiva

---

## Reinícios necessários

- **Agora (fase de estudo)**: nenhum.
- **Quando aplicar**: `conveniente` no host de teste (humano: `node index.js`).

---

## Rollback

- **Primário**: `git revert` do commit de migração + `self_update` + restart do `conveniente`.
- **Secundário**: usar o backup do INC `INC-20260305-0900-02` para restaurar baseline (se necessário).

