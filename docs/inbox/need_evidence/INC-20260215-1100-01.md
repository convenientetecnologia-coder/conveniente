### INC-20260215-1100-01 — RM1: Robe postar (Marketplace) — tela preta residual e confiabilidade de retentativa

- **P**: P0
- **Sistema**: `conveniente` (Robe/browser automation)
- **Host alvo primário**: RM1 (`hostId=084c8fff-c508-47bd-a33e-3ab34aeb1e3d`)
- **State**: `need_evidence` (reaberto)
- **Rollout**: `deployed_partial`
- **Validation**: `monitoring`

---

## Contexto

Este INC havia sido encerrado após correções importantes, mas foi reaberto por evidência operacional nova:

- ainda há casos de "tela preta" em parte das contas durante postagem;
- o comportamento de retentativa após falha visual não está suficientemente confiável em produção.

Objetivo desta reabertura: fechar o gap residual sem regressão no fluxo já estabilizado.

---

## Histórico de correções já aplicadas

Commits previamente aplicados no ciclo original:

- `40a6ae1`
- `08d2410`
- `5f17b31`
- `0062a42`
- `91dd50e`

Essas mudanças reduziram fortemente o problema, porém não eliminaram 100% dos casos reportados.

---

## Sintoma atual (humano)

- contas ainda podem apresentar tela preta na abertura de `create/item` em alguns ciclos;
- há percepção de que o fluxo "não faz retentativa" em todos os cenários esperados;
- o alvo é convergir para postagem de primeira tentativa com fallback robusto quando necessário.

---

## Evidência necessária (fase atual)

1. Capturar recortes de `robe` com falha residual em produção:
   - `issues_fallback`
   - `logger` (ramo de robe)
   - eventos de recuperação/reload/create-item
2. Confirmar quando a retentativa entra e quando não entra.
3. Correlacionar por perfil/horário para separar:
   - falha de UI transitória recuperável;
   - falha de sessão/autenticação no contexto de Robe;
   - falso positivo por leitura cruzada de domínio.

---

## Critério de aceite

- taxa de tela preta residual tende a zero em janela de monitoramento;
- quando ocorrer falha transitória, o fallback de retentativa é acionado de forma determinística;
- sem regressão no `open_all`, Virtus e fluxos de login já estabilizados.

---

## Próximos passos

1. Rodada de coleta dedicada para casos residuais.
2. Decisão de patch somente com evidência objetiva do ramo não coberto.
3. Rollout controlado (host alvo) + soak.

