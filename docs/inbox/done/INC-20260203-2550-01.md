### INC-20260203-2550-01 - P1 - CT Chat: carregar mensagens antigas ao subir (paginacao)

- **State**: `done`
- **Rollout**: `deployed`
- **Validation**: `passed`

#### Relato
- Ao subir o chat, ele para em ~27/01 e nao carrega mensagens mais antigas. Precisa permitir ver o historico completo (ou muito mais antigo) carregando em paginas ao chegar no topo.

#### Evidencia (a coletar)
- Logs do browser (debug) com cursor/limit + resposta do endpoint.
- Evidencia do backend: query/limit aplicado.

#### Hipoteses iniciais
- H1: Frontend sempre pede apenas ultimas N mensagens (limit fixo) e nao existe cursor para buscar mais antigas.
- H2: Backend limita por default (ex.: LIMIT 200) e nao exp̵e pagina̤̣o antesId/offset.
- H3: Existe pagina̤̣o no backend, mas o scroll handler no frontend nunca chama.
- H4: O frontend perde a referencia do menor id (minId) e para de solicitar mais.

#### Plano (debug)
- Instrumentar: logar parametros de fetch (sinceId/beforeId/limit) + tamanho do retorno + min/max id.
- Implementar scroll-top trigger para carregar pagina anterior com cursor (antesId).

