# INC-20260201-0200-01 — Forense RAM: RM4/RM5/RM6 (capacidade máxima), autoMode light/full e thresholds

- **P**: P0 (decisões erradas de “modo leve” impactam produção e capacidade)
- **Sistema**: `conveniente` (telemetria/autoMode/ram policy) + `sitechatbot` (CT para coleta de logs)
- **State**: `cancelled`
- **Rollout**: `not_deployed`
- **Validation**: `not_run`
- **Precisa reiniciar agora?** não (investigação/telemetria)
- **Precisa reiniciar p/ validar?** não (apenas coleta e análise)

---

## Decisão de encerramento

Cancelado por decisão operacional do humano nesta rodada.  
A análise histórica já foi registrada e não há ação adicional imediata pendente.

---

## RAW_INPUT (humano — 2026-02-01)

```text
triagem inbox

recentemente fizemos uma configuração no sistema conveniente pra registro de memoria ram por minuto modo leve e full e essas coisas

agora chegou a hora de puxar de todos os servidores pra que possamos fazer um estudo minucioso

na verdade nao vamos puxar de todos, vamos puxar do robe mae 4 5 6 , desses 3 que ja ha alguns dias estao com limite maximo de contas

precisamos pegar esse dados, o mais organizado possivel e precismos de uma avaliação extremaemnte ultra perfeita para entender como esses servidores se comportaram com a memoria ram, como comportaram com modo leve e full, como reagiram esses dias

porque eu percebi uma coisa nesses dias, eu percebi por exemplo, robe mae 3 , ele tava com menos de 50% da capacidade total de contas no servidor e mesmo assim tava no modo leve

provavelmente por questoes daquela coisa de ping tictac interno , aquilo pelo que percebi ta falho e nao ta ajudando muito, tipo algo inconsistente ta tomando decisoes muito pesadas no sistema com opor exemplo botar o servidor em modo leve sendo que o serviodr que ele fez isso tava com 30+ gb de ram sobrando

agora sobre memoria ram precisamos saber se o sistema ficou sem memoria ram esses servidores lotados, ficaram sem memoria ram? qual foi o minimo ? como se comportou? com a capacidade maxima ligada ativos+trabalhando no maximo, como ficou a ram, quero agora tudo o que é relacionado a ram dos servidores, quero saber tudo, quero saber se a ram correu algum risco, ou se ela ta sob controle, quero saber se o limite maximo de contas por servidor ta correto ou devemos diminuir um pouco, se todas as regras sobre memoria ram estao dentro do aceitavel, ou estamos trabalhando errado, enfim, ...
```

---

## Escopo (definição objetiva)

- **Servidores-alvo**:
  - RM4: `hostId=825a4485-1465-4c11-aa18-52f0597b23a3`
  - RM5: `hostId=1b0f6f98-46bf-40c6-a0f9-dad6e1965c22`
  - RM6: `hostId=aba80611-8b8a-46ed-a852-f5f0d573c03b`
- **Janela de análise**: últimos “alguns dias” (vamos extrair o máximo que o log por minuto tiver, e documentar o range real pelo primeiro/último timestamp encontrado).

---

## Perguntas (o que precisamos responder com dados)

1) **RAM sob risco?**
   - qual foi o **mínimo** de `freeMB` e em que horários?
   - houve “stickiness” em modo leve mesmo com `freeMB` alto?

2) **autoMode light/full está coerente?**
   - quais sinais de entrada o governor usa (RAM, lag, etc)?
   - quando o sistema entrou em “light”, qual era a condição real (RAM e demais métricas)?
   - existe histerese/tictac que está “flipando” ou segurando demais?

3) **Capacidade máxima por servidor (N contas) está correta?**
   - RAM mínima observada com N contas em trabalho real
   - margem de segurança (ex.: 20–30% RAM) para picos

4) **Ações sugeridas (sem achismo)**:
   - manter como está / reduzir capacidade / ajustar thresholds / remover tictac / refatorar regras

---

## Evidência a coletar (via CT)

### 1) logs_manifest (descobrir chaves existentes)
- Comando: `logs_manifest` por host (RM4/RM5/RM6).
- Objetivo: listar exatamente quais “keys” existem para:
  - RAM por minuto
  - eventos de autoMode (light/full)
  - status snapshots

### 2) logs (após manifest)
- `status` (para `sys.freeMB/totalMB`, `autoMode`, e carimbo de tempo)
- key específica da telemetria “por minuto” (descoberta via manifest)
- qualquer log de “autoMode transitions”/governor (descoberta via manifest)

---

## Arquivos/funções relacionadas (mapa de impacto)

### `conveniente`
- `C:\conveniente\scripts\worker.js`
  - geração de `status.json` (inclui `sys.freeMB/totalMB`, `autoMode`)
  - regras de governor/autoMode light/full (localizar e citar após coleta)
- `C:\conveniente\scripts\ramPolicy.js` (provável) / `opsState.js` (provável) — confirmar no código

### `sitechatbot` (CT)
- `C:\sitechatbot\index.js`
  - enfileirar `logs_manifest`/`fetch_logs` e armazenar evidências em `C:\sitechatbot\dados\logs\<hostId>\...`

---

## Critério de sucesso

- Produzir um checkup com:
  - min/percentis de RAM livre (por host)
  - timeline de autoMode (light/full) com motivo inferido por regra + métrica real
  - recomendações (capacidade/thresholds) com margem explícita

---

## Coleta realizada (evidência)

- Coletado via CT:
  - `fetch_logs(keys=["governor_snapshots"], tailLines=6000)` para RM4/RM5/RM6
  - `fetch_logs(keys=["status"], tailLines=5000)` para RM4/RM5/RM6
- Arquivos de evidência no CT: ver seção “Evidência (CT)” do checkup abaixo.

---

## Resultados (resumo)

Resumo calculado a partir de `governor_snapshots.jsonl` (1/min) + `status.json` (totalMB atual):

- **RM4**:
  - totalMB≈15870
  - min freeMB=289 (1.8%), p5=1003 (6.3%), p50=1934 (12.2%)
  - lightMinutes=1201 / 2189 (~55%)
  - Conclusão: **risco real** (RAM muito baixa em momentos) → capacidade/infra precisa ajuste.
- **RM5**:
  - totalMB≈16351
  - min freeMB=1772 (10.8%), p5=2290 (14.0%), p50=3528 (21.6%)
  - lightMinutes=0
  - Conclusão: **sob controle** na janela analisada.
- **RM6**:
  - totalMB≈20356
  - min freeMB=2505 (12.3%), p5=3082 (15.1%), p50=4174 (20.5%)
  - lightMinutes=0
  - Conclusão: **RAM ok**, mas houve pico de lag extremo (investigar se afeta produção).

---

## Checkup gerado

- `C:\conveniente\docs\checkups\checkup_2026-02-01_ram_forense_rm4_rm5_rm6.md`

