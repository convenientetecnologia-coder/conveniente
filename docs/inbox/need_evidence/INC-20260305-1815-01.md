### INC-20260305-1815-01 — Blindagem total do navegador (UAFP/fingerprint/geo/rede)

- **P**: P0
- **Sistema**: `conveniente`
- **State**: `need_evidence`
- **Rollout**: `not_deployed`
- **Validation**: `not_run`

---

## Sintoma humano consolidado (impacto de negócio)

- queda de contas em cascata acima do aceitável operacional;
- referência histórica: com setup anterior (antidetect), queda era drasticamente menor;
- necessidade: reduzir queda com resultado rápido e evidência objetiva (sem achismo).

---

## Decisão de arquitetura (fixada)

1) **Congelar engine no canário RM7 em Chromium** (sem zigue-zague de engine durante investigação).
2) **Não migrar os demais hosts agora** (RM1..RM6 permanecem no cenário atual até canário aprovado).
3) **Prioridade máxima**: estabilidade + forense total + redução de quedas (não “trocar engine” repetidamente).

Justificativa:
- troca de `chrome.exe` vs `chromium` isoladamente não resolve queda em cascata;
- queda é multifatorial (comportamento, sessão, pacing, lock/open stability, checkpoints, rede, consistência de identidade técnica).

---

## Meta objetiva (aceite do owner)

- cenário de validação: RM7 lotado;
- alvo operacional: **janela de 24h com no máximo 2 contas caídas**;
- sinal de regressão crítica: >2 quedas em 24h no RM7.

Métrica auxiliar:
- estabilidade de runtime: open-all deve convergir sem travar (sem “abre/fecha preso no 9o”).

---

## Evidência já observada nesta fase (pré-código blindagem)

- RM7 apresentou episódios de travamento de abertura parcial:
  - snapshots com `desired_active=15` e `active=8` (parada em sequência de abertura);
  - eventos repetidos de `nurse_open_attempt` com `nurse_open_denied` em perfis específicos;
  - `open_all` por vezes finalizando parcial com perfis inativos.
- Após hardenings recentes, houve melhora pontual para `active=12`, ainda abaixo do ideal canário.

Fontes usadas:
- `status_node_1`, `desired`, `provision_audit` via CT (`logs_manifest` / `fetch_logs`).

---

## Escopo da blindagem RM7 (olhos de deus)

1) **Identidade técnica por perfil (UAFP/fingerprint)**
   - UA/UA-CH/viewport/DPR/hardware/lang/timezone;
   - consistência entre manifest, runtime aplicado e estado observado.
2) **Coerência geo/rede**
   - geolocalização e timezone alinhados;
   - superfície de vazamento (WebRTC/DNS/sinais incoerentes) auditável.
3) **Convergência operacional**
   - `desired -> open -> work` sem lock preso;
   - ausência de stuck em `activateOnce`/nurse/open_all.
4) **Forense de queda**
   - quando cair conta, reconstruir timeline das últimas horas com causa provável baseada em evidência.

---

## Matriz forense (novo padrão obrigatório)

### A) Timeline por perfil (janela 24h/72h)

Cada perfil deve permitir reconstrução ordenada:
- eventos de open/close/reopen;
- lock decisions (`provision_lock`, owner/kind/ttl);
- mudanças de flags (`loginRequired`, `identity`, `appeal`, `humanHold`, `virtus`);
- falhas de launch (com taxonomia de erro);
- transições de trabalho (`trabalhando` on/off) e motivo.

### B) Taxonomia de causa (normalizada)

Categorias mínimas:
- `launch_failure`
- `lock_contention`
- `session_inconsistency`
- `checkpoint_or_captcha`
- `identity_or_appeal_flow`
- `network_timeout_or_remote_api`
- `manual_human_intervention`

### C) Sinais de risco agregados (por perfil e por host)

- contador de falhas por janela;
- tempo em estado degradado;
- razão de reaberturas/negações;
- variação de consistência de identidade técnica.

---

## Dossiê de implementação (antes de codar) — plano faseado

### Fase 0 — Baseline e contrato de observabilidade (sem mudar comportamento)

- mapear exatamente os eventos já existentes em:
  - `C:\conveniente\scripts\worker.js`
  - `C:\conveniente\scripts\browser.js`
  - `C:\conveniente\scripts\provisionAudit.js`
- definir contrato canônico de evento forense (`event`, `profile`, `flowId`, `cause`, `decision`, `metadata`).

### Fase 1 — Instrumentação “P3 olhos de deus”

- adicionar logs estruturados nos pontos críticos:
  - pré-launch / pós-launch / fail-launch;
  - decisão de lock / bypass / timeout;
  - mudanças de flags de conta;
  - coerência de fingerprint aplicada em runtime.
- expor leitura de timeline por perfil (consulta por janela temporal).

### Fase 2 — Guardrails de estabilidade RM7

- impedir stuck de nurse/open_all por tentativa longa única;
- detecção e recuperação automática de controller órfão/zumbi;
- finalização robusta do ciclo open-all com motivo explícito.

### Fase 3 — Validação canário e laudo diário

- gerar laudo diário RM7:
  - quedas do dia;
  - top causas forenses;
  - perfis com maior risco;
  - comparação com janela anterior.

---

## Não-fazer nesta fase (para reduzir risco)

- não alternar engine em produção a cada tentativa;
- não replicar “session template compartilhada” entre contas;
- não escalar para RM1..RM6 sem aprovação do canário RM7.

---

## Critérios de aprovação para avançar de fase

1) Instrumentação ativa e íntegra (sem lacunas de eventos críticos);
2) open-all convergente e sem travamento recorrente;
3) janela de 24h com <=2 quedas no RM7 lotado.

---

## Rollback operacional

- qualquer regressão: `git revert` do lote de blindagem + `self_update` + restart humano (`node index.js`) no RM7;
- manter trilha forense da regressão no próprio INC antes de novo patch.

---

## Próximo passo imediato

- produzir checkup pré-código detalhando:
  - mapa de eventos existentes,
  - gaps de telemetria por eixo,
  - patch list mínima por fase (F0/F1/F2),
  - plano de validação T+2h / T+24h / T+48h.
- checkup gerado:
  - `C:\conveniente\docs\checkups\checkup_2026-03-05_dossie_blindagem_total_rm7_pre_codigo.md`

---

## Auditoria ponta a ponta — concluída (pré-código)

Resumo objetivo da varredura técnica:

1) fluxo ponta a ponta mapeado (CT enqueue -> dashboard/applyCommands -> api_perfis -> worker/nurse -> browser -> ACK);
2) fontes de verdade inventariadas (desired/status/provision_audit/issues + commands/acks/log-ingest CT);
3) riscos priorizados (P0/P1/P2) identificados com evidência de código e runtime;
4) gaps de blindagem confirmados (coerência UAFP fim-a-fim, WebRTC/DNS, validação forense por sessão);
5) lacunas forenses confirmadas (falta timeline canônica por perfil com correlação automática).

Referência canônica da auditoria:
- `C:\conveniente\docs\checkups\checkup_2026-03-05_dossie_blindagem_total_rm7_pre_codigo.md`

Decisão operacional mantida:
- engine congelada no RM7 (Chromium), sem expansão para RM1..RM6 até bater meta de <=2 quedas/24h no canário lotado.

---

## Execução técnica — Lote 1 (instrumentação forense base)

Implementado (sem alterar lógica de negócio):

1) `C:\conveniente\scripts\worker.js`
- adicionado emitter canônico `appendForensicEvent(...)` em `provision_audit` com campos:
  - `eventType`, `source`, `hostId`, `profileName/nome`, `traceId/flowId`, `outcome`, `severity`, `cause`, `decision`, `meta`.
- instrumentação no fluxo crítico de abertura do nurse:
  - `nurse_open_begin`
  - `nurse_open_end` (com `durationMs`)
  - `nurse_open_ram_denied`
  - `nurse_open_success`

2) `C:\conveniente\scripts\api_status.js`
- novo endpoint local de diagnóstico:
  - `GET /api/perfis/:nome/forensics?hours=24&limit=800&bytesTail=2097152`
- retorna timeline filtrada do `provision_audit` para investigação rápida por perfil.

Validação técnica local:
- parse/sintaxe OK em `worker.js` e `api_status.js`;
- sem erros de lint nos arquivos alterados.

---

## Execução técnica — Lote 2 (forense de queda + detector de flapping)

Implementado:

1) `C:\conveniente\scripts\worker.js`
- dossiê automático de queda em `dados/fall_forensics.jsonl` (`captureFallForensicsSnapshot`);
- classificação de causa (`launch_instability`, `resource_pressure`, `checkpoint_or_login`, etc.);
- trigger automático em pontos críticos:
  - falha de abertura no nurse (`nurse_open_failed`);
  - padrão repetido de abre/fecha/falha (`nurse_open_flapping_suspected`);
  - detecção de `login_required` novo;
  - início de banflow (`banned_detected_predelete`).

2) `C:\conveniente\scripts\api_status.js`
- endpoint `/api/perfis/:nome/forensics` expandido:
  - inclui `fallSnapshots` (dossiê de queda),
  - inclui `causeSummary` e `fallCauseSummary`.

Objetivo operacional do lote:
- responder com evidência quando houver conta “abre/fecha rápido”;
- reduzir investigação no escuro e acelerar ação corretiva por causa real.

### Adendo Lote 2.1 — liberação CT para dossiê de queda

- `C:\conveniente\scripts\dashboard.js` atualizado para permitir a key `fall_forensics` no `fetch_logs/logs_manifest`.
- Resultado esperado: CT passa a conseguir coletar remotamente `dados/fall_forensics.jsonl` (antes retornava `not_allowed`).

---

## Adendo 2026-03-06 21:xx (forense RM7: hipótese de rajada/loop confirmada em evidência)

Escopo da coleta (sem código aplicado nesta etapa):

- host alvo: `29546e77-083e-4c81-b90f-4402499d0fef` (RM7)
- comandos CT:
  - `logs_manifest` cmdId `6ab89ede-d0ae-40ae-8109-f3b3746ecc0e` (`requestId=76d85b0c-7360-42d1-af59-fe775da1e606`)
  - `fetch_logs` cmdId `cbd44791-8887-47e3-9251-b8f8856b0ff1` (`requestId=1340daac-03bc-44c3-b0a4-afdfe13df51f`)
  - `fetch_logs_query` cmdId `96300198-1a0e-4077-a721-2baf93c02d3c` (`requestId=dd2d5668-8009-4eb2-972c-3ff9d33a286e`)
  - `fetch_logs_query` cmdId `399a226c-bea5-42a8-98fd-1f88ad297f64` (`requestId=40f66930-ca53-4625-a0b9-4cad2ca9c98b`)
  - `fetch_logs` cmdId `51001f84-ae25-43ef-9d05-ac9f859b20d9` (`requestId=cc667244-0bea-4445-8b2e-d83d66cba745`)

Evidências objetivas encontradas:

1) **Rajada repetitiva de nurse open** em dois perfis específicos, com ciclo curto (~9.8s a ~13.4s):
- `franca-1772253971467`
- `cascavel-1771110668314`
- assinatura recorrente por ciclo:
  - `nurse_open_begin`
  - `nurse_open_end` (erro)
  - `fall_forensics_snapshot`
  - `nurse_open_denied`
  - `nurse_open_ram_denied`
- no recorte de 5000 linhas: 1000 ocorrências de cada evento acima (total 5000), 4880 bursts <=15s.

2) **Causa dominante registrada** em `fall_forensics`:
- `supervisor_denied:ram_low` (517 ocorrências no recorte de 673 eventos)
- secundário: `maintenance_provision` (110)
- também aparecem `already_opening` (2), coerente com competição/pressão de abertura.

3) **Scanner de login_required em alta frequência**:
- `login_required_events` com 3999 linhas no recorte;
- 28 perfis; top perfis com `~148 scans` cada;
- mediana de intervalo por perfil ~10s (`medianDeltaMs` ~10.0s);
- perfil `taubate-1772081222212` com `lrHits=148/148` (login form persistente).

4) **Snapshot de runtime remoto (RM7)**:
- 30 perfis ativos no status;
- 18 perfis em estado de risco operacional (issues>0 ou virtus offline);
- destaques:
  - `petrolina-1771560719856` (`issuesCount=117`)
  - `florianopolis-1764625643701` (`issuesCount=112`)
  - `taubate-1772081222212` (`virtusOnline=false`, login_form persistente nos scans).

Leitura técnica (sem achismo, baseada nos eventos acima):

- existe sim padrão de **ação cíclica curta** no runtime (principalmente nurse/open sob `ram_low`);
- esse padrão não prova sozinho o texto exato de UI ("recurso usado de forma indevida"), porém confirma uma condição de **pressão/repetição** compatível com degradação de confiança de sessão;
- ponto prioritário de investigação/fix: **reduzir frequência e repetição de tentativas de reabertura** para perfis em `ram_low/maintenance_provision`, com backoff mais longo e lock de tentativa por perfil.

Checkup canônico desta rodada:
- `C:\conveniente\docs\checkups\checkup_2026-03-06_dossie_forense_anti_rajada_rm7.md`

---

## Adendo 2026-03-07 — F0 aplicado (contenção mínima anti-rajada no nurse/open)

Escopo desta etapa (código aplicado):

- arquivo alterado: `C:\conveniente\scripts\worker.js`
- foco: cortar ciclos curtos de `nurse_open_*` em perfis sob `ram_low/supervisor_denied`.

Mudanças objetivas:

1) **Backoff progressivo por perfil em negação de abertura por RAM/supervisor**
- substituído o backoff fixo de `3s` por backoff escalonado com jitter.
- defaults:
  - `NURSE_OPEN_BACKOFF_MIN_MS=120000` (2 min)
  - `NURSE_OPEN_BACKOFF_MAX_MS=2700000` (45 min)
  - `NURSE_OPEN_BACKOFF_GROWTH=2`
  - `NURSE_OPEN_BACKOFF_JITTER_MS=20000`
- comportamento: falha repetida em `ram_low/slots/headroom` aumenta janela de hold (`activationHeldUntil`) por perfil.

2) **Gate explícito de tentativa durante hold**
- quando perfil está em `activationHeldUntil`, o nurse não tenta abrir novamente.
- telemetria adicionada para auditoria da decisão de pular tentativa (`nurse_open_backoff_skip`, debounce 60s por perfil).

3) **Telemetria de aplicação de backoff**
- novo evento em `provision_audit`: `nurse_open_backoff_applied` com:
  - `nome`, `streak`, `backoffMs`, `prevBackoffMs`, `reason`, `swapped`.
- objetivo: provar timing e convergência sem paralelismo agressivo.

4) **Reset de estado quando abre com sucesso**
- em `nurse_open_success`: zera `openBackoffMs` e `openDeniedStreak` do perfil.

Critério técnico de validação (canário RM7):

- queda relevante de `nurse_open_denied` e `nurse_open_ram_denied` por hora;
- ausência de bursts <=15s para o mesmo perfil;
- aumento de `nurse_open_backoff_skip`/`nurse_open_backoff_applied` nos perfis com pressão de RAM (esperado na contenção).

Status:

- F0 (contenção anti-rajada): **aplicado em código**.
- pendente operacional: commit/push + `self_update` + restart no host canário (RM7).

---

## Adendo 2026-03-07 — P1 aplicado (cadência humana de LR scan)

Escopo desta etapa (código aplicado):

- arquivo alterado: `C:\conveniente\scripts\worker.js`
- foco: remover varredura agressiva de `loginRequired` em ciclo de segundos.

Mudanças objetivas:

1) **Cadência por estado para `lr_scan_tabs`**
- perfil saudável: base `5 min` (`LR_SCAN_HEALTHY_BASE_MS`, default `300000`) + jitter (`LR_SCAN_HEALTHY_JITTER_MS`, default `30000`);
- perfil em risco (`loginRequired/loginRemediateFailed/messengerPin/twoFactor/identity/appeal`): base `20 min` (`LR_SCAN_RISK_BASE_MS`, default `1200000`) + jitter (`LR_SCAN_RISK_JITTER_MS`, default `60000`).

2) **Gate de varredura**
- quando ainda está no cooldown do scan, o nurse não executa nova varredura de abas FB/Messenger;
- evento de auditoria (debounced): `lr_scan_deferred`.

3) **Telemetria de cadência aplicada**
- novo evento: `lr_scan_cadence_applied` com `tier`, `nextAt` e `waitMs`.

Leitura técnica:

- a detecção continua existindo (não ficamos cegos), porém saiu do modo “pressão contínua em segundos” para um pacing humano;
- isso reduz superfície de ação repetitiva sem quebrar a fila global de correção.

Status:

- F1 (scan cadence anti-pânico): **aplicado em código**.
- pendente operacional: commit/push + `self_update` + restart no RM7.

---

## Adendo 2026-03-07 — P2 safe-mode aplicado (governança recovery + fail-safe de conta saudável)

Escopo desta etapa (código aplicado):

- arquivos alterados:
  - `C:\conveniente\scripts\worker.js`
  - `C:\conveniente\scripts\virtus.js`
  - `C:\conveniente\scripts\robe.js`

Mudanças objetivas:

1) **Recovery queue com estado único por perfil (sem competição simultânea)**
- `enqueueRecoveryAction` agora evita múltiplos itens ativos do mesmo perfil em paralelo;
- quando chegar novo tipo de recovery para perfil já em fila:
  - item `pending` troca de tipo sem resetar `nextEligibleAt`;
  - item `running` registra transição para o próximo tipo (follow-up), executado na sequência.

2) **Fail-safe anti “conta saudável viva e parada”**
- guardrail no nurse: se perfil está saudável, sem bloqueio real e sem recovery pendente, mas `desired.virtus=off`, o sistema reativa `virtus` automaticamente;
- evento forense: `virtus_off_auto_heal_guardrail`.

3) **Fila global de recovery acelerada (pós-crise)**
- defaults alterados para:
  - `RECOVERY_QUEUE_DELAY_MIN_MS=900000` (15 min)
  - `RECOVERY_QUEUE_DELAY_MAX_MS=1800000` (30 min)

4) **Robe cooldown recalibrado**
- cooldown padrão alterado de `30–45 min` para `20–35 min`.

5) **Debug runtime legado desativado por padrão**
- `dbg_agent_runtime` (`stage2/3-pre-fix`) só roda se `LEGACY_RUNTIME_DEBUG=1`;
- default em produção: desligado.

Garantias operacionais da etapa:

- `Virtus` e `Robe` não entram em orçamento/cooldown de ações corretivas;
- transições naturais (`LR -> captcha -> identity -> appeal`) continuam possíveis, porém serializadas por perfil;
- objetivo explícito: reduzir pânico sem congelar conta saudável.

