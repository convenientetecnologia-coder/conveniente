### INC-20260203-2400-01 - P1 - CT Chat: composer auto-grow + Ver mais em mensagens grandes

- **P**: P1 (melhora UX/operacional grande no chat)
- **Sistema**: `sitechatbot` (CT) - modulo `convenientetecnologia`
- **State**: `in_progress`
- **Rollout**: `needs_restart`
- **Validation**: `not_run`
- **Precisa reiniciar agora?** sim (CT)
- **Como reiniciar (humano)?** `node index.js`
- **Por que?** para carregar ajustes de UI do chat (autogrow + ver mais).

---

## Relato (humano)

No chat do CT:

1) Ao escrever, o composer precisa crescer automaticamente conforme o texto aumenta, para nao sumir o que esta sendo digitado. Teto sugerido: 8 linhas.
2) Quando uma mensagem for muito grande, mostrar botao Ver mais (e permitir recolher com Ver menos).

---

## Hipoteses (debug / evidencia)

- H1: #ctChatComposerInput esta com altura fixa/overflow; falta handler no input para ajustar height baseado em scrollHeight.
- H2: layout do painel precisa que o composer ajuste maxHeight/overflowY para nao quebrar scroll das mensagens.
- H3: bolhas renderizam texto inteiro; precisamos truncamento por chars/linhas com toggle por mensagem.

Evidencia a coletar:
- NDJSON via debug ingest: eventos chat_autogrow_v1 e chat_vermais_v1 (sem PII).

---

## Implementacao (mudanca minima)

- Arquivos:
  - C:\sitechatbot\convenientetecnologia\public\ct.js (chat composer + render de mensagens)
  - C:\sitechatbot\convenientetecnologia\public\ct.css (estilos do composer e do ver mais)

### Requisito 1 - composer auto-grow (ate 8 linhas)

- Autogrow no #ctChatComposerInput:
  - a cada input: height=auto; depois height=min(scrollHeight, maxPx).
  - se exceder teto: overflowY=auto.

### Requisito 2 - Ver mais em mensagens grandes

- No render das mensagens:
  - acima de um threshold, renderizar truncado + botao Ver mais.
  - toggle por mensagem (data-* e/ou map in-memory).

---

## Validacao (runtime)

Apos reiniciar CT:

1) Abrir Chat no CT.
2) Digitar texto com varias linhas -> o composer deve crescer ate ~8 linhas.
3) Visualizar mensagem grande -> deve aparecer Ver mais e expandir/recolher.

---

## Rollback

- Reverter alteracoes em ct.js/ct.css e reiniciar node index.js no CT.

