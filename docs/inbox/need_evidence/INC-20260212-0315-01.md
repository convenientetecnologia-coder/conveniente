### INC-20260212-0315-01 — RM2: “wipe”/sobrescrita de perfis (perfis.json caiu de ~100 para 7) — investigação forense (aberto)

- **P**: P0
- **Sistema**: `conveniente` (+ CT para evidência)
- **Status**: `need_evidence`
- **Rollout**: `not_deployed` (investigação/guardrails ainda não aplicados)
- **Validation**: `not_run`

---

## Sintoma (humano)

- RM2 operava com ~100 contas.
- Em algum momento (provavelmente durante cadastro/provision), o servidor passou a mostrar **apenas 7 perfis** no CT/dashboard.
- Restart confirmou que o sistema “começou do zero” (arquivos JSON foram sobrescritos), e depois cadastrou 7 contas novas.
- Último backup “bom” identificado:
  - `C:\conveniente\_backup_auto\20260211_181809` (11/02 ~ 18:18)

---

## Impacto

- P0: perda operacional grande (contas paradas; risco de “reservar/cadastrar” em loop; perda de continuidade do host).
- Risco sistêmico: **não pode existir nenhuma hipótese** em que o sistema apague/sobrescreva `perfis.json/desired.json` de forma silenciosa.

---

## Evidência já coletada (até agora)

- Backup RM2 detectado via `backup_restore_probe`:
  - `backupPerfisCount=103`
  - `backupDesiredCount=107`
  - Backup dir: `C:\conveniente\_backup_auto\20260211_181809`
- Estado “quebrado” antes do restore (apenas 7 perfis no runtime):
  - CT presnap: `C:\sitechatbot\dados\logs\bcf01e8d-82da-4d5d-aed0-d60305d4696d\rm2_restore_presnap2_1770868834020.json`
- Recuperação operacional (restore) foi aplicada e validada em INC separado:
  - `docs/inbox/done/INC-20260212-0240-01.md`

- Queries forenses (CT → RM2) já coletadas (parcial):
  - `provision_audit` (grep) — `rm2_wipe_forensic_1770871034248_audit.json` (CT):
    - `C:\sitechatbot\dados\logs\bcf01e8d-82da-4d5d-aed0-d60305d4696d\rm2_wipe_forensic_1770871034248_audit.json`
  - `logger` (grep) — retornou `ok=false` (provável arquivo ausente/rotação); precisa revalidar com `logs_manifest`/`health_manifest`:
    - `C:\sitechatbot\dados\logs\bcf01e8d-82da-4d5d-aed0-d60305d4696d\rm2_wipe_forensic_1770871034248_logger.json`

---

## Hipóteses principais (a provar)

### H1) Sobrescrita por shard (cluster)

- Algum fluxo de escrita gravou `C:\conveniente\dados\perfis.json` usando um subconjunto “do shard” (ex.: 7 perfis) em vez do conjunto global.
- Este é um risco conhecido em ambientes sharded se algum caminho usar `loadPerfisJson()` (filtrado por shard) e depois `savePerfisJson()` para gravar o arquivo global.

### H2) Migração/cleanup indevido

- Algum comando (ex.: `profiles_cleanup`, `migrate_profiles`, `delete_perfis` indevido) limpou entradas ou reescreveu os JSON.

### H3) Corrupção/IO parcial (write não-atômico ou corrida)

- `perfis.json` foi truncado/corrompido por write concorrente/crash no meio do write, levando o sistema a “se recuperar” criando arquivo novo pequeno.

---

## Evidência a coletar (próxima sessão)

### 1) Linha do tempo exata (quando caiu de ~100 para 7)

- MTIME de:
  - `C:\conveniente\dados\perfis.json`
  - `C:\conveniente\dados\desired.json`
- Janelas de backup em `C:\conveniente\_backup_auto\` para delimitar o intervalo.

### 2) Logs do executor (quem escreveu / por quê)

Usar CT (RM2) `fetch_logs_query`:
- key=`logger` por padrões:
  - `perfis.json`
  - `desired.json`
  - `GUARD`
  - `savePerfisJson`
  - `migrate_profiles`
  - `profiles_cleanup`
  - `delete_perfis`
- key=`provision_audit` por padrões:
  - `profiles_cleanup`
  - `migrate_profiles`
  - `delete_perfis`
  - `stock_provision`
  - `boot_reset`

### 3) Auditoria de comandos CT (se houve comando destrutivo)

- Verificar `sitechatbot/dados/logs/<hostId>/ack_*.json` no intervalo por `cmdType=delete_perfis|profiles_cleanup|migrate_profiles`.

---

## Blindagem (proposta — só após evidência)

- Guardrail “anti-wipe” para `perfis.json`:
  - bloquear shrink abrupto (ex.: cair >X% ou >N perfis) sem override explícito via env (modo manutenção).
  - gravar audit estruturado (antes/depois, caller/opId, stack curta) em `provision_audit`.
- Telemetria obrigatória:
  - evento `perfis_json_shrink_detected` (com contagens e hash) para não existir wipe silencioso.

---

## Arquivos relevantes (para inspeção)

- `C:\conveniente\scripts\fileStore.js` (`loadPerfisJson`/`savePerfisJson`, IO atômico e guardrails)
- `C:\conveniente\scripts\worker.js` (shards + quaisquer caminhos que escrevam `perfis.json`)
- `C:\conveniente\scripts\dashboard.js` (comandos destrutivos: `delete_perfis`, `profiles_cleanup`, `migrate_profiles`)

