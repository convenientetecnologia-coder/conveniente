### INC-20260130-0103-01 — P0: CT “estoque/servidores → liberar” liberava cooldown de ROBE + RM3 não cadastrava (pause_timeout)

- **P**: P0
- **Data**: 2026-01-30
- **Sistema**: CT `sitechatbot` + hosts `conveniente`
- **Host alvo (teste)**: ROBE MÃE 3 — hostId `5d7c3309-8581-4a50-a421-e6cbb52d8070`

---

## Status (rígido)

- **Estado atual**: `done`
- **Rollout**: `needs_restart`
- **Validation**: `not_run`

---

## Sintoma (humano)

- No CT/Estoque/Servidores, ao “liberar” servidor, ocorreu efeito colateral proibido: liberar cooldown do Robe.
- No mesmo teste, o `stock_provision` falhou e a conta não cadastrou no RM3.

---

## Evidência (CT + RM3)

### 1) “Liberar” disparou `robes_release_all` (efeito colateral)

- **CT enqueue**: `robes_release_all`
  - `cmdId=fc984608-6440-4831-abac-32c7023e377d`
  - `target=RM3 (5d7c...)`
  - `ackOk=true`

### 2) `stock_provision` falhou por `pause_timeout`

- **CT enqueue**: `stock_provision`
  - `cmdId=9101ea87-67ba-49ff-a3c3-40a60d4892dd`
  - `target=RM3 (5d7c...)`
  - `ackOk=false`
- **ACK details (RM3)**: erro `pause_timeout count=20` (falha na etapa `quiesce_pause_done`)

---

## Correções entregues

### Fix A (CT `sitechatbot`): “liberar servidor” NÃO mexe em Robe/Virtus

- **Mudança**: `POST /api/stock/servers/release_all` passa a apenas zerar o cooldown do servidor no CT.
- **Removido**: `enqueueCommand(... { type: 'robes_release_all' })` desse endpoint.
- **Melhoria**: aceita `hostIds` para “liberar selecionados”.
- **UI**: o modal agora explicita que não mexe em Robe/Virtus.

> Observação: CT não é git neste workspace; fix é patch local e requer restart do CT.

### Fix B (`conveniente`): `stock_provision` não falha por não conseguir “pausar Virtus”

- **Mudança**: a etapa `quiesce_pause` virou best-effort e não dá mais `pause_timeout`.
- **Commit**: `93b1c7e`
- **Self_update**: RM3 `cmdId=4c34605d-4bd3-42a8-a4f8-088acd781e0b` `ackOk=true`

---

## Próximo passo (pra valer em runtime)

- **CT (sitechatbot)**: reiniciar `node index.js` (carregar Fix A)
- **Hosts conveniente** (principal: RM3): reiniciar `node index.js` (carregar Fix B)

