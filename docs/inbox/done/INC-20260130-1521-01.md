### INC-20260130-1521-01 — P0: CT/Servidores mostra “Desconhecido” e precisa virar fonte de verdade operacional (flags + estados acionáveis)

- **P**: P0
- **Data**: 2026-01-30
- **Sistema**: `sitechatbot` (CT + UI Servidores) + `conveniente` (origem dos flags/telemetria por perfil)

---

## Status (rígido)

- **Estado atual**: `done`
- **Rollout**: `manual_step_required` (precisa restart do CT para valer no runtime)
- **Validation**: `not_run`

---

## Sintoma (humano)

- No CT → menu **Servidores**, aparecia estado **“Desconhecido”**.
- O usuário usa o menu **Servidores** como fonte de verdade para decidir qual host ir (ex.: “ROBE MÃE 2 tem 5 humano invocado”, “ROBE MÃE 3 tem 1 captcha”).
- Hoje os estados/contadores precisam ser 110% acionáveis/confiáveis.

---

## Evidências (sem achismo)

### 1) Causa do “Desconhecido” no dashboard

- UI do CT adicionava pill “Desconhecido” com base em `accountsAgg.unknown`:
  - `C:\sitechatbot\public\index.html` (`addPill('Desconhecido', n('unknown'), ...)`)

- O classificador central retornava `kind:'unknown'` quando `loginRequired=true` e o `loginReason` não era reconhecido:
  - `C:\sitechatbot\convenientetecnologia\lib\fbAccountState.js`

### 2) Motivos reais existiam no snapshot (exemplo)

- `C:\sitechatbot\dados\5d7c3309-8581-4a50-a421-e6cbb52d8070-30b3fe928b.json`
  - perfis com `loginRequired: true`
  - `loginReason: "probe_failed"`

---

## Implementado (mudança mínima e auditável)

### A) Catalogação/renomeação do estado

- `unknown` → `login_other` (**Outros (login)**), mantendo o `loginReason` técnico para auditoria:
  - `C:\sitechatbot\convenientetecnologia\lib\fbAccountState.js`

### B) Flags operacionais no `/servers`

- CT passou a expor `flagsAgg` por host (somente quando `onlineRaw=true`), incluindo:
  - `human_invoked` (perfis com `humanControl=true` ou `humanHold=true`)
  - `login_reasons_top` (top 8 motivos `loginReason`, para explicar “Outros (login)” sem payload gigante)
  - `C:\sitechatbot\index.js` (rota `GET /servers`)

### C) UI “Servidores” (card)

- Remove “Desconhecido” e passa a mostrar:
  - **Humano invocado**
  - **Outros (login)** (com tooltip `loginReason top`)
  - `C:\sitechatbot\public\index.html`

### D) UI “Contas Facebook” (contas-facebook-contas)

- Atualiza catálogo para `login_other`:
  - `C:\sitechatbot\convenientetecnologia\public\contas-facebook-contas.html`

---

## Rollout / Restart (humano)

- **Precisa reiniciar agora?** sim (se quiser ver a correção no CT já)
- **Precisa reiniciar para validar?** sim
- **Qual projeto?** `sitechatbot` (CT)
- **Como reiniciar (humano)?** `node index.js`
- **Por quê?** o CT é um processo Node; mudanças em `index.js`/HTML só entram após restart.

---

## Validação (operador GPT, após o “reiniciado”)

- `GET http://127.0.0.1:3000/servers` deve conter:
  - `servidores[].accountsAgg.login_other` (em vez de `unknown`)
  - `servidores[].flagsAgg.human_invoked`
  - `servidores[].flagsAgg.login_reasons_top`
- UI CT → menu **Servidores**:
  - não deve existir pill “Desconhecido”
  - deve existir “Outros (login)” e “Humano invocado” quando aplicável

---

## Rollback

Observação importante (evidência): `C:\sitechatbot` não está em Git neste host (não existe `.git`), então rollback canônico é:

- restaurar arquivos a partir do backup local (`C:\sitechatbot\_backup_auto_root\...`) e reiniciar `node index.js` no CT.

