### INC-20260305-1815-01 — Blindagem total do navegador (UAFP/fingerprint/geo/rede)

- **P**: P0
- **Sistema**: `conveniente`
- **State**: `need_evidence`
- **Rollout**: `not_deployed`
- **Validation**: `not_run`

---

## Sintoma humano consolidado (impacto de negócio)

- queda de contas em cascata acima do aceitável operacional;
- referência histórica: com setup anterior (antidetect), queda era drasticamente menor;
- necessidade: reduzir queda com resultado rápido e evidência objetiva (sem achismo).

---

## Decisão de arquitetura (fixada)

1) **Congelar engine no canário RM7 em Chromium** (sem zigue-zague de engine durante investigação).
2) **Não migrar os demais hosts agora** (RM1..RM6 permanecem no cenário atual até canário aprovado).
3) **Prioridade máxima**: estabilidade + forense total + redução de quedas (não “trocar engine” repetidamente).

Justificativa:
- troca de `chrome.exe` vs `chromium` isoladamente não resolve queda em cascata;
- queda é multifatorial (comportamento, sessão, pacing, lock/open stability, checkpoints, rede, consistência de identidade técnica).

---

## Meta objetiva (aceite do owner)

- cenário de validação: RM7 lotado;
- alvo operacional: **janela de 24h com no máximo 2 contas caídas**;
- sinal de regressão crítica: >2 quedas em 24h no RM7.

Métrica auxiliar:
- estabilidade de runtime: open-all deve convergir sem travar (sem “abre/fecha preso no 9o”).

---

## Evidência já observada nesta fase (pré-código blindagem)

- RM7 apresentou episódios de travamento de abertura parcial:
  - snapshots com `desired_active=15` e `active=8` (parada em sequência de abertura);
  - eventos repetidos de `nurse_open_attempt` com `nurse_open_denied` em perfis específicos;
  - `open_all` por vezes finalizando parcial com perfis inativos.
- Após hardenings recentes, houve melhora pontual para `active=12`, ainda abaixo do ideal canário.

Fontes usadas:
- `status_node_1`, `desired`, `provision_audit` via CT (`logs_manifest` / `fetch_logs`).

---

## Escopo da blindagem RM7 (olhos de deus)

1) **Identidade técnica por perfil (UAFP/fingerprint)**
   - UA/UA-CH/viewport/DPR/hardware/lang/timezone;
   - consistência entre manifest, runtime aplicado e estado observado.
2) **Coerência geo/rede**
   - geolocalização e timezone alinhados;
   - superfície de vazamento (WebRTC/DNS/sinais incoerentes) auditável.
3) **Convergência operacional**
   - `desired -> open -> work` sem lock preso;
   - ausência de stuck em `activateOnce`/nurse/open_all.
4) **Forense de queda**
   - quando cair conta, reconstruir timeline das últimas horas com causa provável baseada em evidência.

---

## Matriz forense (novo padrão obrigatório)

### A) Timeline por perfil (janela 24h/72h)

Cada perfil deve permitir reconstrução ordenada:
- eventos de open/close/reopen;
- lock decisions (`provision_lock`, owner/kind/ttl);
- mudanças de flags (`loginRequired`, `identity`, `appeal`, `humanHold`, `virtus`);
- falhas de launch (com taxonomia de erro);
- transições de trabalho (`trabalhando` on/off) e motivo.

### B) Taxonomia de causa (normalizada)

Categorias mínimas:
- `launch_failure`
- `lock_contention`
- `session_inconsistency`
- `checkpoint_or_captcha`
- `identity_or_appeal_flow`
- `network_timeout_or_remote_api`
- `manual_human_intervention`

### C) Sinais de risco agregados (por perfil e por host)

- contador de falhas por janela;
- tempo em estado degradado;
- razão de reaberturas/negações;
- variação de consistência de identidade técnica.

---

## Dossiê de implementação (antes de codar) — plano faseado

### Fase 0 — Baseline e contrato de observabilidade (sem mudar comportamento)

- mapear exatamente os eventos já existentes em:
  - `C:\conveniente\scripts\worker.js`
  - `C:\conveniente\scripts\browser.js`
  - `C:\conveniente\scripts\provisionAudit.js`
- definir contrato canônico de evento forense (`event`, `profile`, `flowId`, `cause`, `decision`, `metadata`).

### Fase 1 — Instrumentação “P3 olhos de deus”

- adicionar logs estruturados nos pontos críticos:
  - pré-launch / pós-launch / fail-launch;
  - decisão de lock / bypass / timeout;
  - mudanças de flags de conta;
  - coerência de fingerprint aplicada em runtime.
- expor leitura de timeline por perfil (consulta por janela temporal).

### Fase 2 — Guardrails de estabilidade RM7

- impedir stuck de nurse/open_all por tentativa longa única;
- detecção e recuperação automática de controller órfão/zumbi;
- finalização robusta do ciclo open-all com motivo explícito.

### Fase 3 — Validação canário e laudo diário

- gerar laudo diário RM7:
  - quedas do dia;
  - top causas forenses;
  - perfis com maior risco;
  - comparação com janela anterior.

---

## Não-fazer nesta fase (para reduzir risco)

- não alternar engine em produção a cada tentativa;
- não replicar “session template compartilhada” entre contas;
- não escalar para RM1..RM6 sem aprovação do canário RM7.

---

## Critérios de aprovação para avançar de fase

1) Instrumentação ativa e íntegra (sem lacunas de eventos críticos);
2) open-all convergente e sem travamento recorrente;
3) janela de 24h com <=2 quedas no RM7 lotado.

---

## Rollback operacional

- qualquer regressão: `git revert` do lote de blindagem + `self_update` + restart humano (`node index.js`) no RM7;
- manter trilha forense da regressão no próprio INC antes de novo patch.

---

## Próximo passo imediato

- produzir checkup pré-código detalhando:
  - mapa de eventos existentes,
  - gaps de telemetria por eixo,
  - patch list mínima por fase (F0/F1/F2),
  - plano de validação T+2h / T+24h / T+48h.
- checkup gerado:
  - `C:\conveniente\docs\checkups\checkup_2026-03-05_dossie_blindagem_total_rm7_pre_codigo.md`

---

## Auditoria ponta a ponta — concluída (pré-código)

Resumo objetivo da varredura técnica:

1) fluxo ponta a ponta mapeado (CT enqueue -> dashboard/applyCommands -> api_perfis -> worker/nurse -> browser -> ACK);
2) fontes de verdade inventariadas (desired/status/provision_audit/issues + commands/acks/log-ingest CT);
3) riscos priorizados (P0/P1/P2) identificados com evidência de código e runtime;
4) gaps de blindagem confirmados (coerência UAFP fim-a-fim, WebRTC/DNS, validação forense por sessão);
5) lacunas forenses confirmadas (falta timeline canônica por perfil com correlação automática).

Referência canônica da auditoria:
- `C:\conveniente\docs\checkups\checkup_2026-03-05_dossie_blindagem_total_rm7_pre_codigo.md`

Decisão operacional mantida:
- engine congelada no RM7 (Chromium), sem expansão para RM1..RM6 até bater meta de <=2 quedas/24h no canário lotado.

---

## Execução técnica — Lote 1 (instrumentação forense base)

Implementado (sem alterar lógica de negócio):

1) `C:\conveniente\scripts\worker.js`
- adicionado emitter canônico `appendForensicEvent(...)` em `provision_audit` com campos:
  - `eventType`, `source`, `hostId`, `profileName/nome`, `traceId/flowId`, `outcome`, `severity`, `cause`, `decision`, `meta`.
- instrumentação no fluxo crítico de abertura do nurse:
  - `nurse_open_begin`
  - `nurse_open_end` (com `durationMs`)
  - `nurse_open_ram_denied`
  - `nurse_open_success`

2) `C:\conveniente\scripts\api_status.js`
- novo endpoint local de diagnóstico:
  - `GET /api/perfis/:nome/forensics?hours=24&limit=800&bytesTail=2097152`
- retorna timeline filtrada do `provision_audit` para investigação rápida por perfil.

Validação técnica local:
- parse/sintaxe OK em `worker.js` e `api_status.js`;
- sem erros de lint nos arquivos alterados.

