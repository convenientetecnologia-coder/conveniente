### INC-20260214-0930-01 — Host anti-wipe total: inventário de writes + guardrails uniformes (`perfis.json`/`desired.json`)

- **P**: P0
- **Sistema**: `conveniente` (hosts)
- **Status**: `cancelled`
- **Rollout**: `not_deployed`
- **Validation**: `not_run`

---

## Encerramento (fase 2)

Escopo reclassificado para fase 2.
Não bloqueia o GO do programa único de cadastro sem duplicação (`INC-20260214-0900-01`).

## Contexto

Os wipes históricos de `perfis.json` foram P0. Já existe blindagem em alguns caminhos, mas precisamos garantir:

- **nenhuma** rota/worker/bootpath consegue escrever estado destrutivo por fallback/silêncio
- todas as escritas críticas usam lock + write atômico + guardrails consistentes

---

## Escopo

### In-scope

- Mapear TODOS os pontos de escrita/leitura de:
  - `dados/perfis.json`
  - `dados/desired.json`
  - `dados/status.json` (somente o que influencia painel/estoque)
- Garantir que:
  - nenhum `readJsonSafe(..., [])` alimenta write destrutivo
  - `withPerfisFileLockUpdate` é o único caminho para mutação de perfis (onde aplicável)
  - guardrails são coerentes (empty/tiny-after/unreadable)

### Out-of-scope

- Provision CT↔host: `INC-20260214-0900-01`
- Canal de comandos/ACK: `INC-20260214-0910-01`

---

## Evidência requerida

- Lista completa de “call sites” (arquivo+função) que:
  - leem `perfis.json`/`desired.json`
  - escrevem `perfis.json`/`desired.json`
- Prova por categoria:
  - boot paths (startup)
  - rotas HTTP (`api_perfis`, etc.)
  - workers/reconciliadores

---

## Critério de “done”

- 0 caminhos “silenciosos” que possam zerar `perfis.json`
- write atômico real (sem janela de perda) em todas as gravações críticas
- locks e retries aplicados consistentemente
- telemetria/ledger suficiente para reconstruir causa de qualquer tentativa de wipe

