### INC-20260130-0023-01 — RM3: estoque liberou conta, mas cadastro/provision falhou (não cadastrou)

- **P**: P0 (impacta cadastro/estoque em produção)
- **Data**: 2026-01-30
- **Sistema**: `conveniente` + CT (`sitechatbot`)
- **Host alvo**: ROBE MÃE 3 — hostId `5d7c3309-8581-4a50-a421-e6cbb52d8070`

---

## Status (rígido)

- **Estado atual**: `done`
- **Rollout**: `needs_restart`
- **Validation**: `not_run`

---

## Sintoma (humano)

- No RM3, o estoque “servidores” liberou uma conta para cadastrar.
- O RM3 recebeu, mas **não cadastrou** e ocorreu algum erro.

---

## Evidência coletada (2026-01-30)

- **CT enqueue**: `cmdId=ae137d13-b5dc-4d39-ae7d-5b56bfa8ecbe` (`stock_provision`) para hostId `5d7c3309-8581-4a50-a421-e6cbb52d8070`
- **CT ack**: `ackOk=false` com erro:
  - `provision_lock_busy owner=human_reconcile_login_form:indaiatuba-1769668565265:1769743131171`

Interpretação:
- O RM3 estava com `provision_lock` ativo (fluxo de `human_reconcile_login_form`) no momento em que o CT liberou a conta para cadastro.
- O `stock_provision` **falhou imediatamente** ao tentar adquirir o lock, em vez de aguardar dentro do budget.

---

## Correção aplicada (mínima, owner-safe)

Em `conveniente/scripts/dashboard.js`:
- `execStockProvision` passa a **aguardar o `provision_lock` ficar livre** (com backoff leve) até esgotar `STOCK_PROVISION_BUDGET_MS`.
- Em caso de timeout, retorna erro com evidência: `waitedMs` e `attempts`.
- Adiciona evento de auditoria: `stock_provision_wait_provision_lock`.

---

## Entrega / Deploy evidence

- **Commit**: `5cfd6e8` (`fix(p0): wait provision lock for stock_provision`)
- **CT self_update (RM3)**:
  - `cmdId=07ea5fbf-4178-451d-82cd-f92c44a7bd92`
  - `ackOk=true` (aplicou pull no host)

---

## Próximo passo (para valer no processo)

- **Precisa reiniciar?** **Sim**
- **Qual projeto?** `conveniente` (ROBE MÃE 3)
- **Como reiniciar (humano)?** `node index.js`
- **Por quê?** carregar o código novo que faz `stock_provision` aguardar `provision_lock` em vez de falhar imediato.

